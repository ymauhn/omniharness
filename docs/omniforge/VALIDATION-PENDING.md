# Deferred validation register

Owner authority: 2026-09-25. Continue toward a functional MVP with minimal intervention. Nonblocking checks that cannot currently run may be deferred to one final validation round. Do not fabricate passes, skip installable dependencies, enable a known unsafe path, or erase acceptance requirements. A prototype can be usable while a provider or release certificate remains unverified.

| ID | Check and present evidence | Why pending / impact | Final action and pass condition |
|---|---|---|---|
| V-01 | Local Lab and mascot visual/DOM verification. Mascot has two skins, three themes and 7 passing behavior checks; Agentes and new graph hover/focus help have deterministic DOM seams. [Isolated demo](../archive/DEMO-CHECKPOINT.md) and [guided sheet](GUIDED-ACCEPTANCE.md) are ready for actual observation. | Native browser navigation was denied by platform review despite the owner's exact connector consent. Passive native Windows inventory found no matching already-open window. No current viewport, keyboard/DOM, motion, contrast or CPU/FPS verdict. The owner plans Claude-assisted visual acceptance. Optional later experiment: opt-in mascot following the pointer/themed cursor, only if low effort and respectful of text cursor, focus and reduced motion. | Once a permitted browser route or an owner-opened target is available, inspect both skins/themes, pause/selection/apply/undo, Off, focus, narrow layout and reduced motion; include Agentes import/review/derive/pin, graph hover/focus/scroll/help and scope switches. Keep real captures and distinguish static appearance from interaction/performance observations. Do not repeat a denied navigation via another backend. Pointer-follow/cursor styling is optional, not a release blocker. |
| V-02 | JEV live typed classification and consumption. [Adapter](CLASSIFIER-ADAPTERS.md) integrated after two independent reviews; 20 shared Laya/JEV offline adversarial checks. | Remote account/key/credits not configured for this task. No live response, latency or consumption evidence. The owner expressly permits this check at the final round. | Owner connects the provider and supplies applicable credit authority. Run bounded sanitized positive/negative/ambiguous PT/EN cases, invalid/timeout fallback, record observed usage and compare against the same frozen local baseline. Until then label the adapter unverified/disconnected. |
| V-03 | Laya resident worker and composer controls connected; [actual local API lifecycle and quality observations](LAYA-LOCAL-OBSERVATION.md). | Activation, bounded queue/deadline and unload work; final real HTTP smoke lifecycle passed, but both positive catalog prompts abstained. Prior standalone warm median 205 ms / ~1.60 GiB; HTTP path 0.631–0.947 s. No calibrated gain. Warm-up counters are not aggregated; host shortlist defaults to Codex. | Evaluate the frozen human-labeled PT/EN corpus, broader latency/memory and usefulness without tuning against smoke examples. Connect host selection and whole-classifier accounting. Preserve abstentions and lexical fallback; no release/quality pass from lifecycle alone. |
| V-04 | Managed native tools/descendants isolation and receipt reconciliation. [Native adapter checkpoint](../archive/NATIVE-ADAPTER-CHECKPOINT.md) records attempt-labeled reservations, one-use prepared Claude worker and model-free CID/nonce broker binding at source `6c33ff2`. | Offline adversarial gates and model-free Docker probe pass. No production JS↔Python host callback/ledger facade, pre-prompt exclusive tool inventory, cross-process worker serialization, read-only reviewer mount, descendant cancellation or complete Codex usage is proven. Latest restrictive Windows command canary refused `:root` deny before a child ran. This blocks an isolated-managed-mode claim and admission, not ordinary approved native collaboration or other OS work. | Establish the trusted callback and a verified supported boundary; run exact negative canaries, cancel/recovery and an actual bounded task with trusted per-role worker identity and usage provenance before enabling/advertising managed mode. Never weaken the profile and label it equivalent. |
| V-05 | Fresh complete release-host suite and clean Windows installation/update/recovery. | Latest native-adapter source `6c33ff2`: **298 Python / 150 Lab / 7 mascot**, zero skips, other Node/portal/eval checks passed on the normal host; [validation log](../t13/VALIDATION.md) and separate graph check recorded. A restricted-sandbox Lab attempt failed real PTY cleanup, and the demo Ctrl+C could not confirm shutdown. Clean-user installer is still implementation work. Two owner-preserved Claude settings differences remain accepted configuration drift (27/29 rows), so the aggregate runner exits 1. | Run applicable zero-skip suites for future integrated changes, then release install/update/recover/uninstall scenarios in a clean Windows environment. Report intentional settings divergence separately from test passes and resolve or document PTY shutdown uncertainty for the installed path. |
| V-06 | Human labels, product/design acceptance and calibrated E2/genetic pilot. | Small catalog diagnostic has agent-proposed labels and 3/7 positive holdout top-one hits; no calibrated generalization claim. Product validation and evolutionary pilot remain open. | Present a consolidated runnable MVP and finite review set to the owner; adjudicate labels, retain false positives/negatives, then run the controlled pilot under its measured prerequisites. |
| V-07 | Owner-selected session-to-agent derivation and actual task use. | The Agentes panel/API passes deterministic fixture cases, but no owner-designated real session note was selected in this checkpoint. A pinned profile is non-executable metadata and native managed execution remains closed under V-04. | Select one real, sanitized session decision; derive and review the candidate in the Lab, retain exact source revision/hash and task pin, then validate attributable use only after the host's execution/accounting boundary is accepted. |
| V-08 | Engine live acceptance (SWARM-PLAN W2/W3), 2026-09-26, source `4300982` + fixes `456b3fd`. **Claude (passed except usage):** real Claude Code 2.1.283 session in its own worktree edited README; Stop hook → idle, `/exit` → done (code 0); diff, gated merge with a PowerShell test command (exit 0) and evidence bundle; merge commit `f66f884` in the throwaway repo. Usage unknown: transcript path was 263 characters with `LongPathsEnabled=0`, so Claude never wrote it (fixed: 8-character worktree folders, honest reason). **Codex (engine side passed, edit failed):** codex-cli 0.155.0-alpha.16.4 launched with `--no-daemon`, `-C` and `notify`; notify → idle twice; its rollout was found by cwd (208,027 input incl. 178,048 cached, 1,081 output tokens, read by the coordinator from the rollout). The Lab had resolved `~/.codex/.sandbox-bin/codex.exe`, which lacks `codex-code-mode-host.exe`, so every tool failed closed and nothing was edited (fixed: complete app build preferred). The coordinator's own stray Esc also interrupted the first Codex turn. Both CLIs opened prompts without hooks (folder trust; Codex hook review and model switch); now detected as blocked from the terminal text, checked against the logged real output. | Codex weekly allowance showed under 5% left, so the retry with the complete build is deferred. Claude usage with the short worktree path is unverified live. | One real Codex one-line task with the complete build through the Lab UI: edit, idle, exit → done, observed usage from its rollout, merge and evidence. One real Claude run confirming observed usage. Answer only the CLIs' own prompts; never trust the owner's hooks on their behalf. |

Update each row with artifact/source hash, exact command or scenario, date, result and limitations when executed. Resolved checks move to evidence references; retain their failed attempts. A dependency that can be installed is work to do, not a reason to mark a test skipped. This register is the final-round queue, not permission to call an incomplete product certified.
