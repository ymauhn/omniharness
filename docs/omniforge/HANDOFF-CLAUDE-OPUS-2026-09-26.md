# OmniForge V1 — interruption handoff to Claude Opus 5.5

Updated 2026-09-26. The owner explicitly paused the current Codex goal because the account had only **4% weekly allowance remaining** at the last read-only query, and asked Claude Opus 5.5 in another session to continue. The owner subsequently requested a thorough V1 review, real Playwright/permitted-browser verification, and relevant coding/review skills in that continuation. **The full agreed MVP objective is unchanged; payments alone remain deferred.** This document is a resume checkpoint, not a release certificate or a request to restart planning from scratch.

## First actions in the Claude session

1. Read root `AGENTS.md`, this document, [LATEST-CHECKPOINT](LATEST-CHECKPOINT.md), [V1-RELEASE-CONTRACT](V1-RELEASE-CONTRACT.md), [T13-H1](../t13/TICKET-HANDOFF-CLAUDE.md), [native adapter checkpoint](../t13/NATIVE-ADAPTER-CHECKPOINT.md), [T13 validation](../t13/VALIDATION.md) and [deferred validation](VALIDATION-PENDING.md). Later dated evidence takes precedence over older journal snapshots.
2. Inspect `git status --short --branch`, `git log -5 --oneline --decorate`, both unstaged and staged diffs, and remote PR state **before editing or committing**. Branch `codex/mvp-host-callback-ledger` started cleanly from `origin/master` merge **`3b2494b87f28a7e4fbb45db5895d488885fe04d4`** from [PR #10](https://github.com/ymauhn/omniharness/pull/10). After that, Codex edited only four documentation files plus this new handoff. An initial Git staging attempt hit a transient approval-service 401; inspect whether these files have since been committed. Do not discard or overwrite them.
3. Review and commit the five pending documentation files if still uncommitted and your host permits Git writes: `docs/omniforge/LAB.md`, `docs/omniforge/LATEST-CHECKPOINT.md`, this file, `docs/t13/NATIVE-ADAPTER-CHECKPOINT.md`, `docs/t13/TICKET-HANDOFF-CLAUDE.md`. Stage exact paths, run `git diff --cached --check`, inspect the staged scope, commit and record the SHA. The owner already authorized scoped commits, pushes, PRs and merges. A failed Codex automatic approval review is **not** a source-code failure or a reason to alter Git permissions.
4. Continue the next bounded executor/accounting slice and update the journal after every validated checkpoint. Preserve dirty `_intake/mvp-*` worktrees, old backups and stash `8105be6`; root integrated source is authoritative. Do not copy stale worktrees over it.

## Delivered Git and validation identity

- [PR #6](https://github.com/ymauhn/omniharness/pull/6) merged workspace flows as `4f4fb0362dadbfdd7c78f6001a36e36a685ed4a0`; [PR #7](https://github.com/ymauhn/omniharness/pull/7) merged Arsenal UI as `ed8923646c3e13b1f00b2edbad4a094c0d0b468a`; [PR #8](https://github.com/ymauhn/omniharness/pull/8) merged isolated demo/graph help as `adcd10cd76bb77000090085ab0ff1f680dc7559e`.
- [PR #9](https://github.com/ymauhn/omniharness/pull/9) merged model-free broker source pinning and all-role Swarm gates as `c8e1bb231d25f499e3ca23704cbca1d39384854e`. [PR #10](https://github.com/ymauhn/omniharness/pull/10) merged exact reviewed head `d251d559b882a08c2ae87c305e1293edb0533900` as **`3b2494b87f28a7e4fbb45db5895d488885fe04d4`**. It adds exact reservation labels, prepared one-use Claude Docker workers and broker CID/nonce checks. No live managed-agent admission followed.
- Final **PR #10 source** normal-host `scripts/check.ps1`: **298/298 Python, 150/150 Lab, 7/7 mascot**, zero skips; other Node/portal/eval checks passed. Graph: 546 nodes, 100 edges, 5 proposals, 0 errors. Aggregate runner exit 1 is only two owner-preserved Claude `ask`/`deny` settings differences (installer 27/29). Do not rewrite those user permissions merely to make the install check green. Full log `%TEMP%/omniharness-native-adapter-final-20260925.log`, SHA-256 `4474CF6C954F21C887BAA81FD34F64F29A56D349A6172E77651B2DB3C095BCFE`. Focused Python broker/bridge/probe 34/34, Swarm driver 10/10 and real **model-free** MCP-to-Docker probe 26/26 also passed. There has been **no source edit after this validation** at this handoff.
- Independent PR review found no blocker for that model-free scope. It found one next-adapter contract: Swarm labels such as `implement:a:0` are not valid Docker IDs (`container_worker.ATTEMPT` allows only letters, digits, `_` and `-`, max 64). Derive a separate safe ID from immutable run/label/lease, retain verifiable one-to-one provenance, and test collision, tamper and replay. Do not silently normalize labels.

## Current runtime and the broken-link report

The owner opened `C:/Users/Yeonatan/master_team/omniforge-lab/index.html` directly and found the controls inert. That is expected for `file://`: the Lab requires its local authenticated server; `/vendor` assets and `/api/*` are served there. Codex started a **fresh synthetic demo** with `pwsh -NoProfile -File .\scripts\start_omniforge_demo.ps1`; its process was still live at handoff, and an HTTP probe returned page 200 plus authenticated `/api/state` with 2 projects, 3 tasks and 2 sessions. The demo used `%TEMP%/omniforge-demo-OyhSwU` and port `61796`. Its launch token is intentionally **not saved in Git**; that URL may expire with the process. Restart the script and use the exact printed `http://127.0.0.1:<port>/?token=<...>` URL if needed. Do not navigate to the HTML file. The demo launches no model. A previous Ctrl+C attempt had unconfirmed PTY shutdown, so do not claim descendant cancellation from this UI demo.

Codex's browser navigation was denied by platform review despite owner consent; do not disguise another backend as the same browser check. The owner subsequently opened the correct loopback demo in the in-app browser and said it was “ficando bem legal,” a positive appearance impression rather than a completed interaction checklist. Claude may use its independently permitted browser route and record actual viewport/keyboard/motion observations in [guided acceptance](GUIDED-ACCEPTANCE.md). HTTP/DOM tests and this short owner comment are not full visual acceptance. No screenshot or complete owner verdict was recorded at this checkpoint.

## Owner-requested V1 quality and browser round

- First audit the **entire current V1** against [V1-RELEASE-CONTRACT](V1-RELEASE-CONTRACT.md), the design intake, code, tests and [VALIDATION-PENDING](VALIDATION-PENDING.md). Classify every acceptance item as implemented and observed, implemented but unverified, absent, failing or blocked. Review architecture, security boundaries, error handling, data isolation, cancellation, accessibility, maintainability and install/recovery paths. Fix concrete defects in small checkpoints; keep the full scope and honest release blockers visible. “Failure-resistant” means tested handling of realistic failures, not an absolute guarantee.
- On Claude's **permitted** browser route, launch a fresh isolated demo through the script and complete [GUIDED-ACCEPTANCE](GUIDED-ACCEPTANCE.md) with actual interaction, keyboard/focus, responsive layout, both mascot skins/three themes, reduced motion, prompt pause and selected-text apply/undo, memory isolation/conflicts, saved Workflows, graph hover/focus help, Agent Arsenal, one-to-eight terminals, second window and reconnect/replay. Add Playwright end-to-end regression tests for stable critical paths and explicit negative cases where useful; install Playwright/browser dependencies before running them. Keep viewport captures and sanitized evidence tied to source SHA. Test the server URL, never `file://index.html`. If the Claude host denies browser control, record the exact denial; do not route around it with another backend or call a DOM test visual acceptance.
- Before implementation, run `skills-graph route` for the specific task and read only applicable installed skills. The local graph currently reports `using-superpowers`, `tdd`, `code-review`, `review-security`, `ponytail-review`, `checkpoint-build` and `to-spec` as installed. Use TDD for fixes/features, independent code and security review for consequential changes, and complexity/ponytail review before merge. Use `to-spec` only for a genuine acceptance gap: the V1 contract already exists, and that skill describes issue-tracker publication, which must respect the owner's external-action authority. Do not install an unrelated remote “superpowers” candidate merely because it appears in the catalog.
- Preserve the existing zero-skip test discipline: targeted failure first, regression after fix, complete `scripts/check.ps1`, Playwright/browser evidence and separate graph check where applicable. Record exact commands, counts, source SHA, failures, screenshots/recordings and limitations. A failed or unavailable scenario stays failed/blocked until rerun; a prototype appearance verdict does not certify the executor, provider billing, clean installer or five pillar slices. Maintain an independent review pass and a reversible checkpoint before accepting the V1 as ready for owner use.

## Immediate technical sequence

1. Implement the trusted JS-to-Python host callback/ledger facade **offline**. Reuse `swarm/swarm.workflow.js`, `harness/swarm_accounting.py`, `harness/claude_worker_bridge.py`, `harness/container_worker.py` and existing tests. Map each exact Swarm attempt/lease to a separate Docker-safe worker ID; preflight a worker before launch; keep its frozen handle on the coordinator side; derive `worker_identity` from verified CID/nonce and exact worktree; run only that handle; reconcile captured Claude stream through `Ledger.start/settle`; map `attempt_id`, `reservation`, `worker_identity`, `usage_scope`, `usage_complete`, `source_sha256`, `total_tokens` and client-estimated USD into the JS callback contract. Never use model-reported identity, estimated cost as billed spend, or unknown usage as zero. Cover role/candidate/retry, integration and review, replay, A/B swap, crash, cancellation and over-budget behavior in deterministic tests.
2. Keep **managed admission closed** until a supported pre-prompt exclusive Claude tool route is proven; broker/coordinator worker commands are serialized across processes (their current locks are process-local); reviewer mount is genuinely read-only; exact worker and descendant cancellation is confirmed; Codex descendant usage and whole-tree receipts are complete; and bounded real negative canaries pass. The current print-mode Claude inventory check happens after prompt submission. A prior synthetic paid attempt timed out, and a restrictive Windows canary never reached a child. Do not replay/widen those blindly. See [NATIVE-ISOLATION](../t13/NATIVE-ISOLATION.md) and V-04.
3. Keep moving the other MVP slices that do not depend on managed admission: secure optional API-key/credit/usage UI, live JEV when the owner connects credentials/credits, calibrated Laya routing, reviewed generated mini-tools and requested repair, asset-link checker, clean Windows install/update/recovery, own-app versus Goose comparison, and real dogfood across engineering, orchestration, Thesis Factory, education/community and marketing. Preserve payments as deferred and follow the complete [release contract](V1-RELEASE-CONTRACT.md); do not label templates or mock data as completed pillar demos.
4. Complete pending visual/product checks, owner-selected session-to-agent derivation and human-labeled graph-weighted E2 eval/evolution only with real evidence. [VALIDATION-PENDING](VALIDATION-PENDING.md) is the final-round queue, not a waiver. Install missing test dependencies rather than skip tests. Run focused tests, full `scripts/check.ps1` with zero skips and separate graph check after source changes; document intentional Claude permission drift apart from test passes. Independently review and checkpoint useful increments with exact SHAs and observed usage.

## Authority and current interruption

The owner authorized scoped local setup, public research, native Codex/Astra delegation using existing allowance, and repository commits/pushes/PRs/merges. Additional-charge services, account credentials, private uploads, broad host security changes, destructive operations and product publication still require their own decision. The owner explicitly selected preserving current Claude permissions. No free usage reset was redeemed. As of the last read-only query, Codex weekly remaining was **4% account-wide**, not measured task usage.

Two bounded Codex subagents for the next callback seam failed with service `401 Unauthorized` before any usable output. One `git add` escalation for the post-merge handoff was also rejected because automatic approval review itself returned 401; the system said the action was **not executed** and made **no safety determination**. A later ordinary escalation succeeded. Do not bypass a permission check; inspect fresh Git status and use Claude's normal permissions if a later commit is still needed. There are no source files from those failed agents.

## Paste-ready continuation prompt

> Continue the full Windows-first OmniForge V1 in `C:\Users\Yeonatan\master_team` from `docs/omniforge/HANDOFF-CLAUDE-OPUS-2026-09-26.md`. Read the linked AGENTS, release contract, latest checkpoint, T13 journal/validation, guided browser acceptance and pending checks; inspect Git before edits. Review **all** current V1 requirements against code and evidence, flag defects and missing slices, and strengthen them with small TDD checkpoints. Route the task through installed `using-superpowers`, `tdd`, `code-review`, `review-security`, `ponytail-review` and `checkpoint-build` where applicable; use `to-spec` only for a real acceptance gap and respect its publication boundary. Launch a fresh isolated demo through the script, then perform real permitted-browser and Playwright interaction tests, including positive/negative, visual, keyboard, responsive, reduced-motion, multi-window and failure/recovery cases in `GUIDED-ACCEPTANCE.md`. Record screenshots/results by source SHA; never substitute DOM tests for visual observation or bypass a host browser denial. Implement the bounded offline host callback/ledger facade with Docker-safe attempt mapping and adversarial tests, then continue every remaining release-contract slice. Keep managed admission closed until live isolation/accounting gates pass. Run the full zero-skip suite, independently review, document exact changes/limits and commit reviewable checkpoints for Codex to resume. The owner already authorized scoped commits, pushes, PRs and merges; new paid services, credentials/private uploads and deployment need their separate decision. Do not claim billed USD, live containment or a complete V1 without evidence.
