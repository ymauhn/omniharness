# Native bridge checkpoint after OmniForge demo merge

Updated September 25, 2026. **Full MVP remains active; managed-agent admission remains closed.** Start from [LATEST-CHECKPOINT](../omniforge/LATEST-CHECKPOINT.md) and this note, then inspect actual Git status. [PR #8](https://github.com/ymauhn/omniharness/pull/8) merged as `adcd10cd76bb77000090085ab0ff1f680dc7559e`. Root switched cleanly to `codex/mvp-executor-boundary` from that `origin/master` and committed only the merge handoff as `faa6fc1`. The last read-only Codex account query showed **9% weekly allowance remaining**, shared account-wide; no reset was used. The owner requested continued work with a checkpoint fit for Claude or a later Codex session.

## Starting evidence and limits

- Latest normal-host repository runner before this slice: **283 Python, 150 Lab, 7 mascot tests passed, zero skips**, other Node/portal/eval passed; aggregate exit 1 only for the owner's two preserved Claude `ask`/`deny` list differences. Final-source Lab rerun passed 150/150. `docs/t13/VALIDATION.md` records the restricted PTY failure, normal-host pass, real demo Ctrl+C uncertainty and log paths.
- `harness/container_worker.py` has a disposable Docker worker profile with model-free 14/14 process and 18/18 worker-adapter acceptance; `harness/claude_broker_probe.py` has a 26/26 real MCP→Docker model-free probe. `harness/claude_worker_bridge.py` binds a fake Claude stream, broker events, worker command ledger and usage in offline tests. None is an authenticated all-role isolation/usage result.
- The installed Codex restricted Windows profile probe was **inconclusive** because the CLI requested the elevated backend; no child isolation result. A prior synthetic Claude subscription attempt timed out before a final receipt; its usage is unknown. Do not repeat either blindly, elevate Windows sandbox setup, or mark attempts zero-cost.
- Ordinary native collaboration under the owner's existing allowance is authorised, but additional-charge services, credentials/private uploads and broad host/security changes need their own authority. A live bounded canary requires a pinned synthetic fixture, actual effective tool inventory, denied host/sibling/source sentinels, exact process/container cancellation and complete receipt checks. No live call is part of the current model-free source-pin subtask.

## Active bounded work and file ownership

- `/root/claude_bridge_pins` owns only `harness/claude_worker_bridge.py`, `harness/claude_broker.py` and their focused tests. Goal: pin and verify specific executable broker/worker source bytes before serving MCP tools; a JSON binding hash alone is insufficient. Keep the claim narrow: hashes of named files are not full runtime/supply-chain attestation. Root will review, run focused/full zero-skip checks and document exact result before a source commit.
- `/root/codex_boundary_audit` is a read-only audit of current Codex bridge/accounting/Windows probe seams. It will return the smallest executable next acceptance step and file scope. It must not mutate the root. Root does not overlap either agent's files.
- **Resume if interrupted:** inspect `git status --short --branch`, `faa6fc1`, the two agent statuses and their file groups. Finish/review model-free source pins; run targeted tests and `scripts/check.ps1` on the normal host without skips. Report accepted Claude permission drift separately and run the graph check if the runner stops there. Preserve all dirty `_intake/mvp-*` authoring worktrees/backups/stash `8105be6`. Update this note, [T13 journal](TICKET-HANDOFF-CLAUDE.md) and [latest pointer](../omniforge/LATEST-CHECKPOINT.md) with source SHA, tests, failed attempts, real usage/unknowns and the next task. Do not promote a file hash or offline test to a live model/containment verdict.

The full [V1 release contract](../omniforge/V1-RELEASE-CONTRACT.md) still includes Windows installation, generated read-only asset-link tool, the five real pillar demonstrations, secure optional JEV/key controls, human visual acceptance and calibrated graph-weighted eval/genetic pilot. Payments remain deferred.

## In-progress source checkpoint after `e3c6edd`

The two disjoint source edits are present in the working tree and under independent review; **do not infer a final source commit from this paragraph**. The Claude bridge currently adds exact SHA-256 pins for `claude_broker.py`, `container_worker.py` and `swarm_worktrees.py` to the broker binding, checks them in an inline stdlib bootstrap before importing the broker, then rechecks before worker construction. Its focused offline tests passed **22/22**. The model-free MCP→Docker probe passed **26/26** on the normal host with `model_invoked=false`, `task_pass=null` and its container removed. An earlier restricted-sandbox probe failed on Docker pipe access before broker startup and preserved `%TEMP%/omni-worker-acceptance-claude-broker-x2ib5zde` with `cid:null`, phase `unknown`; it is a failed attempt, not acceptance. These pins cover only named project source bytes and do not attest Python/stdlib, Docker/image, bytecode cache or later changes.

The Swarm coordinator edit makes each implementer, integrator and reviewer invocation require an attempt-bound preflight and post-run verification. Settlement now requires `usage_complete=true`, `usage_scope=whole-tree`, matching attempt/reservation, finite units and a unique source digest. Its offline Node scenarios passed, including missing integration/review proof and incomplete or foreign receipts. This is a fail-closed **callback contract**, not proof that a real Codex host enforces exclusive tools, gives a container identity or covers descendants. The final source review, complete repository runner and validated source commit are still pending at this checkpoint. A subsequent session must inspect Git status and rerun final-source checks before claiming them.
