# Native bridge checkpoint after OmniForge demo merge

Updated September 25, 2026. **Full MVP remains active; managed-agent admission remains closed.** Start from [LATEST-CHECKPOINT](../omniforge/LATEST-CHECKPOINT.md) and this note, then inspect actual Git status. [PR #8](https://github.com/ymauhn/omniharness/pull/8) merged as `adcd10cd76bb77000090085ab0ff1f680dc7559e`. Root branch `codex/mvp-executor-boundary` contains merge handoff `faa6fc1`, interruption checkpoints `e3c6edd`, `18ceba8`, `385ef26`, and validated source **`322337b0c8c83bf98111c031f4130eaab622f8e0`**. The last read-only Codex account query showed **7% weekly allowance remaining**, shared account-wide; no reset was used. The owner requested continued work with a checkpoint fit for Claude or a later Codex session.

## Starting evidence and limits

- Latest normal-host repository runner before this slice: **283 Python, 150 Lab, 7 mascot tests passed, zero skips**, other Node/portal/eval passed; aggregate exit 1 only for the owner's two preserved Claude `ask`/`deny` list differences. Final-source Lab rerun passed 150/150. `docs/t13/VALIDATION.md` records the restricted PTY failure, normal-host pass, real demo Ctrl+C uncertainty and log paths.
- `harness/container_worker.py` has a disposable Docker worker profile with model-free 14/14 process and 18/18 worker-adapter acceptance; `harness/claude_broker_probe.py` has a 26/26 real MCP→Docker model-free probe. `harness/claude_worker_bridge.py` binds a fake Claude stream, broker events, worker command ledger and usage in offline tests. None is an authenticated all-role isolation/usage result.
- The installed Codex restricted Windows profile probe was **inconclusive** because the CLI requested the elevated backend; no child isolation result. A prior synthetic Claude subscription attempt timed out before a final receipt; its usage is unknown. Do not repeat either blindly, elevate Windows sandbox setup, or mark attempts zero-cost.
- Ordinary native collaboration under the owner's existing allowance is authorised, but additional-charge services, credentials/private uploads and broad host/security changes need their own authority. A live bounded canary requires a pinned synthetic fixture, actual effective tool inventory, denied host/sibling/source sentinels, exact process/container cancellation and complete receipt checks. No live call is part of the current model-free source-pin subtask.

## Validated model-free source slice

- `322337b` changed eight source/test files. The Claude broker binding pins SHA-256 of three named executable project modules and the **absence** of `harness/__init__.py`. An inline bootstrap rejects changed source or a newly created initializer before importing the broker, bypasses pre-existing source `.pyc` cache using a fresh nonexistent `sys.pycache_prefix`, and the broker checks pins again before worker construction. The focused suite passed **24/24**; a real model-free MCP→Docker reprobe passed **26/26**, `model_invoked=false`, `task_pass=null`, with the exact disposable container removed. Pins are a consistency check at launch/startup, not attestation of Python, stdlib, Docker, image, host config, or later source changes.
- The Swarm driver now requires per-attempt preflight and post-run verification for implementer candidates, integrators and reviewer, with matching attempt, reservation and worker identity. Whole-tree settlement requires explicit completeness, matching identity and a non-replayed source-stream digest. Duplicate/unsafe reservation IDs are rejected **before** any affected agent launches, including cross-wave reuse. The offline Node driver scenarios passed **10/10**. This is a fail-closed coordinator callback contract; it does not make the missing native Codex tool confinement or descendant usage complete. Its real host adapter must derive worker identity and receipt hash from trusted evidence, rather than echoing a model report.
- Independent reviews exposed and then rechecked three false-admission paths: timestamp-valid malicious `.pyc` and a new package initializer before broker import; A/B worker identity mismatch; and duplicate reservations used by parallel candidates. Adversarial regressions cover each. The final review found no remaining blocker in this **offline slice**, while explicitly leaving live isolation/accounting acceptance open.
- Final normal-host `scripts/check.ps1`: **288/288 Python** (89.051 s), **150/150 Lab**, **7/7 mascot**, other Node/portal/eval checks passed, **zero skips**. Log `%TEMP%/omniharness-native-boundary-final-20260925.log`, SHA-256 `c5fc3418fc55d7f2a4e56048d56826002cf6076bca042ea39822011235264e88`. Installation check **27/29**: only the two owner-preserved Claude `ask`/`deny` lists differ, so aggregate exit 1; the installed Swarm driver now matches source. It was copied only after backing up `C:/Users/Yeonatan/.claude/workflows/swarm-driver.js.pre-omniharness-20260925-190827`. Graph check, run separately after installation stopped the runner: **546 nodes, 100 edges, 5 proposals, 0 errors**. `git diff --cached --check` passed before the source commit.

**Resume if interrupted:** inspect actual `git status --short --branch`, `322337b`, this file and [T13 journal](TICKET-HANDOFF-CLAUDE.md). Preserve all dirty `_intake/mvp-*` authoring worktrees, backups and stash `8105be6`. Do not repeat the earlier timed-out synthetic Claude turn blindly or treat the Codex Windows probe as contained. The next executable gate is a trustworthy per-role adapter and bounded live negative canary that proves effective tool inventory, actual worker identity, host/sibling/source denials, exact descendant cancellation and complete whole-tree receipts; Codex stays incomplete/unknown if descendants are not attributable. Then return to [V1 release contract](../omniforge/V1-RELEASE-CONTRACT.md) and [pending validations](../omniforge/VALIDATION-PENDING.md). No file hash, offline test or this checkpoint certifies a managed live agent.

The full [V1 release contract](../omniforge/V1-RELEASE-CONTRACT.md) still includes Windows installation, generated read-only asset-link tool, the five real pillar demonstrations, secure optional JEV/key controls, human visual acceptance and calibrated graph-weighted eval/genetic pilot. Payments remain deferred.

## Preserved failed attempts during this slice

The first restricted-sandbox MCP→Docker probe failed on Docker pipe access before broker startup and preserved `%TEMP%/omni-worker-acceptance-claude-broker-x2ib5zde` with `cid:null`, phase `unknown`; it is a failed attempt, not acceptance. The model-free reprobe on the normal host passed after source changes. A complete repository runner before the final review fixes executed its tests but reported a third installation drift for the installed Swarm driver; the targeted backup/copy resolved that row. Neither run changed the owner's Claude permission lists. No authenticated model call or verified paid usage was part of this slice.
