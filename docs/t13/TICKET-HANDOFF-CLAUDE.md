# T13-H1 · Claude checkpoint and return handoff to Codex

**Resume first: [current MVP checkpoint](../omniforge/LATEST-CHECKPOINT.md).** The owner requested interruption-ready documentation near the weekly allowance limit and explicitly said to continue normally. Branch `codex/mvp-workspace-flows`, base `17230c093f933c8cd943216a80bc02eea4530079` after PR #5, now integrates saved Workflows, eight-pane/multi-window terminals with bounded replay, and memory edit/history/archive controls. Final **282 Python / 130 Lab / 7 mascot**, zero skips, and two independent final reviews per slice/integration. Check actual Git status for the resulting commit/PR; root is the final source, worktrees are stale. Full MVP and visual/provider/executor acceptance remain open.

Newest continuation journal — September 25, workspace checkpoint: changed Lab core/server/index/pane scope; added workflow service/panel, memory panel, terminal grid/replay and regression files. Retained failures and final hashes in [VALIDATION](VALIDATION.md), with detailed cycles/reviewer identities in [WORKSPACE-FLOWS-CHECKPOINT](../omniforge/WORKSPACE-FLOWS-CHECKPOINT.md). Workflow and grid each needed three actual repair cycles; no counters were reset and expansion paused for narrow closure. Full runner aggregate exit 1 remains only owner-preserved Claude ask/deny drift, 27/29 rows; graph 546/100/0 errors. No browser workaround, model dispatch, billing claim, release, private export or deployment occurred. Native delegate usage unavailable. Next bounded implementation: authenticated arsenal API/UI and deliberate reviewed-profile pins on selected project tasks; retain every unclosed full-MVP requirement and update this journal plus the resume pointer before handing back.

Status: **LOCAL LAYA AND ARSENAL CHECKPOINT VALIDATED.** Copilot PR [#4](https://github.com/ymauhn/omniharness/pull/4) merged as `af369b248d7cda94a0bd38c36af9373df4809839`; continuation branch `codex/mvp-local-assistance`. Inspect Git and the newest journal before editing. Final integrated checks: 282 Python, 69 Lab and 7 mascot, zero skips; aggregate exit 1 remains the two accepted Claude settings differences (27/29 installation rows). Resident Laya activation/unload and the five-profile registry/Agent Forge are implemented; arsenal UI/task dispatch remains. Native all-role containment, complete usage and V1 remain open.

Newest owner amendment, 2026-09-25: scoped commits, branch pushes, PRs and merges are authorised; preserve unrelated changes and remote history. Laya local, the opt-in JEV adapter and a reusable agent arsenal are required MVP work. Nonblocking unavailable checks may wait for the final functional-MVP validation round in [VALIDATION-PENDING](../omniforge/VALIDATION-PENDING.md). This explicitly permits continued implementation/provisional integration despite unavailable browser/provider evidence. Do not enable a known unsafe execution path or present deferred evidence as passing. Earlier journal statements forbidding pushes or stopping all OS development describe previous authority, not the current instruction.

Latest owner continuation, 2026-09-25: the owner explicitly approved native Computer Use and removal of the former AGENTS browser-route restriction, authorized implementation of the entire agreed MVP, and selected mascot design/animation as the first real dogfood task. The native-browser policy change is now authorised; normal tool approval review still applies. The complete MVP objective is active in Codex goal state, including all five pillars and the agreed Copilot/UI extensions; payments remain deferred. Artifact quality and managed-executor containment/accounting require separate evidence. The next technical slice remains a bounded real managed attempt with effective tool-inventory, confinement/cancellation and receipt evidence. Earlier denied visits below are historical and remain accurately recorded.

Latest continuation, 2026-09-25: starting from clean `master` / `eedc43588b9187c36c4a0a9a0641ae9c7cbaa9c0`, the owner asked to parallelize toward a complete OmniForge V1 and chose OmniHarness as the first real project. Offline Claude MCP and Codex App Server dynamic-tool bridges, a source-run Windows [Lab candidate](../omniforge/LAB.md), a [finite release contract](../omniforge/V1-RELEASE-CONTRACT.md) and an offline E2 scorer are now reviewable. Final repository tests passed **215/215 Python and 26/26 Lab methods, zero skips**; aggregate exit 1 remains solely the two owner-preserved Claude permission differences. Independent review prompted Lab scan/context/pane-scope bounds and E2 criterion-hash corrections. A Windows PTY orphan-grandchild counterexample remains a documented release blocker. The owner added a [Copilot/UI design intake](../omniforge/PROMPT-COPILOT-AND-OS-EXTENSIONS-2026-09-25.md) and [parallel mascot handoff](../omniforge/MASCOT-DESIGN-HANDOFF-2026-09-25.md), neither implemented by this checkpoint. See the newest completed journal entry below. This paragraph is not a commit or release certificate.

Earlier planning continuation, 2026-09-25: owner-first OmniForge direction and expanded research are recorded in [PLAN](../omniforge/PLAN.md) and [research register](../omniforge/RESEARCH-2026-09-25.md). At that point the owner required finishing pending E1/T13 work before desktop prototyping; the later parallel-build request amended the timing for candidate A without waiving release evidence. No desktop, memory service or new integration was implemented by that research alone. Read the newest journal entry before continuing.

Latest implementation: **NATIVE CLAUDE/CODEX OFFLINE ADAPTERS; MODEL-FREE WORKER ADAPTER VALIDATED — live agent-tree containment/accounting remains open**. The process, usage, cancellation and recovery seams were committed as `89d24baa76495e86f4b11f2fec6f976910a082e0`; the full battery then passed 140/140 Python tests without skips. Both native login-status preflights succeeded. Docker recovery was committed as `b8cebf37772ef92318d8f06aed79c6c82dea195e`; the strengthened process canary and model-free coordinator worker adapter now pass **14/14** and **18/18** with current source hashes in renewed durable reports. The final full runner, including the fixture-cleanup guard, passed **176/176 Python tests, zero skips**, plus Node/portal/eval; aggregate exit 1 is solely the two explicitly owner-preserved Claude installation-policy differences, and the separate graph passed. No authenticated model/tool turn used the container. Read the newest journal and validation before continuing.

Originally prepared by Codex on 2026-09-21. Repository: `C:/Users/Yeonatan/master_team`. Original handoff branch/HEAD: `master` / `1a2289cd69ba48685a5f131563e20cf4a9591a90` (T1); its then-uncommitted delivery was subsequently committed as `8e6c20ee9952e5f1314f9ba19b28296280b41f05`. The September 22 research began with a clean tree. Re-read actual Git state on arrival; historical snapshots are not permission to overwrite later work.

Owner request: leave a ticket that Claude reads before changing anything or preparing the requested commit, and document every subsequent change so Codex can resume from repository evidence. This ticket does not itself perform a commit or grant push/publication authority. Once the owner asks Claude to commit, that request is sufficient for that commit; do not ask for the same approval again.

Owner authority: see the newest amendment above and current AGENTS.md. Native Codex/Astra agents using the existing allowance require no repeated paid-call approval. Unknown usage and unverified isolation still cannot become benchmark evidence. Repository Git publication is authorised within scope; product deployment and unrelated publication are separate.

## Read before changing files

1. `AGENTS.md`, especially task-scoped autonomy. Routine dependencies, public documentation/assets, local setup and validation are already covered by task approval. Do not restore the old blanket per-command questions.
2. This ticket, including the latest continuation entry at the bottom.
3. `CONTEXT.md`, `docs/adr/0006-evaluation-first-evolution.md`, `docs/evals/RECORDS.md` and `docs/experiments/eval-integrity-2026-09.md`.
4. `docs/t13/README.md`, `docs/t13/PLAN.md`, `docs/t13/SEEN.md`, `docs/t13/VALIDATION.md` and ADR 0005.
5. The actual changed code, tests and untracked files. Older sections in `docs/handoff-codex.md` and `docs/commercial/TICKETS.md` describe historical plans, not current completion or authority. Current AGENTS.md and dated evidence take precedence over obsolete wording.

## Current stage

| Work | State at handoff | Remaining boundary |
|---|---|---|
| T1 parity/stabilization | Committed as `1a2289c`; complete visual coverage | Live host parity was not certified |
| E1 eval integrity | Implemented and locally validated in this checkpoint | Live control still needs integration with the tested OS containment profile |
| T13 S1 envelope/menu | Manual/coordinator CLI implemented; Claude's Bash guard remains block-only and never asks | No native envelope interception or real workspace envelope is claimed |
| T13 S2 routing | Measured-cost/prerequisite/alternative routing implemented and tested | Recommendations do not execute themselves or establish task-quality weights |
| T13 S3 swarm | Offline driver, ledger, whole-tree Claude parser, worktree audits, native process adapters and offline Claude/Codex tool-bridge candidates implemented; disposable process and worker-adapter canaries passed 14/14 and 18/18 | Live exclusive tool routing, all-role executor/usage/isolation wiring and E1 controls remain; ordinary native allowance work is authorised |
| T13 S4 history/hooks | Implemented, installed and locally tested | SessionStart/PostToolUse native delivery still needs live Claude evidence; shell writes are outside the edit-hook coverage |
| T13 S5–S7 | Not complete | B8/B9 depend on valid accounting/isolation; portal/showcase and final closure remain downstream |
| E2 / genetic search / commercial work | Offline task-graph scorer/fixtures added in the current pending checkpoint | Human labels, cross-pillar cases, external checker digest, calibration and genetic pilot remain; no fitness or commercial claim |

Historical September 21 complete battery: `scripts/check.ps1` exited 0; **92/92 Python tests, 0 skipped, 67.654 s**, including both visual methods. Node: 5 Gauntlet + 6 Scout + 7 Swarm scenarios and portal checks passed. Eval selftest and 25 installation rows passed. Graph: 511 nodes, 92 edges, 5 pending proposals, 0 errors. Separate real Docker probe: **12/12, 1.297 s**. Evidence: `docs/t13/VALIDATION.md`; local log `%TEMP%/omniharness-task-authority.log` (not guaranteed to survive on another host). Current September 25 evidence is at the top of VALIDATION and the newest journal entry.

The historical eval audit has four invalid and four unverified attempts, no certified baseline. Regression output remains INCOMPARABLE; a completed command is not proof of no regression. Cold/warm fixture counts are Scout 4→3 and Gauntlet 3→1 simulated calls, not real token, dollar or quality gains.

## What Claude can change

**For a commit-only request:** review the existing diff, validate it, fix concrete defects necessary to preserve its intended behavior, update the handoff/evidence and commit the reviewed delivery. Avoid unrelated cleanup or starting another feature. Document any correction with its failing case and resulting check. Do not discard the current work to recreate it from HEAD.

**If the owner also asks to continue implementation:** the next recommended slice is the S3 host adapter and its deterministic boundary tests. Work primarily in `swarm/`, `harness/`, the portable swarm entry, relevant tests and `docs/t13/`. Establish what the actual host can enforce before inventing an API. `budget.reserve` / `budget.settle` are harness contracts, not verified native Workflow APIs. Keep unsupported hosts refusing execution. E1 control isolation and native-hook verification are adjacent evidence tasks; give each a bounded plan and log entry. Do not silently fold them into a checkpoint commit.

Current starting point for that adapter is [ACCOUNTING.md](ACCOUNTING.md): reuse `harness/swarm_accounting.py` and `harness/swarm_worktrees.py`, their 12 tests and the driver's `cancel`/isolation contracts. Do not duplicate them or restore the stashed all-tool hook. Recover Docker's failed runtime first for OS acceptance; a destructive reset or machine restart needs its own owner decision. Actual Git isolation is tested, but worktrees cannot substitute for OS containment.

Historical September 21 update: Docker recovery and the offline container process probe then passed 12 checks. On September 25 Docker Desktop initially failed again; after a second verified socket-only backup, the Linux daemon recovered. Its strengthened [14-check process probe](../experiments/sandbox-containment-2026-09-25.json) and the new [18-check worker-adapter canary](../experiments/container-worker-2026-09-25.json) now pass. The next implementation is the coordinator/native agent tool-execution bridge with attributable usage, not a blanket model-charge approval round. Neither passing canary certifies an agent connected to that profile.

Known file groups in the pending package (inspect exact paths, not a blanket staging command):

| Area | Files/directories to inspect |
|---|---|
| E1 | `evals/run.py`, `evals/records.py`, `evals/__init__.py`, `tests/test_runner.py`, `tests/test_eval_integrity.py`, `tests/fixtures/evals/`, `docs/evals/`, `docs/experiments/eval-integrity-2026-09.md`, `docs/experiments/eval-history-2026-09.json`, `docs/benchmarks.md` |
| Envelope and installation | `harness/envelope.py`, `harness/guard_bash.py`, `harness/settings.json`, `scripts/install.py`, `scripts/check.ps1`, `tests/test_envelope.py`, `tests/test_layout.py`, `.agents/skills/omniharness/`, `.gitignore`, `docs/install.md` |
| Routing and swarm | `.agents/skills/skills-graph/`, `.agents/skills/scout/`, `.agents/skills/swarm/`, `swarm/`, `tests/test_skills_graph.py`, `tests/test_swarm_driver.js`, `docs/skills-graph/` |
| History and auxiliary hooks | `harness/seen.py`, `harness/hooks/`, `gauntlet/SKILL.md`, `gauntlet/gauntlet.workflow.js`, `scout/scout.workflow.js`, `tests/test_seen.py`, `tests/test_hooks.py`, `tests/test_driver.js`, `tests/test_scout_driver.js` |
| S3 continuation | `harness/swarm_accounting.py`, `harness/swarm_worktrees.py`, `tests/test_swarm_accounting.py`, `tests/test_swarm_worktrees.py`, `tests/test_guard.py`, `docs/t13/ACCOUNTING.md`, `docs/experiments/claude-reconciliation-2026-09-21.md`, `docs/experiments/swarm-usage-2026-09.json` |
| Governance and handoff | `AGENTS.md`, `CONTEXT.md`, `docs/adr/0005-session-envelope.md`, `docs/t13/`, `docs/handoff-codex.md`, `docs/commercial/TICKETS.md` |

Preserve these boundaries: unknown usage stays unknown; invalid executions never pass; incomplete judgments never enter reusable history; old confirmed findings remain visible; USD is not converted to tokens with a fixed ratio; worktrees are not an OS sandbox. Historical raw eval records remain untouched. Real model runs need the applicable approval, cost/budget and checkpoint evidence; old strategic approval is not proof that present technical prerequisites hold. Native permissions remain authoritative.

## Review and commit procedure

1. Read this ticket and inspect `git status --short --untracked-files=all`, `git diff --no-ext-diff --no-textconv`, `git diff --cached --no-ext-diff --no-textconv` and current HEAD. Read untracked source files explicitly; `git diff` alone omits them. If another agent is writing, coordinate through the owner before overlapping edits.
2. Mark this ticket `IN PROGRESS` and append a dated continuation entry with the current HEAD, requested scope and starting worktree state. For a commit-only request, keep the existing feature scope.
3. Validate with `./scripts/check.ps1`. The runner chooses bundled Python 3.12.14 on this host; `-Python <absolute-path>` selects another suitable runtime. Dependencies already installed here include Playwright/Chromium and PyYAML. Install/configure missing dependencies on another host; **no skipped tests**. Public Google Fonts used by visual tests are within the standing task authorization.
4. If installed drivers or hooks drift after a correction, use the same Python to preview `scripts/install.py --dry-run`, then apply the necessary update with backups and recheck. Do not hand-edit copied drivers instead of their repository sources. Current settings backup is `C:/Users/Yeonatan/.claude/settings.json.pre-omniharness-5`; record any newer backup. User-home installs are not Git content.
5. Record actual checks and any failures in `docs/t13/VALIDATION.md` plus the continuation entry. Preserve previous measurements as dated evidence. Run `git diff --check`; inspect the proposed staged files and diff. Include required new modules, fixtures, skills and this ticket. Exclude ignored runtime state, credentials, user-home settings, raw `evals/results/` and generated captures/logs. Do not stage unrelated files merely because they are present.
6. Update this ticket's status and the current summaries in `CONTEXT.md` / `docs/handoff-codex.md` when their statements change (including the pending-versus-committed state). Identify the exact delivery and its evidence; do not mark all T13 complete. A suitable combined commit subject is `feat(harness): harden eval records and add local T13 autonomy`. Shared files span E1/T13; do not manufacture separate stage boundaries that produce broken intermediate commits.
7. Commit when requested by the owner, preserving T1 history. A commit request does not imply push. Return the actual full SHA, branch, included scope, test result, residual worktree changes and next action to the owner. Verify final `git status --short`; describe leftovers instead of deleting or resetting them.

## How to document every continuation

Before handing back to Codex, append a completed entry below. Use one entry per coherent change or session; record failed attempts and limitations too. Do not rewrite earlier entries to imply a check was performed later than it was. Chat-only explanations are insufficient for decisions or changed behavior.

Update the affected contract page when behavior changes (`RECORDS.md`, `SEEN.md`, T13 README/PLAN), `VALIDATION.md` for executable evidence, and CONTEXT/handoff for current stage. An architectural decision gets an ADR or a dated amendment to its existing ADR. Keep raw transcripts/private material out of tracked documentation; provide sanitized evidence and reproducible commands.

Copy this template as a new entry, replacing every placeholder or explicitly writing `not measured` / `not applicable`:

```markdown
### YYYY-MM-DD HH:MM timezone — Claude/Codex — short change title
- Owner request / authority: exact scope and reference to the instruction.
- Branch / HEAD before work / starting dirty files:
- Ticket state: READY / IN PROGRESS / CHECKPOINT READY / HANDED BACK.
- Changed files and behavior: path → what changed → why; include tests/docs.
- Decisions: rationale, rejected alternative if relevant, ADR/contract link.
- Validation: exact commands, runtime/host, exit codes, passed/failed/skipped counts,
  elapsed time when measured, evidence path; identify offline fixtures versus live evidence.
- Failures and unresolved risks: reproducer, attempted fix, remaining limitation.
- Host changes: dependencies/versions, installed driver/hook updates, backup paths;
  write “none” when only repository content changed.
- Commit scope: included delivery, proposed subject or already-existing commit SHA;
  list residual uncommitted files. Actual new SHA is returned after the commit.
- Resume here: one next action, exact relevant files, acceptance check and dependencies.
```

A commit cannot contain its own SHA in a tracked file. Record the baseline and proposed commit subject in its entry; report the resulting SHA after committing. The next session resolves it from Git and can append that SHA in its next normal documentation change. Do not create a chain of amend/extra commits just to embed a self-reference.

## Acceptance for T13-H1

- [ ] Claude has read the current ticket and inspected staged, unstaged and untracked work.
- [ ] Any corrections have a recorded reason and validation; the existing delivery was preserved.
- [ ] The complete battery passes with zero skips before the checkpoint is described as validated.
- [ ] The owner-requested commit includes the reviewed delivery and updated handoff, with no unrelated or local runtime artifacts.
- [ ] The owner receives the actual SHA, residual Git state and next action; Codex can resume from the repository journal.
- [ ] Live-host/accounting/isolation limitations remain explicit unless new evidence closes them.

## Continuation journal

### 2026-09-21 — Codex — resume after Claude guard changes (in progress)

- Owner request: inspect the latest Claude changes, explain their effect, and continue actual swarm usage accounting/isolation.
- HEAD remains `1a2289c`; pending E1/T13 work is not committed. Fetched remote refs without merging. Claude's `7a9b438` on `fix/guard-never-asks` pins a Bash-only, block-only, fail-open guard; the reverted T13 guard/settings remain preserved in stash `8105be6`. This policy is retained.
- Remote `90534e4` includes Phase C planning; `28d2096` on the screen-control branch adds D4 source verification. Those docs are not integrated locally and introduce no runtime skill. Phase C remains downstream.
- Reconciliation: restored local S4 auxiliary hooks and narrow allow entries while keeping the Bash guard byte-identical to the Claude-restored version. Ported Claude's guard contract test with provenance and moved envelope-specific assertions to the manual policy CLI. Installer probes now check the block-only contract.
- Initial targeted verification reproduced 6 failures and 1 error (21 tests), including missing `allow` configuration and the obsolete all-tool-hook expectation. Current work follows tests before implementation; final results and remaining host limits will be appended here.
- Next: coordinator-side accounting and verified worktree boundaries. Official Claude docs distinguish `modelUsage` whole-tree token totals from main-loop `usage`, and client-estimated USD from authoritative billing. Preserve that distinction in the new contract.

### 2026-09-21 — Codex — handoff prepared

- Owner request: create this ticket so Claude can understand the current stage before its separately requested commit and document any continuation for Codex.
- Branch/HEAD: `master` / `1a2289cd69ba48685a5f131563e20cf4a9591a90`; index empty at inspection; existing E1/T13 changes and new files preserved.
- Change scope: documentation only. Added this ticket and discovery links in AGENTS.md, the master ticket list and the Codex handoff. `CLAUDE.md` remains the one-line AGENTS.md import.
- Prior implementation validation: 75/75 Python plus the complete Node/eval/install/graph battery; see the dated S4 record above. Ticket creation does not claim a new complete battery run.
- Handoff validation: `git diff --check` passed; `python -m unittest tests.test_layout.Skills.test_claude_md -v` passed (1 test, zero skips). All 3 local Markdown links in the changed documents and 8 required reading references resolve. Only existing CRLF normalization notices were emitted by Git.
- Host changes: none. No commit or push performed by Codex for this handoff.
- State: READY FOR CLAUDE. Codex has no further active implementation in this handoff; verify fresh Git state before assuming exclusive ownership.
- Resume here: review/checkpoint the E1 + local T13 package under the owner's commit request. If continuation is requested afterward, investigate the S3 trusted host adapter with boundary tests; keep paid dispatch blocked until its prerequisites are proven.

### 2026-09-21 — Codex — reconciled guard and validated S3 primitives

- Authority: owner's latest request to inspect Claude changes and continue real consumption accounting/swarm isolation; standing routine setup/test authorization. No new paid run, commit or push requested/performed by Codex.
- Branch/HEAD: `master` / `1a2289cd69ba48685a5f131563e20cf4a9591a90`, one ahead/two behind fetched `origin/master`. Index empty; pending E1/T13 preserved. Claude branch `7a9b438` and stash `8105be6` retained; remote Phase C/D4 docs inspected, not merged. No completed Claude journal entry was present, so reconciliation is grounded in Git/files rather than assumed chat history.
- Changes: kept guard byte-identical; restored S4 auxiliary configuration, corrected installer probes and ported Claude's four guard tests. Added SQLite accounting and Git worktree modules/tests, corrected envelope token parsing, and added trusted driver isolation/cancellation checks. Updated AGENTS, ADR 0005, contracts, CONTEXT/handoff and experiment reports. See the file groups and `ACCOUNTING.md` above.
- Validation: complete `./scripts/check.ps1` exit 0; 91 Python tests, 0 skipped, 63.981 s; both visuals; Node 5/6/7 and portal; eval selftest; 25 install rows; graph 511/92/5 pending/0 errors. `git diff --check` passed. Red cases and log location are in `VALIDATION.md`.
- Measurement: reread six complete legacy streams with source hashes; 1,311,288 tokens and US$2.857086 estimated known subtotal. Seventh stream unresolved; billing/complete total unknown. No new model run or quality certification.
- Host changes: installed updated Swarm driver and merged settings with pinned Python 3.12.14; backup `.claude/settings.json.pre-omniharness-5`. No new package. Docker 4.85.0 hidden start failed on the inaccessible inference socket; exact backup rename failed too, no object/data removed or reset. Linux engine/OS acceptance unavailable.
- Remaining: native hook delivery; provider-side caps and coordinator/executor wiring; OS containment, then E1 live controls/B8/B9. Local unit/integration success is not live swarm certification. All E1/T13 source/docs remain uncommitted; no staging or cleanup performed.
- Resume here: recover Docker through a supported nondestructive repair or an explicitly approved host restart; then execute adversarial OS containment checks before wiring paid dispatch. If the next owner request is commit-only, review/checkpoint the validated local slice with these limitations; do not start Phase C or a paid benchmark.

### 2026-09-21 — Codex — standing commits, native allowance and real containment

- Authority: owner explicitly said to continue, change the no-commit rule, commit, and remove the generic paid-agent approval restriction for native GPT Astra agents using the existing allowance. Additional-charge services and publication remain outside that amendment.
- Starting HEAD: `1a2289c`, dirty E1/T13 delivery and empty index. Prepared the whole reviewed local package for a checkpoint; no remote merge/push or stash application. Proposed subject: `feat(harness): harden evals, add swarm accounting and verify containment`.
- Changes: updated AGENTS/Swarm skill/ADR to distinguish native allowance work from external paid calls; removed the generic commit ask from the fragment and added explicit backed-up migration `install.py --task-commits`. Added a regression test preserving other native permissions. Added `harness/sandbox_probe.py`, a fixed model-free acceptance check in disposable real Git worktrees and a restricted Docker process, plus hashed evidence in `docs/experiments/sandbox-containment-2026-09.json`.
- Host: settings backup `.claude/settings.json.pre-omniharness-6`. Recovered Docker by preserving socket-only directories at `%LOCALAPPDATA%/Docker/run.pre-omniharness-20260921`, `run.pre-omniharness-20260921-2`, and `%LOCALAPPDATA%/docker-secrets-engine.pre-omniharness-20260921`. No secret contents were read, machine restarted, or Docker data reset/deleted. Engine 29.6.2. Official Python image pinned at `sha256:2f17fc044b579bab302c2e8054d3a686e2cb9a83de48e70534b94cd8ebbe06a9`; first pull failed EOF, second succeeded. Image and recovery backups retained.
- Validation: full `scripts/check.ps1` exit 0, zero skips, including both visual methods; exact count/time in `VALIDATION.md`. Live containment: 12/12 checks, 1.297 s, exit 0; distinct worktree output, source/sibling unchanged, outside sentinels inaccessible, network/root writes denied, Docker socket absent, non-root/zero capabilities/no-new-privileges/seccomp. Container removed by its exact fixture ID; no existing container touched. No model called by this probe.
- Limits: native agent tool calls are not yet routed through this container profile; a fixed process check cannot close S3's native executor or E1 control adapter. The historical usage audit remains unchanged and billing is not independently reconciled. Unknown usage limits benchmark claims, not authority to perform ordinary native work.
- Resume: inspect current HEAD/cleanliness and use this checkpoint; wire coordinator execution and native usage evidence, preserving the tested containment profile and per-role/retry accounting. Do not recreate the no-commit or per-native-agent paid-call barriers. Document the actual delivery SHA in the next normal journal entry; it cannot be embedded in its own commit.

### 2026-09-22 — Codex — external research, joint-host roadmap and library offer

- Authority: owner requested public internet/GitHub/X research, an integration plan, remaining steps, direction questions and conversion recommendations. Standing local-commit authority applies to the reviewed documentation. Scope is research/planning, not implementation or publication.
- Starting branch/HEAD/state: clean `master` / `8e6c20ee9952e5f1314f9ba19b28296280b41f05`, two ahead/two behind the existing origin reference. This SHA is the previous E1/T13 delivery. No merge, stash application or remote write.
- Ticket state: CHECKPOINT READY for the research documents. Native executor acceptance remains open.
- Files: `docs/research/harness-evolution-2026-09/PLAN.md`, `dossier.md`, `decisions.md`, `evidence.json` record the proposal, source register, owner answers and sanitized observations. CONTEXT, the eval plan status and this ticket link/reconcile the current stage. No runtime, skill, test or landing implementation changed.
- Owner decisions: reliable harness/public demo in 4–6 weeks; developers as primary audience; buying access/joining community as main CTA; skills/templates/tutorial library as the offer; Claude and Codex together for first certification. The new plan is a proposal to review, not a blanket implementation approval.
- Findings: E1 still needs receipt-contract reconciliation and live controls; Docker process evidence is not agent containment. Landing contains stale benchmark claims and reproducible clipboard false-success. Review corrected an initial reliance on the build docstring: actual metrics input is `site/showcase/v3/metrics.json`, while `06-metrics.json` is historical. Public GitHub/docs/papers inform narrow adapters, not a framework rewrite. X posts were index-only and primary sources were followed where possible.
- Research validation: `codex --version` reported `0.155.0-alpha.9.2`; `codex app-server generate-json-schema --out <temporary-directory>` exited 0, with PATH-alias/home warning and no model launch. Local Playwright research at 1440×1000 and 390×844 reproduced the clipboard problem, with zero JS errors/overflow; not a complete visual or Python suite rerun. Published landing HTTP read returned 200 and a recorded content hash. Source/license snapshots and reproduction notes are in the dossier. The September 21 92/92 and Docker 12/12 results remain historical.
- Document validation: Python 3.12 parsed `evidence.json`, checked its non-benchmark/unknown-usage invariants and resolved 16 relative Markdown links across six documents; exit 0. `git diff --check` passed. No tests were counted as skipped or newly passing. Final staged scope is restricted to seven research/context documents.
- Host changes: none installed/configured; temporary local schema files and a research screenshot only. No external repo code executed, credentials read or paid service configured. Three native research agents were used; attributable current tokens/money are unavailable, not zero.
- Commit scope: documentation only; proposed subject `docs(roadmap): plan reliable dual-host harness and library conversion`. Actual SHA is returned after commit; no unrelated files belong in it.
- Resume: read the proposal and confirmed answers; agree the next implementation slice. Recommended P0 truthful public evidence/clipboard plus P1 shared receipt contract with parallel Claude/Codex adapters; E2 offline fixtures can use a disjoint scope. Keep mutation bounded and holdout/benchmark integrity ahead of evolution. Price, member access details and production inputs remain unset; mentoring is not the chosen offer.

### 2026-09-25 — Codex — owner-first OS alignment and integration research

- Authority: owner requested research on shared context, concurrent terminals, generated UI and 20 supplied references. The owner approved the presented own-app/Goose comparative slice, retained the explicit order of completing pending harness work first, and selected mini-tools with new logic for the first prototype. No repeat plan-approval request is needed for that bounded comparison once its prerequisite is satisfied.
- Starting branch/HEAD/state: `master` / `5f0086d7b93da039e73d3f2277dc53db889c107b`, no changes listed by Git. Git warns that the user-global ignore file is unreadable; no global configuration was changed. No remote merge, stash application or publication.
- Files: `docs/omniforge/PLAN.md`, `docs/omniforge/RESEARCH-2026-09-25.md`, `CONTEXT.md`, this ticket and the September 22 plan's supersession notice. Documentation only; runtime, skills, tests and landing unchanged.
- Decisions: reusable harness for multiple owner projects; Windows first; native Claude/Codex allowances first; optional OmniRoute; projects/chat/library plus simultaneous terminal sessions; scoped shared memory and three linked graphs; one generated mini-tool in the first prototype. Keep navigation stable and generated code in a bounded, testable extension. OpenMontage is a prioritized later audiovisual module; Dify has a narrow optional workflow role, not a new core.
- Findings: progressive skill/context loading; explicit session/project routing to prevent concurrent memory-pointer errors; Wave/A2UI, Paperclip/OpenHands and AstrBot provide relevant patterns. Continue announces maintenance end; AgentGPT is archived. Dify's additional license conditions, OpenMontage/AstrBot AGPL and ChatTTS noncommercial weights matter for distribution. Full sources/limits are in the research register.
- Evidence limits: public documentation and selected-code inspection only. No new full suite, model benchmark, container probe or host certification. The September 21 measurements remain historical. No package/service installed, external reference code executed, credential accessed or private transcript uploaded. Three native research agents assisted; current attributable tokens/money are unavailable, not zero.
- Validation: Python document check passed for all five changed/new documents: 25 local Markdown links resolve, all 20 supplied references are represented, and the prerequisite/mini-tool decisions are present. `git diff --check` passed. Independent read-only review found no material issues in the plan/research findings. No runtime tests were counted as newly passing or skipped.
- Checkpoint scope: five documentation files only; intended local subject `docs(omniforge): record harness-first plan and integration research`. Report the resulting SHA after commit; no push is authorized by this checkpoint.
- Resume: reconcile E1 telemetry with whole-tree receipts, then native execution/cancellation/recovery and all-role containment, live controls/B8/B9, scoped S6 and S7. The OS comparison follows that closure. Do not start a memory service, desktop fork, audiovisual workflow or commercial funnel in place of completing the harness.

### 2026-09-25 — Codex — canonical receipt checkpoint and accepted settings drift

- Authority: owner said to proceed with harness-first implementation; standing local-commit/native-delegation authority applies. During validation the owner explicitly chose to preserve current Claude permissions and record installation drift. Do not reapply the conservative settings fragment as an automatic fix.
- Starting state: clean `master` at `ef69c846c209fb6ce8ca3223753ecb0cbbca7335`. No stash, remote merge or push. Root changed production/new tests; one delegated agent migrated the two existing eval test modules; independent review was read-only.
- Files: `evals/records.py`, `evals/run.py`, `harness/swarm_accounting.py`, `tests/test_usage_receipts.py`, `tests/test_eval_integrity.py`, `tests/test_runner.py`; contracts/status in `docs/evals/{PLAN,RECORDS}.md`, `docs/t13/{ACCOUNTING,VALIDATION,TICKET-HANDOFF-CLAUDE}.md`, `docs/omniforge/PLAN.md` and `CONTEXT.md`.
- Delivery: eval schema 3 projects the same receipt as coordinator accounting, including whole-tree/cache tokens, client estimate versus unknown billing, source/session/process binding and explicit scope/version. The runner persists a fresh UUID and rejects case-level session/protocol overrides. Shared terminal validation prevents stale result approval. Comparator rejects contradictory metrics and requires a compatible receipt before resource comparison; old evidence is not rewritten.
- Validation: final `scripts/check.ps1` passed 100/100 Python methods, zero skips, 62.474 s; both visuals, Node 5/6/7, portal and eval selftest passed. Aggregate exit 1 is retained for the two owner-accepted `ask`/`deny` installation differences (23/25 rows conform). Graph run separately passed: 541 nodes, 97 edges, 5 proposals, 0 errors. Initial sandbox run failed only visual font access; it was rerun with authorized public network access. Exact logs/red cases are in `VALIDATION.md`. Independent final review: no blockers, 8/8 new methods passed. No dependencies were skipped.
- Host changes: none. No packages, skills, settings, guards or workflow copies were changed by this checkpoint. Read-only probes report Claude 2.1.282 and Codex 0.155.0-alpha.16.4; Codex home warning remains uninvestigated. No authentication or live model/container benchmark ran. Attributable native delegation tokens/money are unavailable, not zero.
- Commit scope: the receipt runtime/tests and listed documentation only; intended subject `fix(evals): reconcile whole-tree receipts and reject incompatible accounting`. Report the actual SHA after commit; no uncommitted runtime work is intentionally left behind.
- Resume here: implement host-specific native execution/usage/cancel/recovery contracts, preserving Claude versus Codex protocol semantics, then connect all-role containment and E1 controls/B8/B9. This closes receipt reconciliation only, not T13 or live host certification. The owner has also requested research of Shopify Helix and YouTube `bBMp5tLxShQ` and incorporation of the taught coding workflow into the skillset; handle that as a separately reviewed delivery while preserving this continuation order.

### 2026-09-25 — Codex — Helix-inspired coding skill

- Authority: owner supplied the Helix article/video and explicitly requested studying the workflow and adding it to the skillset. Receipt delivery is committed as `0f2d9dcc8b75e515c9d220feb2cc0e26c2afa936`; this extension started from its clean tree and does not reopen the settings decision.
- Scope: new `.agents/skills/checkpoint-build/SKILL.md` and `references/checkpoint-record.md`, `docs/research/helix-2026-09-25.md`, CONTEXT and this journal. No existing runtime, installer, guard, accounting module or driver changed.
- Research: full 456-cue English automatic transcript plus Helix and its two technical article links. No frame-by-frame viewing, paid community bundle or internal Helix code was accessed. Sources, timestamp map and transcript hash are recorded in the research note. Distinguish Shopify's independent reviewers from AI LABS' critic/fixer adaptation.
- Adaptation: reference-based checkpoints, TDD seams, current visual/behavioral evidence, two independent reviews, bounded correction and scoped memory. Standing task/commit authority is retained; human acceptance is never fabricated. The skill is a procedure and record template, not native gate enforcement or a globally installed Stop hook.
- Validation: structural validator passes; 12/12 layout/installer methods, zero skips, 7.232 s. Initial CRLF failure corrected to LF. Independent fresh-context instruction rehearsal covered four adversarial/positive scenarios and refined three ambiguities plus last-cycle success. Graph: 542 nodes, 100 edges, 5 existing proposals, 0 errors. Local references and staged diff checked. No claim of a second full 100-test run after this skill-only addition.
- Host changes: installed public-caption dependencies `yt-dlp 2026.8.19` and `youtube-transcript-api 1.2.4` into the selected Python; installed two collision-checked skill junctions for Claude and shared Agents discovery. Claude settings hash unchanged. Current install check is 25/27, exit 1 solely for the two owner-preserved permission differences. No login, cookie read, global hook, paid call or push.
- Commit scope: five new/updated skill/research/context files; intended subject `feat(skills): add reference-based checkpoint build workflow`. Report actual SHA and Git state after commit.
- Resume: native Claude/Codex execution/usage/cancellation/recovery, then all-role containment and live controls/B8/B9. `checkpoint-build` is now available to structure that work; it does not close those acceptance items or advance the OS prototype ahead of them.

### 2026-09-25 — Codex — native attempt adapters and six-reference intake

- Owner request / authority: “pode seguir” continued the approved harness-first native Claude/Codex checkpoint. The owner then supplied six repositories and asked for placement, study or selective skill-library admission. Standing authority covers the public read, scoped repository changes and a local commit; no external service or new skill installation was requested or performed.
- Branch / HEAD before work / starting dirty files: clean `master` / `61e00eae33f9b0390987ecf9206a67154432a659`; no unrelated working changes. Plan hash: `aeeac46e5be43f476f63bc79ea7db1727d234e77fdaf8f3c356ccecd826366c6`.
- Ticket state: CHECKPOINT READY for the offline native adapter and reference catalog, **not** T13 or benchmark completion.
- Changed files and behavior: `harness/native_process.py`, `harness/claude_native.py`, `harness/codex_exec.py`, `harness/codex_accounting.py` and four matching `tests/test_*.py` files provide exclusive one-attempt process capture, bounded cancellation/recovery and honest Claude/Codex receipt distinctions. `docs/t13/ACCOUNTING.md`, `VALIDATION.md`, `CONTEXT.md` and this journal record limits. `docs/omniforge/RESEARCH-2026-09-25.md` and `docs/catalog/community-skills.md` place all six owner references and three selective skill candidates; no external instructions are installed.
- Decisions: new native calls require read-only same-environment subscription/ChatGPT status preflights and reject API-routing project `env`/credential helpers on the Claude allowance path. Codex `exec --json` turn telemetry is observed only; its whole-tree total and billed amount stay unknown. App Server thread totals are not per-turn totals. Failed/empty/malformed/interrupted output never grants task pass. Shell/batch shims are rejected on Windows; coordinator evidence must remain outside the worker's OS-visible view. The historical 12/12 Docker process proof cannot certify today's unavailable daemon or a real agent tree.
- Validation: bundled Python 3.12.14, Windows. Focused native tests **40/40**, zero skips, 6.276 s. Final `scripts/check.ps1`: **140/140 Python**, zero skips, 80.830 s; both visual methods, Node 5/6/7, portal and eval selftest passed. Aggregate exit **1 solely from two owner-preserved Claude `ask`/`deny` installation differences**, 25/27 rows conform. Separate graph check: **545 nodes, 100 edges, 5 proposals, 0 errors**, exit 0. Log `%TEMP%/omniharness-check-20260925-native.log`; exact evidence and two independent no-blocker read-only review verdicts are in `VALIDATION.md`. `git diff --check` passed before staging.
- Failures and unresolved risks: a Windows launch-failure fixture initially hung on invalid `.exe` and was replaced with a deterministic mock. Current Docker Linux engine did not start after a nondestructive socket-only retry; no current containment acceptance or live model/credential/billing probe followed. Codex descendant coverage, all-role isolation, live E1 controls and B8/B9 remain. Attributable native subagent tokens/money are unavailable, not zero.
- Host changes: no package, user Claude setting, installed skill, guard, hook or credential change. Docker runtime socket-only directories were preserved under `run.pre-omniharness-20260925` and `docker-secrets-engine.pre-omniharness-20260925`; no data reset/delete. Read-only Claude/Codex login-status commands succeeded; no prompt/model run.
- Commit scope: only the eight new runtime/tests and six listed documentation/catalog files. Proposed subject: `feat(t13): add offline native attempt adapters and catalog references`. Actual SHA must be reported after commit; no push.
- Resume here: restore a healthy Docker engine without destructive reset, rerun `harness/sandbox_probe.py`, then bridge one native agent's tools to verified containment and coordinator ledger. Verify both hosts, all roles/retries and E1 controls before B8/B9; keep unknown Codex whole-tree usage explicit. The OmniForge own-app/Goose comparison follows that harness acceptance; memory-server and skill intake candidates remain separate experiments.

### 2026-09-25 — Codex — recovered current disposable Docker profile

- Owner request / authority: continued harness-first isolation work under standing reversible-local-setup authority; no factory reset, broad process stop or privileged container was requested. The preceding adapter/reference commit is `89d24baa76495e86f4b11f2fec6f976910a082e0` on `master` and left a clean tree.
- Ticket state: current disposable **process** profile accepted; native all-role **agent** containment still open.
- Changed files and behavior: documentation/evidence only — `docs/experiments/sandbox-containment-2026-09-25.json`, `docs/t13/{ACCOUNTING,VALIDATION,TICKET-HANDOFF-CLAUDE}.md` and `CONTEXT.md`. No runtime, settings, package, hook or test code change.
- Host recovery: inspected backend failure at `dockerInference`; verified zero Docker processes and one zero-byte reparse-point socket under the resolved Docker `run` directory. Moved that directory to a unique same-parent backup `C:/Users/Yeonatan/AppData/Local/Docker/run.pre-omniharness-20260925-2`, leaving earlier `run.pre-omniharness-20260925` and `docker-secrets-engine.pre-omniharness-20260925` untouched. Started Docker Desktop hidden; daemon `29.6.2` answered. All three backups remain, no delete/reset/credential read.
- Validation: `python -m harness.sandbox_probe --endpoint npipe:////./pipe/dockerDesktopLinuxEngine --image python@sha256:2f17fc044b579bab302c2e8054d3a686e2cb9a83de48e70534b94cd8ebbe06a9` passed **12/12** in 1.766 s, then passed **12/12** in 1.485 s for the durable JSON report. Source hash `5f7db294bfde75b50a2109bb82d791907ef7d1752bb6a5ee054ba53e7ad567a3`. The report verifies a disposable nonprivileged worker, not an agent session. The earlier full repository battery remains 140/140 Python, zero skips, and 25/27 installation rows with the two owner-preserved differences.
- Failures and limits: the first September 25 Docker starts failed as documented above. The successful process probe does not establish that Claude/Codex tool calls are routed into Docker, nor complete usage, E1 controls, B8/B9 or billed cost. No live model call or production upload occurred. Independent native adapter reviews are in the preceding entry; this host-only recovery needs no new code review.
- Commit scope: the five documentation/evidence files named above; proposed subject `docs(t13): record restored Docker containment probe`. Resolve and report the SHA after this commit; no push.
- Resume here: implement the coordinator/native tool bridge with identity and cancellation evidence for Claude and Codex, then run an actual bounded agent-tree containment acceptance against the current Docker profile. Keep credentials and receipts outside the worker; do not treat the disposable probe as this acceptance.

### 2026-09-25 — Codex — model-free worker adapter and native-boundary probes

- Owner request / authority: resume the interrupted harness-first isolation checkpoint. Standing authority covers the scoped local implementation, model-free Docker validation and commit. No push, paid external API service, credential upload or elevated Windows sandbox setup was requested.
- Branch / HEAD before work / starting dirty files: `master` / `b8cebf37772ef92318d8f06aed79c6c82dea195e`, clean after the committed Docker-recovery report. This journal entry is prepared before commit; inspect actual Git state on continuation.
- Ticket state: CHECKPOINT READY after final canaries and repository validation; staged-diff review and local commit remain. It does not close E1/T13 or native agent-tree containment.
- Changed files and behavior: `harness/container_worker.py` and `harness/container_worker_probe.py` establish one owned pinned-image Docker container per attempt, exact CID/label and observed-profile checks, command-output hashes, Git scope audit, exact cancellation and fail-closed unknown recovery. `harness/sandbox_probe.py` strengthens negative outside-path and network-namespace checks. `harness/codex_windows_probe.py` and `harness/claude_tool_inventory.py` add separate native-host boundary probes. Matching tests are under `tests/`; JSON results under `docs/experiments/`. `docs/t13/{ACCOUNTING,VALIDATION,NATIVE-ISOLATION,TICKET-HANDOFF-CLAUDE}.md` and `CONTEXT.md` document status and limits.
- Decisions: a successful container command keeps `task_pass=null`; the worker assumes one coordinator process per attempt and needs an interprocess lease before concurrent coordinators. Native Claude's candidate requires an exact observed startup inventory and a sole bounded tool broker; Codex `exec --json` has no verified all-tool callback. The native Windows profile remains an alternative hypothesis, not a passed boundary. Neither disposable probe becomes a benchmark or an E1 control result.
- Validation: strengthened real process canary **14/14**, 4.984 s, [renewed durable report](../experiments/sandbox-containment-2026-09-25.json); real model-free worker-adapter canary **18/18**, 5.313 s, zero issues, exact container removed and no fixture preserved, [renewed durable report](../experiments/container-worker-2026-09-25.json). Current source hashes are pinned in both. Focused offline suites: container worker **15/15**, Codex Windows probe **15/15**, Claude startup inventory **5/5**, shared process-probe regression **1/1** (**36/36** targeted). The final full battery includes the fixture-cleanup guard; its focused regression also passed **1/1 in 0.522 s**, followed by both renewed real canaries. The selected Windows Codex canary itself is [inconclusive](../experiments/codex-windows-probe-2026-09-25.json), exit 1, elevated backend required; its synthetic Codex home gained a log and `tmp/arg0` of uncertain attribution, so no host-artifact pass was issued. The first `scripts/check.ps1` attempt, before review fixes, ran **173 Python methods, zero skips**, but failed five visual assertions when the restricted command sandbox denied Google Fonts. Final direct runner: **176/176 Python methods, zero skips**, both visual tests, Node/portal/eval passed; aggregate exit **1 solely for two owner-preserved Claude `ask`/`deny` differences** (25/27 rows). Log: `C:/Users/Yeonatan/AppData/Local/Temp/omniharness-check-20260925-isolation-final.log`. Separate graph: **545 nodes, 100 edges, 5 waiting proposals, 0 errors**, exit 0; `git diff --check` exited 0. Staged-diff review and commit remain pending.
- Failures and unresolved risks: first real worker canary timed out on cold `docker create` with an empty CID file; exact name/nonce-label inspection found no container. A later canary passed checks but failed disposal of its own read-only Git object; cleanup was corrected, and the fixture removed. Review then found that PID/IPC namespace modes, uncertain create evidence and missing Codex-home probe paths needed explicit checks; all three were repaired and the real canaries rerun before the current reports were saved. A synthetic Claude no-tools subscription turn timed out before a final receipt; effective tools, token use and billing remain unknown, with no retry of that uncertain attempt. An automatic approval review paused the durable-report command at the account usage limit; that rejected command was not executed, and the resumed authorized run saved the report. No live Claude/Codex tool turn or descendant accounting is certified.
- Host changes: no package, user Claude permissions, hook, skill, credential, Docker data reset or administrator Windows sandbox change in this slice. Disposable Git/container fixtures were removed by exact identity; the earlier socket-only Docker backups remain preserved.
- Commit scope: the new runtime/tests, strengthened probe, three current experiment reports and the T13/CONTEXT documentation; proposed subject `feat(t13): add model-free worker isolation probes`. Actual SHA is pending final validation and commit. No push is authorized.
- Resume here: inspect the exact staged scope and diff, then commit locally and report the resulting SHA. Afterward connect one native agent's tools to a coordinator-owned bounded worker, verify effective inventory and negative host-file canaries, and extend to both hosts/all roles with accountable usage before E1 controls and B8/B9.

### 2026-09-25 — Codex — reviewed native bridges, E2 fixture and OmniForge Lab candidate

- Owner request / authority: continue the approved E1/T13 and parallel OmniForge candidate work, incorporate Prompt Copilot/OS feedback without interrupting validation, and prepare a parallel mascot-design handoff. Standing authority covers scoped local tests and commit, but not push, publication or additional-charge model calls. The owner selected a Discreet Copilot default and native Claude Code terminal plus optional API-key route for the distributed V1. No new provider key was accessed or live model call initiated.
- Branch / HEAD before work / starting dirty files: `master` / `eedc43588b9187c36c4a0a9a0641ae9c7cbaa9c0`, clean. Ticket state: **CHECKPOINT READY for staged-scope review and local commit**; the actual SHA must be read from Git after commit.
- Changed files and behavior: Claude MCP broker/worker bridge, binding/probe/tests and model-free durable report under `harness/`, `tests/` and `docs/experiments/`; Codex App Server bridge/contracts and handshake report in the same areas. `omniforge-lab/` supplies the loopback Lab with project/task/state store, scoped notes, PTYs, graphs, skill/asset views and tests; asset traversal, context brief and cross-project terminal-pane selection received explicit bounds after independent review. `evals/task_graph.py` and `tests/test_task_graph.py` add deterministic E2 graph fixtures with criterion/version-bound reference identity. `scripts/check.ps1` includes the Lab suite. OmniForge plan, release contract, Goose desk comparison, Copilot/UI intake, mascot-design handoff and CONTEXT record decisions and limits.
- Validation: final `scripts/check.ps1` **215/215 Python, zero skips**, including both visual methods, and **26/26 Lab Node, zero skips**, other Node/portal/eval selftest passed. Aggregate exit 1 solely on the two owner-preserved user Claude `ask`/`deny` rows (25/27 installation rows). Log `C:/Users/Yeonatan/AppData/Local/Temp/omniharness-check-20260925-v1-postreview.log`; separate graph check 545 nodes/100 edges/5 proposals/0 errors. The real Claude broker-to-Docker probe passed 26/26 model-free checks with `task_pass=null`, `tokens=null`; the Codex App Server initialize handshake was repeated against the current bridge hash without a thread/turn. E2 focused scorer 8/8 passed. A first restricted-sandbox Lab run after bounds had 23/24 pass with a Unicode-PTY shutdown timeout; a full rerun under normal host permissions passed 24/24 before the two pane-scope tests were added. Exact details and limits are in [VALIDATION](VALIDATION.md).
- Failures and unresolved risks: ConPTY can leave an orphan grandchild after shell stop; the UI calls that state `shell encerrado`, not whole-tree cancellation. `node-pty` emits cleanup-helper `AttachConsole failed` diagnostics even on passing runs. Native Claude/Codex exclusive tool routing, all-role worker confinement, attempts' executable-source pinning, reconciled descendant/token usage, live E1 controls, B8/B9, installer and five-pillar V1 acceptance remain open. The E2 scorer has no human-calibrated quality claim or external checker digest. Goose was reviewed from sources, not installed or run. The distributed Claude mode must follow the provider's API-key guidance; native terminal usage cannot be marketed as subscription-funded managed execution. Automatic browser-use review rejected the final localhost Lab visit because the repo requires Claude Browser or Playwright; the final pane edit has unit/module-route but no new browser visual verdict.
- Host changes: project-local `omniforge-lab` npm dependencies are present and lockfile is tracked; no user Claude permission rewrite, credential read, global plugin install, Higgsfield credit use, push or publication. The source-run Lab can be relaunched from the documented command. Native Codex subagent tokens/cost for this checkpoint are unavailable, not zero.
- Commit scope: stage only the listed task code, tests, evidence and documentation after inspecting the staged names/diff. Proposed subject `feat(omniforge): add local lab and model-free host bridges`. Report actual full SHA and any remaining worktree changes after commit; no self-referential SHA edit is needed.
- Resume here: verify Codex/Claude native tool inventory and exact broker-source binding for one live managed attempt, with deny/read/write/cancel/descendant canaries, before expanding to both hosts or E1 controls. In parallel, the mascot design task should touch only `docs/omniforge/mascot/` and return two reviewed skin prototypes; do not let its visual work claim or edit executor acceptance.

### 2026-09-25 14:21 -03:00 — Codex — status and native-browser route clarification

- Owner request / authority: explain actual blockers, assess native Computer Use as an alternative, and provide a short mascot-session prompt. Earlier standing authority covers scoped rule maintenance; a new explicit local-browser consent question is pending after automatic review rejected the visit again.
- Branch / HEAD before work / starting dirty files: `master` / `fa620d61f72c4566c00b802e792b4a04556e809c`, clean. Ticket state: implementation can continue; visual browser visit awaits explicit scoped consent.
- Changed files and behavior: proposed `AGENTS.md` routing clarification permits available native host controls distinctly from external `browser-use`; this ticket records the actual committed checkpoint and current permission outcome. No runtime or test change.
- Validation / review: read-only independent review found no blocking issue in the narrow rule diff; `git diff --check` passed. Existing 215 Python / 26 Lab results are historical to the implementation checkpoint, not rerun for this documentation-only change.
- Failures and unresolved risks: native browser documentation and tab inventory were readable; the previous Lab tab was absent. Opening `http://127.0.0.1:52664/` was auto-review denied under the prior restriction. No alternative surface, indirect browser command or further visit was attempted after this denial. A local file edit cannot override native approval review.
- Host changes / accounting: none; no dependencies, user permissions, credentials, provider calls, Higgsfield credits or publication. Native review-agent usage is unavailable, not zero.
- Commit scope: only AGENTS and this ticket are modified; leave the proposal uncommitted pending the explicit browser-route answer. The prior implementation SHA is reported above.
- Resume here: apply the owner's answer to the narrow browser-route proposal, then re-evaluate only through normal permission review. Ordinary implementation remains authorised; its next acceptance slice is the bounded native managed-attempt evidence described in the preceding entry.

### 2026-09-25 14:40 -03:00 — Codex — full MVP authority and first implementation checkpoint

- Owner request / authority: implement the entire agreed MVP, explicitly remove the old browser-tool restrictions and use mascot design/animation as the first real test. The subsequent answer specifically authorised `mcp__cua_repl` at local `127.0.0.1`. The full objective is active in Codex goal state; no reduced scope or completion claim.
- Branch / HEAD before work / starting dirty files: `master` / `fa620d61f72c4566c00b802e792b4a04556e809c`; only the prior AGENTS/ticket proposal was dirty. Ticket state: MVP implementation IN PROGRESS; parser checkpoint technically verified.
- Changed files and behavior: AGENTS removes tool-name prohibitions while preserving native review and consequential-action limits. OmniForge plan/contract/intake/handoff and CONTEXT record accepted scope and task boundaries. `harness/codex_accounting.py` accepts the current App Server JSONL envelope with omitted `jsonrpc`; regression tests preserve invalid-version/duplicate rejection and unknown usage coverage. Actual code changed in isolated `_intake/mvp-accounting`, then only its two reviewed files were integrated into root.
- Parallel artifact task: `/root/mvp_scope_audit` works only in `_intake/mvp-mascot`, branch `codex/mvp-mascot`, under `docs/omniforge/mascot/`. Its acceptance was fixed in the existing mascot handoff before implementation. This native task is not yet an OmniForge-managed dispatch; artifact tests and visual feedback cannot certify executor isolation or accounting. Do not stage its incomplete work into root or discard either worktree.
- Validation: current root battery 217 Python / 26 Lab, all passed with zero skips, other Node/portal/eval passed; aggregate 1 retains only intentional Claude settings differences. Separate graph passed. Two independent parser reviewers gave no-blocker verdicts. Exact evidence and reference hashes in the newest [validation entry](VALIDATION.md).
- Failure / limit: automatic review continued to reject the native-browser visit even after exact connector approval, identifying its internal backend as a different `browser-use` tool. No bypass or further browser retry followed. No new visual verdict. Installed App Server schemas still do not prove a global built-in-tool allowlist; do not flip `turn_admission` merely because a dynamic worker tool is exposed. Read-only Windows sandbox readiness/profile probes are a possible next investigation before requesting any administrative setup.
- Host changes / usage: two local Git worktrees; no package, global setting, credential, paid provider, Higgsfield credit, push or publication. Native delegate/reviewer token and money totals are unavailable, not zero.
- Commit scope: only reviewed root parser/tests and task documentation, proposed subject `fix(accounting): accept current Codex JSONL envelopes`. Mascot source remains in its own worktree pending checks/review/visual acceptance. Report the root SHA after commit without an extra self-reference commit.
- Resume here: review the mascot's returned artifact and behavioral evidence, then continue independent catalog/Copilot and OS checkpoints in the accepted plan. In parallel investigate the effective native isolation/readiness boundary before the managed live attempt; all five pillars, Windows delivery, full UI features and calibrated evolution remain required. Browser validation needs a resolved platform permission route; existing owner permission should not be requested again merely under another alias.

### 2026-09-25 — Codex — reviewed MVP foundation and deferred-validation policy

- Authority: owner authorised scoped commits, branch pushes, PRs and merges, added agent arsenal and required Laya/JEV, then explicitly permitted nonblocking unavailable tests to wait for a final functional-MVP round. Current AGENTS and [pending register](../omniforge/VALIDATION-PENDING.md) supersede earlier no-push and blanket checkpoint-stop wording. Preserve integrity, secrets and executor evidence limits.
- Starting state: `master` at `2d035343a09ae59800969df62ee0942f9ab386d9`; continuation branch `codex/mvp-foundation` created without discarding work. `origin/master` was observed 11 local commits ahead / 2 remote commits behind after fetch; the remote Phase C documentation must be preserved when reconciling. No history rewrite or unrelated PR merge.
- Files/behavior: new `harness/skill_catalog.py`, its 17 tests, ten curated overlays and frozen query diagnostic; four Lab memory/backend/API test files; seven `docs/omniforge/mascot/` files and runner inclusion. Docs/context record current authority, architecture, API behavior and deferred checks. Native readiness/canary JSON contains no credentials or private session transcript. The raw skill snapshot, runtime weights and isolated worktrees stay ignored.
- Validation: final integrated 234 Python / 34 Lab / 7 mascot, zero skips; Node/portal/eval passed, graph 545 nodes/100 edges/0 errors. Aggregate check exit 1 retains only two owner-preserved Claude installation-policy differences. Each implementation has two independent source reviews; catalog used two repair cycles, memory and mascot one each. Exact hashes, commands, failures and limits are in [VALIDATION](VALIDATION.md).
- Limits: catalog engine is not yet connected to the Lab's eight-skill UI; only 10 of 114 installed canonical skills have curated extended metadata. Mascot is a tested local design artifact with illustrative templates, not live semantic assistance. Memory editing/history/archive work through the API, not new UI controls. Visual evidence remains pending. The restrictive native Windows canary did not execute, so managed admission remains closed.
- Host changes/parallel next work: Laya 0.3.20 and dependencies installed in ignored `.omniharness/runtime/laya-venv`, public Apache-2.0 multilingual weights downloaded at pinned revision with checked hashes; adapters are a separate in-progress `_intake/mvp-classifiers` writer. No JEV key, paid call, new native managed model task or deployment. Native delegation usage unknown.
- Commit scope: reviewed foundation code/tests/docs only, excluding the next Laya artifact manifest until that checkpoint is reviewed. Subject `feat(omniforge): add catalog, versioned memory and mascot foundation`; report actual SHA. Retain all worktrees.
- Resume: connect catalog and Copilot to the Lab; complete classifier adapters with local Laya measurement and offline JEV tests; add workflow/terminal/memory UI and agent arsenal in disjoint scopes. Track account/visual/clean-install/human gates in the final-round register while continuing implementation. Before any managed mode or certified eval claim, resolve native tool/descendant isolation and complete receipts. Full MVP goal remains active.

### 2026-09-25 — Codex — connected Copilot and reviewed Laya/JEV boundary

- Starting Git: foundation `bb27730`, remote-doc merge `6ef6975`, [PR #3](https://github.com/ymauhn/omniharness/pull/3) merged to `0b7e0b0a5c457540dba4a70e313d489fdebe26f7`; then branch `codex/mvp-copilot-integration`. Remote history and all isolated worktrees retained. No unrelated PR merged or product deployed.
- Scope: full catalog Python/Node/API connection and Lab Copilot UI; typed Laya/JEV module; local Laya runtime/version/artifact observations and user-facing docs. Source IDs remain separate from permission. No keys, model weights, raw session exports or local catalog snapshot enter Git.
- Validation: 259 Python / 51 Lab / 7 standalone mascot, zero skips. Aggregate exit 1 only for accepted Claude policy drift. Graph and diff checks pass. Two final source reviewers per UI, API and adapter slice. See the newest [validation record](VALIDATION.md) for hashes, red failures, repair cycles, identities and exact limitations. An initial multi-file documentation patch failed exact-context verification and was reapplied without partial mutation; it did not alter runtime evidence.
- Actual Laya: installed isolated venv and hash-pinned model; 2/3 unique smoke matches, 205 ms warm median, ~1.60 GiB working set. The missed positive remains in evidence. JEV has offline adversarial tests; no live account/credit test. Current composer uses templates and lexical metadata, not either model yet.
- Parallel ownership: `/root/mvp_scope_audit` owns only new resident-worker/service/test files in `_intake/mvp-classifier-worker`; `/root/mvp_accounting_review_a` owns original arsenal/Agent Forge files in `_intake/mvp-agent-arsenal`. Root owns server/UI integration. Do not overwrite or stage unfinished worktree artifacts wholesale. Unknown delegation tokens/money remain unknown.
- Resume: integrate and test the resident classifier with explicit warm-up/unload, bounded queue/deadline and fallback; connect reviewed arsenal to tasks; proceed with Workflows, dynamic terminals, memory editing and sourced graph help. Preserve all five pillars, generated mini-tool/repair, provider/vault, packaging and evaluation requirements. Keep final-round unavailable checks visible and managed admission closed until actual confinement/accounting evidence exists.

### 2026-09-25 — Codex — resident local classifier and original agent arsenal

- Starting Git: Copilot commit `e775b5c1049ed21a4e5e5ad19224cb6e11e5272e`, [PR #4](https://github.com/ymauhn/omniharness/pull/4) merged to `af369b248d7cda94a0bd38c36af9373df4809839`; branch `codex/mvp-local-assistance`. Scoped commit/push/PR/merge authority persists. No product deployment or unrelated change.
- Changes: `harness/classifier_worker.py`, Lab classifier service/API/provider integration and tests; five original profiles and versioned `harness.agent_arsenal`, tests and Agent Forge skill; sanitized local HTTP observations and linked docs. Explicit worker loading is connected to the composer, default remains lexical. JEV key/provider UI is still required. Arsenal is a reviewed backend/authoring skill, not yet a task/UI dispatch feature.
- Validation: final 282 Python / 69 Lab / 7 mascot, zero skips; other Node/portal/eval checks passed. Aggregate exit 1 only from the owner's two preserved Claude permission differences, 27/29 install rows conform. Graph and diff checks passed. Two independent reviewers per worker, arsenal and API/UI slice rechecked fixes. Exact commands, hashes, failures and limits are in the latest [validation entry](VALIDATION.md).
- Repairs: worker pipe errors now fail the request without crashing the Lab; arsenal disable retains rollback history; GET/POST classification queries use the same UTF-8 bound. Actual final local HTTP warm-up 6.627 s; requests 0.631–0.947 s. Lifecycle passed, two positive prompts abstained and quality is explicitly unknown. Warm-up token accounting remains incomplete. Initial smoke retained. No JEV account/key or paid call.
- Host changes: only absent `~/.claude/skills/agent-forge` and `~/.agents/skills/agent-forge` junctions created to the reviewed repo skill. No global installer adoption, Claude permission rewrite, credential read or browser retry. Existing runtimes/worktrees/backups retained. Native delegate/reviewer usage unavailable.
- Commit scope: these runtime/tests/skill files, two sanitized smoke reports and task docs only. Intended subject `feat(omniforge): enable local classifier and versioned agent arsenal`; report the actual SHA after commit. Do not copy stale worker-service files from its authoring worktree over root: the final pipe repair is in root.
- Resume: Workflows and dynamic terminal layouts can proceed in disjoint scopes; connect memory history/edit/archive and arsenal to OS tasks next. Continue vault/JEV UI, generated mini-tool/repair, packaging, five pillars and calibrated eval/evolution. Preserve pending final checks separately and never enable managed execution until real tool containment/cancellation/attribution is proven.
