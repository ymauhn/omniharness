# Native host adapter continuation after PR #9

Updated September 25, 2026. **Full OmniForge MVP remains active; managed native-agent admission stays closed.** [PR #9](https://github.com/ymauhn/omniharness/pull/9) merged the model-free source and all-role Swarm callback gates as `c8e1bb231d25f499e3ca23704cbca1d39384854e`. Root created `codex/mvp-native-host-adapter` cleanly from that merge and pushed a docs-only resume commit `98853f4`. The last read-only account query showed **7% weekly Codex allowance remaining**. No reset credit was consumed. Always inspect actual Git status, this checkpoint, [LATEST-CHECKPOINT](../omniforge/LATEST-CHECKPOINT.md) and the [T13 journal](TICKET-HANDOFF-CLAUDE.md) before resuming.

## Exact interface gap

`swarm/swarm.workflow.js` now requires each role/candidate/retry to receive a trusted `worker_identity` in preflight, dispatch, post-run verification and a complete whole-tree receipt. `harness/swarm_accounting.py` has unique labeled SQLite reservations and captures Claude's final `modelUsage` plus the immutable stream hash, but returns its native receipt schema, not the JS callback fields. `harness/claude_worker_bridge.py` starts its Docker worker and creates a session inside the agent function, so the coordinator cannot preflight an already verified worker identity. No production adapter wires these together. The current Codex parser deliberately reports descendant usage incomplete and the dynamic tool broker does not prove exclusive native tools.

## Active disjoint model-free work

- `/root/ledger_label_contract` edits only `swarm/swarm.workflow.js` and `tests/test_swarm_driver.js`: pass the exact attempt label into every budget reservation; retain duplicate/unsafe lease rejection before dispatch. Focused Node tests and independent review precede integration.
- `/root/claude_prepare_split` edits only `harness/claude_worker_bridge.py` and `tests/test_claude_worker_bridge.py`: split a verified, coordinator-owned Docker prepare/start and session/MCP binding from one run/finalize; preserve the old wrapper and fail closed on mismatched/reused handles. Focused Python tests and independent review precede integration.
- Root owns only docs/review/integration while those file groups are active. No authenticated model call, new paid provider, credential read or Windows privilege change is part of this model-free source slice. Native allowance work remains governed by the owner's standing task authority; additional-charge services and privilege changes retain their separate gate. Record every attempted test and actual unknown usage; a failed Docker/PTY probe is not green.

## What a later host adapter must prove

Build a coordinator-owned callback facade only after those seams are validated. It must derive identity from verified Docker CID/nonce and exact worktree, pass that to the agent launch, wrap the agent's task report with host evidence, settle the captured stream through `Ledger.settle`, and map `estimated_usd`, disjoint `total_tokens`, `source_sha256`, `usage_complete`, attempt and reservation without model-provided values. Verify exact container removal and worktree audit for every implementer, integrator and reviewer; leave unknown receipts unknown. Test A/B identity, replay, crashes, cancellation and all roles offline. Avoid reconstructing one attempt's `ContainerWorker` in an unrelated process; its lock and command ledger are process owned.

The prepared bridge must place verified CID/nonce in its immutable handle and broker binding. As of PR #9, `harness/claude_broker.py` ignores those fields and loads state by attempt ID; a separate broker-side check against the durable worker state is needed before serving tools. Do not call the identity chain complete merely because the coordinator handle and JavaScript callback agree.

**Live gate remains separate:** Claude's current print-mode prompt is submitted before `system/init` tool inventory is inspected; the bridge's inventory is a post-run check. Reviewer worktree mount is writable. Neither a callback fixture nor a completed Claude stream establishes pre-turn exclusive tools, read-only review, exact descendant cancellation or billed provider spend. A bounded real canary requires a supported pre-prompt gate (or another proven exclusive tool route), host/sibling/source denial sentinels, actual worker identity, complete receipts and exact cleanup. The prior synthetic Claude attempt timed out at 90 seconds with unknown usage; do not replay it blindly. The Codex Windows restrictive canary never reached its child; do not widen `:root` access and call it equivalent. Keep V-04 open.

If interrupted mid-edit: inspect the two agents' files and Git diff before any new changes; do not copy older `_intake/mvp-*` authoring worktrees over root. Run targeted tests first, then `scripts/check.ps1` on the normal host with zero skips, recording the intentional Claude `ask`/`deny` drift separately; run the graph check if the runner stops at installation. Commit reviewed source and exact validation, update this note, [VALIDATION](VALIDATION.md), [LATEST-CHECKPOINT](../omniforge/LATEST-CHECKPOINT.md) and [V1 release contract](../omniforge/V1-RELEASE-CONTRACT.md) status. Preserve backup `C:/Users/Yeonatan/.claude/workflows/swarm-driver.js.pre-omniharness-20260925-190827`, other old backups, dirty `_intake` worktrees and stash `8105be6`.
