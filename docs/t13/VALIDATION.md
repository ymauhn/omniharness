# T13 validation record

## 2026-09-26 — offline host callback/ledger facade

Branch `claude/t13-host-facade` from `81d9a13`, Windows 11, bundled Python 3.12, Node 24.19.0. Every new test was run red before its implementation:

- ledger: `attempt`/`issue` missing;
- host module absent;
- T13/D1: the earlier container was stopped by a colliding prepare;
- D2: the driver kept launching after an isolation failure;
- `Worktrees.git`: stdin was inherited.

Four mutations of the facade guards each turned a host test red:

- reconcile skipped;
- durable-row check skipped;
- report identity unchecked;
- gate ignored in verify.

The JSON-lines test first hung in `git rev-parse`. A faulthandler dump showed Git for Windows blocked on the stdin pipe that the host was reading. Fixed at `Worktrees.git`.

- **Focused tests.**
  - `tests.test_swarm_host`: 16 methods, T1–T13.
  - Ledger, bridge, broker and worktree tests passed.
  - `node tests/test_swarm_host.js`: 5/5 scenarios (T3, T4, T5, T9/T11, T11), stable in 3 consecutive runs.
  - `node tests/test_swarm_driver.js`: 11/11.
- **Full suite.** `python -m unittest discover tests`: **322 tests OK, zero skipped**, 145.7 s. The first run errored only in `test_omniforge_e2e` because this fresh worktree lacked `omniforge-lab/node_modules`. `npm ci --prefix omniforge-lab` installed the locked `node-pty` 1.2.0-beta.15; npm did not run its install scripts, and the shipped win32-x64 prebuild loaded. The e2e module then passed 5/5.
- **Node suites.** `test_driver.js` 5, `test_scout_driver.js` 6 and `test_site.js` 1 passed.
- **Not run.** The Lab/mascot npm suites and the full `scripts/check.ps1`.
- **Probe.** The model-free MCP→Docker reprobe on this source passed **26/26** in 12.2 s: `model_invoked=false`, `task_pass=null`, container removed, no fixture preserved, no labelled container left. Report: [claude-broker-2026-09-26.json](../experiments/claude-broker-2026-09-26.json). The 2026-09-25 report is unchanged.
- **Limits.** Offline fixtures only: fake Docker CLI and a fake model turn. This establishes no live isolation, provider cap, billed usage or managed admission.

## 2026-09-25 — prepared Claude worker and attempt-labeled reservations

Source commits `ce3950d7fe09566b95d7596f88e8e0d0b327b7cd` and `6c33ff206151084159b9d613103a9077686684e5` on `codex/mvp-native-host-adapter`; exact handoff in [NATIVE-ADAPTER-CHECKPOINT](NATIVE-ADAPTER-CHECKPOINT.md). The Swarm coordinator passes one exact label to each reservation. The Claude bridge can prepare/start a verified Docker worker before dispatch, consume one pinned handle, and confirm exact removal; broker startup binds durable CID/nonce. Offline tests cover A/B identity, reused handle, stale deadline, changed MCP binding, stop without removal and duplicate reservation. Independent review found the false cancelled-state success and rechecked its fix. No real model turn, host callback facade or full managed isolation is claimed.

- Focused Python broker/bridge/probe **34/34** and Swarm Node **10/10** scenarios passed. The real model-free MCP→Docker reprobe passed **26/26** on the normal host with `model_invoked=false`, `task_pass=null`, container removed and no fixture preserved. A restricted Docker-pipe failure from the previous bridge slice remains recorded in [NATIVE-BRIDGE-CHECKPOINT](NATIVE-BRIDGE-CHECKPOINT.md).
- Final `scripts/check.ps1`: **298/298 Python in 88.971 s**, **150/150 Lab**, **7/7 mascot**, zero skips; other Node/portal/eval passed. Log `%TEMP%/omniharness-native-adapter-final-20260925.log`, SHA-256 `4474cf6c954f21c887baa81fd34f64f29a56d349a6172e77651b2db3c095bcfe`. Installation **27/29**: only the owner-preserved Claude `ask`/`deny` lists differ, so aggregate exit 1. Graph checked separately: **546 nodes, 100 edges, 5 proposals, 0 errors**. Installed Swarm driver backed up as `C:/Users/Yeonatan/.claude/workflows/swarm-driver.js.pre-omniharness-20260925-adapter-192234` and synchronized.
- Live gates remain open: no production adapter maps the Python ledger to Swarm callbacks, Codex descendants remain uncounted, Claude inventory is observed after prompt submission, the reviewer mount is writable, and broker/coordinator instances have separate process-local locks. No client-estimated USD value is a billed provider total. [V-04](../omniforge/VALIDATION-PENDING.md) remains pending.

## 2026-09-25 — broker source consistency and all-role Swarm admission

Validated source `322337b0c8c83bf98111c031f4130eaab622f8e0` on `codex/mvp-executor-boundary`; detailed [native bridge checkpoint](NATIVE-BRIDGE-CHECKPOINT.md). The bounded source pins cover three named Python modules and absence of `harness/__init__.py`, with fresh cache prefix before broker import. Every Swarm role now requires a per-attempt OS-proof callback, matching worker identity and complete whole-tree usage receipt; duplicate reservation identities stop before dispatch. Independent reviews found the malicious cached `.pyc`, package initializer, A/B worker identity and duplicate lease gaps, then rechecked their adversarial fixes. This is an offline coordinator/bridge validation, not an authenticated all-role agent or billed-usage result.

- Focused Claude broker tests **24/24**, Swarm driver **10/10** scenarios, and model-free MCP→Docker reprobe **26/26** on the normal host passed; no model was invoked. An earlier restricted-sandbox probe failed before broker startup on Docker pipe access and preserved `%TEMP%/omni-worker-acceptance-claude-broker-x2ib5zde` (`cid:null`, `phase:unknown`).
- Final `scripts/check.ps1`: **288/288 Python in 89.051 s, 150/150 Lab, 7/7 mascot**, zero skips; other Node/portal/eval checks passed. Log `%TEMP%/omniharness-native-boundary-final-20260925.log`, SHA-256 `c5fc3418fc55d7f2a4e56048d56826002cf6076bca042ea39822011235264e88`. The installed Swarm driver was updated with backup `C:/Users/Yeonatan/.claude/workflows/swarm-driver.js.pre-omniharness-20260925-190827`. Installation is **27/29**; only the owner's preserved Claude `ask`/`deny` rows differ, so aggregate exit 1. Separate graph check: **546 nodes / 100 edges / 5 proposals / 0 errors**. An earlier full run, before driver synchronization, showed a third install drift; it is not the final result.
- No complete Codex descendant usage, real effective tool inventory, per-role containment, verified PTY orphan cancellation or live bounded task was established. [V-04](../omniforge/VALIDATION-PENDING.md) remains open. Native delegate token and cost attribution remain unknown.

## 2026-09-25 — isolated guided demo and scoped graph help

Branch `codex/mvp-guided-demo` started clean at merged [PR #7](https://github.com/ymauhn/omniharness/pull/7) SHA `ed8923646c3e13b1f00b2edbad4a094c0d0b468a`; documentation-first checkpoint `4b53101`. This slice adds a fresh synthetic OmniHarness demo launch path and hover/keyboard help for graph nodes. It does not start a model or certify a managed executor, human visual result or product installer.

- Source: `omniforge-lab/demo.mjs`, `scripts/start_omniforge_demo.ps1`, `omniforge-lab/index.html`, and focused `test/demo.test.mjs`/`test/graph-help.test.mjs`. The demo uses a new temp data directory, a checkout-root project with three dependent open tasks, Build/Pesquisa PTYs, clearly synthetic accepted/rejected session notes and a second isolated project. It refuses reseeding. Its API test requires the local token; failed startup/cleanup prints both error causes and the preserved data path. Missing Lab npm dependencies trigger `npm ci` from the tracked lockfile; that fallback was **not exercised** on this already configured host.
- Two normal-host `scripts/check.ps1` runs after initial demo/graph source returned **283/283 Python, 150/150 Lab, 7/7 mascot**, zero skipped, with other Node/portal/eval checks passing. The later log is `%TEMP%/omniharness-guided-demo-final2-20260925.log`: Python 90.253 s, Lab 23.154 s, mascot 0.062 s. The aggregate exit is **1 only because the owner-preserved Claude `ask`/`deny` rows differ** (27/29 installation rows conform). A separate graph check passed **546 nodes, 100 edges, 5 proposals, 0 errors**. After the final narrow graph layout edit, a complete normal-host Lab rerun passed **150/150, zero skips, 22.610 s**; focused demo/graph checks passed 4/4 and `git diff --check` passed. Thus the latest Lab source was checked after the last code edit; Python and other components were unchanged after the normal full runner.
- A separate **restricted-sandbox** `npm --prefix omniforge-lab test` run failed 149/150 on the real Windows Unicode PTY cleanup (`AttachConsole failed`, `Encerramento do PTY 45620 não confirmado`), with zero skips. The same complete suite then passed 150/150 on the normal host. This failed attempt is retained, not counted as green or hidden by skipping the PTY case. It does not establish reliable PTY process-tree cancellation.
- Actual foreground launcher smoke printed fresh `%TEMP%/omniforge-demo-M0Y7HJ` state and an exact loopback token URL (not recorded). Ctrl+C returned **exit 1** after approximately 15 seconds because `Encerramento do PTY 12044 não confirmado`; both saved sessions were `interrupted`, while read-only checks found their recorded PIDs 12044 and 5060 absent. The state was preserved, no model was called, and missing-dependency fallback was not exercised. This is a failed shutdown confirmation and no descendant-cancellation claim. [Demo checkpoint](../omniforge/DEMO-CHECKPOINT.md) and [guided acceptance](../omniforge/GUIDED-ACCEPTANCE.md) retain reproduction and unfilled visual evidence.
- Independent read-only reviewers: `/root/demo_review` found hidden simultaneous startup/shutdown diagnostics and Ctrl+C overclaim; both were fixed with a focused regression. `/root/integrated_demo_graph_review` found transient catalog-error cache permanence, hover priority over keyboard focus, popover clipping and pointer gap; all were fixed and the DOM test was expanded for rejection/retry, overlap, viewport clamping and pointer passage. Final reviewer verdict: **no remaining source blocker** in the reviewed files. No browser visual navigation was attempted after platform denial; V-01 remains open.


## 2026-09-25 — project-bound agent arsenal in the Lab

Branch `codex/mvp-arsenal-ui` started from merged PR #6 (`4f4fb0362dadbfdd7c78f6001a36e36a685ed4a0`). Its interruption handoff was committed first as `922ec92d01566773019938d851d0bea2bd274680`; reviewed source commit **`43f95690c157733a424aab75f6b39e7a3aa1c8e1`**. This slice connects the existing `harness.agent_arsenal` registry and Agent Forge procedure to actual Lab project, selected session-note and task identities. No second agent registry, host export or model dispatch was added.

- Final `scripts/check.ps1` on the normal Windows host: **283/283 Python in 95.903 s**, **146/146 Lab in 22.519 s**, **7/7 mascot in 0.060 s**, zero failures/skips. Existing portal visual tests, other Node scenarios and eval selftest passed. Aggregate exit **1 solely because the two owner-preserved Claude user `ask`/`deny` lists differ** from the install profile; 27/29 rows conform. Log `%TEMP%/omniharness-arsenal-final-20260925.log`. The separately required graph check passed **546 nodes / 100 edges / 5 pending proposals / 0 errors**. `git diff --check` passed before documentation was updated. No source file was changed after this full run.
- Backend: `harness/agent_arsenal_bridge.py` and `omniforge-lab/arsenal-service.mjs` use bounded short-lived Python JSON stdin, strict project UUID pathing, filtered child environment, generic errors and per-project registry files. The existing registry gained public scoped pin-list/find methods; absent pin is 404, corrupt registry remains unavailable. Mutations return a revision matching their returned snapshot under concurrent writers. **16/16 focused Python** and **5/5 focused Node bridge** checks passed.
- API/UI: authenticated project-bound routes show five built-ins, version previews, explicit per-rule and inherited-settings review, activation/disable/rollback, and immutable task pins with `runnable:false`. Derivation resolves only user-selected current session notes on the server, preserving rejected/quoted/experimental text as provenance rather than rules. The browser sends selected IDs, revisions, classifications and sanitization attestations, not raw excerpts. Cross-project sources/tasks, stale/archived notes, forged review, unavailable hosts and duplicate/late responses are rejected in focused tests. Snapshot and pin-list reads must share a registry revision; after three races GET returns 409 rather than combining inconsistent data. The final integration recheck covered **11/11 UI+HTTP focused cases**; the full Lab count above includes all tests.
- Independent read-only reviews: `/root/arsenal_contract_review` found and rechecked repairs for corrupt-pin error mapping and mutation/snapshot revision consistency (7/7 focused backend+HTTP recheck, no blocker); `/root/arsenal_http_review` found and rechecked template selection, stale session notes, stale UI loads/duplicate pins, API read coherence and version-status labeling (11/11 focused UI+HTTP recheck, no blocker). Backend writer `/root/arsenal_backend`, UI writer `/root/arsenal_ui_panel` and root API integration had disjoint ownership. The UI writer's first intentionally pending-POST regression hung because its promise was awaited before being resolved; that test was terminated, corrected and rerun. It is not counted as a pass. Backend had one repair cycle, UI two, root API one.
- Limits: no actual owner-selected session was derived; fixture notes establish mechanics only. The `local-owner` review label and `sanitized` flag are caller attestations, not independent identity/secret scanning. Host executable observation does not verify login, usage allowance, execution permission or containment. Pins do not run agents or attribute tokens. Browser navigation remains platform-denied, so DOM seams are **not** visual/keyboard evidence (V-01); JEV live, native all-role isolation/usage, clean Windows installer, five real pillar tasks and human-calibrated eval/evolution remain open. No external paid model, credential, publication or product deployment occurred; native delegation token/dollar attribution remains unknown.

## 2026-09-25 — Workflows, terminal windows and memory controls

Reviewed runtime commit: `9e2282e412fe418e551aeb89b56ed7faeed58304`; [PR #6](https://github.com/ymauhn/omniharness/pull/6). Follow-up handoff identity updates do not alter runtime evidence. The PR initially reported CLEAN/MERGEABLE and no configured GitHub checks; the passing local suite is the recorded validation, not a fictitious remote CI pass.

Base `17230c093f933c8cd943216a80bc02eea4530079` after PR #5, branch `codex/mvp-workspace-flows`; documentation backup `9e7a8b74e530d16e5ef673ee8c5964817d841d76` was pushed before closing implementation. Owner requests normal continuation and an interruption-ready [handoff](../omniforge/LATEST-CHECKPOINT.md), with nonblocking visual/provider acceptance deferred.

- Final `pwsh -NoProfile -File ./scripts/check.ps1`: **282/282 Python in 89.655 s**, **130/130 Lab in 18.421 s**, **7/7 mascot in 0.058 s**, zero failures or skips. Both existing portal visual methods, other Node scenarios and eval selftest passed. Aggregate exit **1 solely from the two owner-preserved Claude ask/deny differences**, 27/29 installation rows. Separate graph: **546 nodes / 100 edges / 5 proposals / 0 errors**. `git diff --check` passed. Log `%TEMP%/omniharness-mvp-workspace-final-20260925.log`; existing ConPTY `AttachConsole failed` helper stderr remains visible.
- Saved workflows: five original non-executable suggestions, exact project scope, validated acyclic dependencies, immutable versions/digests, atomic ancestor task creation, pinned snapshots and idempotent request IDs. UI copy/compose actions do not send terminal input or launch models. Explicit task creation starts open tasks; user-supplied review flags are assertions, not authority. 24/24 focused workflow/API tests pass. Unconfirmed IDs survive project switches in one page, not reload; inspect history after reload before creating a new run.
- Terminals: 1–8 panes, tab/project layouts, resizing and token-free same-origin windows. Root replay is volatile and bounded (256 KiB text/512 frames per session, 2 MiB/32 buffers globally); exact HTTP/SSE frames and project identity, cursor/epoch, Unicode and visible loss semantics. Stale SSE cannot wipe confirmed replay; quiet-stream catch-up is coalesced and stops on errors. Closing a pane does not kill its shell. This is not durable full-screen restoration or managed-worker containment.
- Memory: connected scoped edit/history/archive controls, preserved conflict drafts and focus, paginated history before cloning (46-revision red, then pages 20/20/6). Complete note-list/store growth remains a separate scalability limit. Generic state no longer clones private note/workflow histories. Two actual-page integration checks failed before wiring; final 18/18 integration seams pass. DOM fixtures are not browser visual evidence.
- Independent final reviews: memory/replay and Workflows by `/root/codex_tool_bridge` plus `/root/mvp_accounting_review_a`; grid by `/root/codex_tool_bridge` plus `/root/workspace_final_review`; root integration by the latter two, each with 18/18 focused tests. Each reviewer was read-only and did not author its reviewed slice. Grid author is `/root/mvp_accounting_review_a`, workflow original author `/root/mvp_scope_audit`, root authored memory/replay and integration. Reviewers received actual diffs, source and acceptance constraints. Repair counts: memory 2, workflow 3, grid 3; the original two-cycle default was exceeded only for documented narrow focus/reset-visibility closure while feature expansion paused. [Detailed record](../omniforge/WORKSPACE-FLOWS-CHECKPOINT.md) preserves intermediate failures and reassessments.
- Earlier restricted root expansion: 28/29, zero skips, Unicode PTY cleanup failure at PID 31504; grid restricted full run 83/84 at PID 36968, then normal-host 85/85 before later grid repairs. Read-only inspection found neither failed-attempt PID still running. Final integrated normal-host run above supersedes runtime acceptance, not the retained failure record. No broad process kill or browser workaround.

Final runtime SHA-256:

| File | SHA-256 |
|---|---|
| `omniforge-lab/index.html` | `207af7acf7c8d2e0028d146323be1136d0cee2dd5229c0f9d96460a6989d2d34` |
| `omniforge-lab/server.mjs` | `533f461a98d80bab2abb7c97bae92aa0d80b0da0ce658cc1523c2543b2d66300` |
| `omniforge-lab/core.mjs` | `58780391e0eede8fffebcc7805a9e0735c451fbb1350f198c1fdb941b942c0fd` |
| `omniforge-lab/workflows.mjs` | `c3bfcf9d7a60e42e2f679cfd8ea3471ee0e1320a525685a6d2f426dfa91856d4` |
| `omniforge-lab/workflow-panel.mjs` | `e87e19cbf41408b7ff184e4772ad3cb918b45d72d5f0c7205eb005302469ff80` |
| `omniforge-lab/memory-panel.mjs` | `44f9a288dcce15622dc2cacc7eb323102be081e02edfa07177b901b4be84ae6d` |
| `omniforge-lab/terminal-grid.mjs` | `48322338ca69d7d01249d6f51980705f63953f711909670402196e2cad4d8b5f` |
| `omniforge-lab/terminal-output.mjs` | `e08e693af8de3ce413489baea1912838ce49ccfa8e7780df5dc15874c53fbe20` |

No paid provider call, API key, model dispatch, clean-user install, calibrated quality/GA result, release or native managed execution is certified by this checkpoint. Delegate token/dollar usage remains unavailable. V-01 through V-06 and the complete release contract remain active; next implementation is arsenal task/UI integration.

## 2026-09-25 — resident Laya and versioned arsenal checkpoint

Base `af369b248d7cda94a0bd38c36af9373df4809839` (merged [PR #4](https://github.com/ymauhn/omniharness/pull/4)), branch `codex/mvp-local-assistance`. The owner's full-MVP authority and deferred-validation amendment apply. This checkpoint adds the local worker lifecycle, authenticated composer routing and original five-pillar profile registry; it is not full MVP or native managed-executor acceptance.

- Final `scripts/check.ps1`: **282/282 Python in 88.850 s**, **69/69 Lab in 17.557 s**, **7/7 standalone mascot in 0.063 s**, zero skips. Both existing portal visual methods, Node drivers, portal and eval selftest passed. Aggregate exit **1 solely for two owner-preserved Claude `ask`/`deny` installation differences**, now 27/29 conforming rows. Log `%TEMP%/omniharness-mvp-local-assistance-final-20260925.log`. ConPTY helper stderr remains visible. Separate skills graph: 546 nodes, 100 edges, 5 proposals, zero errors. Catalog observation: 133 discovered paths, 115 canonical installed skills, 547 rows, 10 curated overlays, zero issues within discovery scope.
- Worker/API: explicit enable/warm/unload, one active and two waiting requests, bounded protocol/input/output, deadlines and idle unload; filtered child environment and exact runtime/artifact selection. Failed or unconfirmed child cleanup blocks replacement. API resolves candidate metadata itself and validates project/snapshot/closed-choice IDs. Nothing loads on server start. Warm-up inference usage is not included in returned classification token counters, so whole-worker usage remains incomplete.
- Real installed-model [HTTP smoke](../experiments/laya-http-smoke-2026-09-25.json) passed lifecycle checks: initial off and disabled 409, ready in 6.627 s, three requests in 0.631–0.947 s, unload to off and subsequent 409, server closed. Two relevant positive prompts abstained below threshold; the weather negative abstained. `quality_pass=null`. The [first smoke](../experiments/laya-http-smoke-2026-09-25-initial.json) is preserved separately. No quality gain, hardware-energy total, JEV success or zero-cost claim follows. The final report pins current worker/service/API/server hashes.
- Arsenal: five portable built-ins, immutable versions, explicit rule/source review, activation/disable/rollback, task pins, cooperating lock and stale-write rejection. Selected sanitized excerpts only; actor/source classifications are caller assertions, not authentication. All profiles remain `runnable=false`. **15/15** dedicated tests and the system skill structural validator passed. Agent Forge was installed through two collision-checked junctions only; user Claude permissions were unchanged. UI/task-dispatch and a real owner-selected session derivation remain open.
- Red checks and repairs: the worker initially permitted extra fields before inference and mishandled queue overflow. Review repair 1 added stdout/stderr error listeners after an injected pipe error showed an unhandled process-crashing exception; **13/13** final service tests passed. Arsenal review repair 1 retained the prior active version after disable, restoring reviewed rollback without changing existing pins. Composer review repair 1 uses one UTF-8-bounded query for both lexical shortlist and model request; a real controller Node DOM fixture first failed on a long prompt and now covers ASCII and emoji parity. These DOM fixtures are not browser/visual evidence.
- Independent final reviewers: worker/service — `/root/mvp_accounting_review_a` and `/root/codex_tool_bridge`; arsenal — `/root/codex_tool_bridge` and `/root/mvp_scope_audit`; root API/UI — `/root/mvp_accounting_review_a` and `/root/codex_tool_bridge`. Each reviewed the actual relevant source/tests/contracts read-only, with repair rechecks and no remaining blocker. No writer is counted as a reviewer of its own slice. Native review/delegation tokens and monetary totals are unavailable.

Final source SHA-256: `harness/classifier_worker.py` `cc7ebe040e18ac05234a7965690d9d70cb5e84eb26514b37c82abafab37bc721`; `omniforge-lab/classifier-service.mjs` `63e5148f8acda389fa59e1d70f36d93a6c009477a3f20e0f4385a96bb256f6d4`; `omniforge-lab/copilot-classification.mjs` `93b2ebaa880b5d6f422cde632072d1fd0194a1a3821c2b4a25308beb36dbc4fa`; `omniforge-lab/copilot.mjs` `35c7a2d3da6dfcf17690d8a76675e582bc5c67bed8f448338138a4aa60c49479`; `omniforge-lab/server.mjs` `b7ea74d50963b0a454f75e4976832daa1ca66fb6946ae56776cd6add26223d1b`; `harness/agent_arsenal.py` `54222b7a1e1416f6c5dba3818374c6f763d42ce0796e3fdc3a9ed55b843c3911`; `harness/agent_profiles.json` `5d662e27fb8e413396f6b121962ad5d65f6f56948ee65b4d4e59cf8e65987e85`; `.agents/skills/agent-forge/SKILL.md` `eb34eed78055d2d6cc95c667a34f11899e0f6ee8a94e30a5b107c1816f70461b`.

Deferred checks remain in [VALIDATION-PENDING](../omniforge/VALIDATION-PENDING.md): Lab visuals, connected JEV, classifier calibration/host-specific routing, managed boundary, clean install and human/pillar/evolution acceptance. Continue functional implementation under the owner's amendment while keeping unsafe managed admission closed. No further browser attempt or alternate backend followed the platform rejection.

## 2026-09-25 — production Copilot/catalog and typed classifier adapters

Base `0b7e0b0a5c457540dba4a70e313d489fdebe26f7`, branch `codex/mvp-copilot-integration`. Foundation commit `bb27730` was reconciled with remote Phase C docs in `6ef6975`, then merged through [PR #3](https://github.com/ymauhn/omniharness/pull/3). Owner authority covers this continued MVP implementation, scoped Git publication and explicitly deferred nonblocking evidence.

- Final `scripts/check.ps1`: **259/259 Python in 98.354 s**, **51/51 Lab in 16.757 s**, **7/7 standalone mascot in 0.071 s**, zero skips. Both existing portal visual methods, other Node scenarios and eval selftest passed. Aggregate exit **1** remains solely the owner-preserved Claude `ask`/`deny` drift (25/27 installation rows); those settings were not rewritten. Separate graph: **545 nodes / 100 edges / 5 proposals / 0 errors**. `git diff --check` passed. Log: `%TEMP%/omniharness-mvp-copilot-20260925.log`. Existing ConPTY cleanup `AttachConsole failed` stderr remains visible.
- Catalog bridge: paginated authenticated metadata/detail/refresh endpoints, bounded fixed Python subprocess, four-child concurrency limit, atomic snapshot refresh, no skill body/absolute path exposure. New checks: 18 catalog + 4 API Python and 4 Node. A real main-repository Node-to-Python query found 114 canonical installed sources / 546 rows, complete discovery with zero issues. It did not execute a skill. Initial integration failed because the required home discovery argument was omitted; the CLI now supplies it and a real subprocess fixture prevents regression. Malformed `%ZZ` detail IDs initially returned 500; a red HTTP case now returns 400.
- Copilot: bottom composer integration, two SVG skins, deterministic clarification/context/research templates, lexical skill suggestions, pause/selection, preview/apply/undo, scoped context and draft/project revision guards. Initial stale edit-and-restore test failed; revision binding repaired it before freeze. Review repair cycle 1 addressed GET-only refresh and focus lost when loading details were replaced; retained red cases now pass. Final **13/13** seams include the actual asynchronous catalog controller using a minimal Node DOM fixture. This is not a real-browser visual/keyboard verdict.
- Independent final UI reviewers: `/root/mvp_accounting_review_a` and `/root/codex_tool_bridge`, each read-only on the frozen four-file worktree and project constraints, rechecked repair 1 with no remaining blocker. The same two reviewers independently checked the catalog API/backend; malformed-ID repair was rechecked. Classifier writer `/root/mvp_accounting_review_a` received independent reviews from `/root/codex_tool_bridge` and `/root/mvp_scope_audit`; both final verdicts found no blocker on the 20-test adapter slice. Reviewers received actual source/test/contracts; no reviewer of a slice is counted as its writer. Native task usage is unavailable, not zero.
- Classifier repair cycle 1 rejects extra checkpoint-root artifacts and requires the original tokenizer backup to be hash-pinned. Typed closed-choice output never grants execution; invalid/ambiguous responses abstain. JEV has a fixed HTTPS/versioned contract and explicit caller opt-in/key, with no credential discovery, redirect or retry. All **20 offline adapter tests** passed. No live JEV call, credential or credit consumption was requested.
- Laya is actually installed in a separate ignored runtime, with pinned public model artifacts and recorded dependency versions. The [actual adapter smoke](../omniforge/LAYA-LOCAL-OBSERVATION.md) matched **2/3 unique public cases** repeated four times; the regression-test positive abstained consistently. Cold hash/load/first inference 21.384 s, 11 warm median 205.422 ms, final working set ~1.60 GiB. These are smoke/resource observations, not calibrated gains. Resident lifecycle and composer provider controls are the next slice; current production suggestions remain lexical/templates.

Final source SHA-256: `prompt_classifier.py` `73683b5ce23f1f18c6cf170a64823baedf3b2d99fbded63d80596912f25b3c54`; `copilot.mjs` `0eca527c524d27f8fa55b343c2b0c16f85bd7cc32babaec138af011470d6dc74`; `catalog_api.py` `94755297e309d18b62c595e7fe1be6f24bdf713cd2f51ad413f53cf239882462`; `catalog-service.mjs` `b3385b5b1611e7b5bd4d1237ba5d9c004632ccdee3da175a33d3e35ff6818003`; `server.mjs` `bece70f7febf67ee0ea95979963aa7aa58baf6542fb83f75802c96374156e2f6`.

Deferred: real Lab browser/visual/performance acceptance (V-01), JEV live (V-02), broader local classifier calibration and lifecycle integration (V-03), native managed boundary (V-04), clean installer (V-05), human/evolution acceptance (V-06). The owner permits continued implementation, not false passes. Agent arsenal proceeds in a separate worktree. No native managed task, complete token ledger, release or paid provider success is certified here.

## 2026-09-25 — catalog, versioned memory and mascot foundation

Base `2d035343a09ae59800969df62ee0942f9ab386d9`, integration branch `codex/mvp-foundation`. Owner scope: full MVP, minimal routine intervention, repository commits/pushes/PRs/merges, and explicit deferral of nonblocking unavailable validation. Plan SHA-256 `e9bf39f7c3ecceebd4ce0a65efc7aecee8b0ea1bcdecedc3444a89a36f38e55d`. No complete-MVP or human acceptance claim.

- Final `scripts/check.ps1`: **234/234 Python in 110.331 s**, **34/34 Lab in 16.104 s**, **7/7 mascot in 0.068 s**, zero skips; both existing portal visual methods, other Node scenarios and eval selftest passed. Aggregate exit **1 solely from two owner-preserved Claude `ask`/`deny` settings differences**, 25/27 installation rows. Separate graph: 545 nodes, 100 edges, 5 proposals, 0 errors. Log: `%TEMP%/omniharness-mvp-foundation-20260925.log`. ConPTY's existing `AttachConsole failed` helper stderr remains visible despite passing real terminal assertions.
- Catalog: module SHA-256 `65dde11e94571109a3837a225b85909580df7c88b8d6fb23cf1a76d954ca8e02`; 17 new tests plus 7 existing graph tests. Two repair cycles corrected bounded traversal/incomplete discovery, requested-host ranking, snapshot contradictions, denied registry stat and failed-read accounting. The last two added regressions failed 2/2 before repair. Independent reviewers `/root/codex_tool_bridge` and `/root/mvp_accounting_review_a` rechecked the final hash, no material blockers. Main checkout: 130 discovered paths, 114 canonical installed sources, 546 rows, 10 curated, zero issues. [Frozen observation](../skills-graph/catalog-query-observation-2026-09-25.json): positive holdout 3/7 top-one and 7/7 top-three, 2/2 abstentions; human labels pending. This is not certified accuracy/latency. Integrity hashes are not signatures or permissions.
- Memory: core SHA-256 `1c3ec37571c941b1dc1b1126dfb721df8d2a35114ed037c0f250220259b6846e`, server `c1dc86bb9f7638a0760d149287aea8701834b353c89d2a6f84d8e83378a5c8f3`. Writer `/root/mvp_accounting_review_a`; independent reviewers `/root/codex_tool_bridge` and `/root/mvp_scope_audit`, no blockers after repair cycle 1. The 31-note tied-clock case failed before global mutation ordering. Final writer 34/34; reviewer focused 8/8 and 22/22. Restricted initial reviewer run 21/22 retained for existing Unicode PTY shutdown failure; normal-host rerun passed unchanged. [API limits](../omniforge/MEMORY.md): optimistic revisions, exact immutable scope, durable history/archive, migration/rollback and recency; `/forget` retains history, not secure erasure. Editing UI remains open.
- Mascot: writer `/root/mvp_scope_audit`, independent source reviews `/root/codex_tool_bridge` and `/root/mvp_accounting_review_a`. After repair cycle 1 both found no blockers; 7/7 model checks and syntax passed. [Studio evidence](../omniforge/mascot/README.md) retains the initial stale-selection red and fixture correction. Two SVG skins/three palettes, finite motion, pause/selection preview/apply/undo and Off; templates are illustrative, not a production semantic Copilot. Four runtime files total 41,519 bytes / 13,212 gzip bytes; no CPU/FPS claim. Visual/DOM/focus/reduced-motion rendering remains V-01, expressly deferred for continued implementation.
- Model-free Windows readiness reports configured elevated sandbox ready; a root-denied restrictive command failed before any child ran, requiring effective root read. [Readiness](../experiments/codex-windows-readiness-2026-09-25.json) and [inconclusive command](../experiments/codex-elevated-command-canary-2026-09-25.json) are sanitized. No live model/tool turn, descendant containment, billing or managed admission follows. Native delegation consumption remains unknown, not zero.
- Authority/rules/runner changes received separate read-only review from both final memory reviewers, no blockers. User settings and historical failed attempts are preserved. Laya setup is separate in an ignored runtime for the next adapter checkpoint; no paid JEV call, credential read, Higgsfield generation or product deployment occurred. Deferred provider/visual/release checks are in [the final-round register](../omniforge/VALIDATION-PENDING.md).

## 2026-09-25 — MVP-C1: current Codex accounting wire compatibility

Baseline `fa620d61f72c4566c00b802e792b4a04556e809c`; scope `harness/codex_accounting.py` and `tests/test_codex_accounting.py`, plus owner-authority/plan documentation. The owner explicitly approved the full agreed MVP, native browser controls and mascot design as the first real task. Approved plan: `docs/omniforge/PLAN.md`, SHA-256 `f4ea4e61655fcb28a314dcad113df9dec9559e305b6f103430af420feb162b89`. Mascot acceptance brief SHA-256 `d4653d517cc955d5f22b357f67c51cda44f761dd1591b79f060a47468699be7e`.

- Acceptance fixed before correction: absent `jsonrpc` and explicit string `2.0` parse consistently; explicit invalid values and duplicate keys fail; raw hash and unknown turn/descendant spending remain intact. Worker `/root/native_mascot_path` reproduced failures on all-omitted and mixed-envelope fixtures in `codex/mvp-accounting`, then changed the shared notification parser. It reported **30/30** targeted accounting/tool-bridge/exec tests in **1.152 s**, zero skips. No managed model turn was run.
- Final root `scripts/check.ps1`: **217/217 Python tests in 108.563 s**, zero skipped including both existing visual tests; **26/26 Lab Node tests in 16.556 s**, zero skipped; other Node/portal/eval checks passed. Aggregate exit **1 only for the two owner-preserved Claude `ask`/`deny` installation differences**, 25/27 rows conform. Separate graph check: 545 nodes, 100 edges, 5 proposals, 0 errors. Log: `C:/Users/Yeonatan/AppData/Local/Temp/omniharness-mvp-accounting-20260925.log`.
- Independent fresh read-only reviews of the actual two-file code diff: `/root/mvp_accounting_review_a` and `/root/codex_tool_bridge`, each **no blocking finding**. Each ran the focused 10-test accounting suite; the first also repeated it with omitted-envelope fixtures. Neither implemented the change or received the other's verdict. No post-review code repair was required; repair limit 2, repair cycles used 0. Parser visual applicability was declared **not applicable** before implementation because no UI behavior changed. The mascot's visual gate remains separately required and pending.
- Browser authority was explicitly broadened, including an exact owner answer authorising `mcp__cua_repl` for the local Lab. Nevertheless, normal automatic review again denied the visit, claiming the underlying `browser-use` connector differed from that authorised tool. No further browser retry, raw browser command or alternate surface followed. This platform decision is not a new AGENTS restriction and does not block unrelated implementation. No fresh Lab visual verdict is claimed.
- Technical parser acceptance: passed. Human MVP/mascot acceptance: pending. Native delegation token totals, billed/estimated cost and complete child coverage: unknown; no fabricated zero receipt. Worktree isolation is observed Git separation, not OS containment. The full MVP goal remains active; dynamic tools still do not establish exclusive native tool inventory.

## 2026-09-25 — native tool-bridge and OmniForge Lab checkpoint, post-review

Starting from clean `master` / `eedc43588b9187c36c4a0a9a0641ae9c7cbaa9c0`, this pending checkpoint adds model-free Claude MCP-to-ContainerWorker and Codex App Server dynamic-tool bridges, a source-run Windows OmniForge Lab candidate, a documentary Goose comparison, an offline E2 task-graph scorer and the owner's later Copilot/UI design intake. No native agent/model tool turn, subscription-billing reconciliation or complete OS release is claimed.

- Final post-review `scripts/check.ps1` ran **215/215 Python tests in 118.894 s**, zero skipped, including both visual tests; all existing Node Gauntlet/Scout/Swarm scenarios, portal validation, eval selftest and **26/26 Lab Node tests** (zero skipped, 15.343 s) passed. The aggregate command exited **1 solely on the two owner-preserved Claude user-setting `ask`/`deny` installation differences**; 25/27 installation rows conformed. Log: `C:/Users/Yeonatan/AppData/Local/Temp/omniharness-check-20260925-v1-postreview.log`. The separate skills-graph check passed: **545 nodes, 100 edges, 5 waiting proposals, 0 errors**. The earlier interim 215-Python/22- and 24-Lab runs and all prior dated measurements are superseded for this source checkpoint, not erased.
- The first focused Lab run after the inventory/context bounds had **23/24 pass, zero skipped** under the restricted command sandbox: the Unicode PowerShell PTY test timed out waiting for confirmed shutdown, and its exact PID was absent on inspection. Repeating the full 24-test suite under normal host permissions passed **24/24**, zero skipped. After the pane-scope fix, both a focused and the final repository run passed **26/26**, zero skipped. `node-pty@1.1.0` continues to emit `AttachConsole failed` helper diagnostics during disposal even in passing runs; no test is suppressed. The independent real orphan-grandchild counterexample remains documented in [the PTY report](../experiments/omniforge-pty-descendant-2026-09-25.md); the UI's `shell encerrado` label does not certify descendants.
- The [Claude broker probe](../experiments/claude-broker-2026-09-25.json) passed **26/26** real model-free MCP/worker checks in 7.485 s with `task_pass=null`, `tokens=null`, `model_invoked=false`, pinned source hashes and exact container/fixture removal. The [Codex App Server handshake](../experiments/codex-app-server-handshake-2026-09-25.md) succeeded against installed CLI `0.155.0-alpha.16.4` without a thread/turn; dynamic tool dispatch and built-in-tool exclusion remain unproven. Codex bridge contract tests and Claude bridge/probe tests are included in the 215-test battery. A live attempt must additionally pin the executable broker/worker code, not only its JSON binding, before its receipt is called reproducible.
- [E2's offline reference scorer](../evals/E2-TASK-GRAPH.md) passed **8/8** adversarial methods. Its reference hash now binds topology, critical flags, explicit criterion text and acceptance version; an external checker/fixture digest still needs binding before live comparisons. Weights and loop coefficients remain uncalibrated, and no genetic-search gain is claimed. An independent review found and prompted bounded asynchronous asset scanning, a serialized context/note budget, the E2 identity correction and a narrower V1 Claude policy claim. The latest owner request for selection-scoped Prompt Copilot help and the separate two-skin mascot handoff are design documents, not tested product features.
- Earlier source-run Lab behavior was checked manually in the local browser: simultaneous OmniHarness `Build` and `Pesquisa` PTYs had distinct output, a project-scoped note refreshed via SSE in an already-open session, and switching to another project hid that note until returning. The final cross-project pane filter is tested by `pane-scope.mjs` and the authenticated module route, with no new visual-browser verdict: automatic review rejected opening the current localhost Lab via the computer-use browser because `AGENTS.md` routes this project to Claude Browser or Playwright. No alternate browser surface was used after that rejection. There is no packaged installer, live all-role host isolation, complete child/token attribution, B8/B9 or V1 certification. The owner chose native Claude Code terminal plus optional API-key route for the distributed product; [Anthropic's third-party guidance](https://support.claude.com/en/articles/13189465-log-in-to-your-claude-account) means a subscription-backed managed Claude route is not promised.


## 2026-09-25 — model-free worker-adapter isolation checkpoint

Starting from `master` / `b8cebf37772ef92318d8f06aed79c6c82dea195e`, after the native-attempt and Docker-recovery commits. This slice strengthens the disposable Docker process probe and adds the coordinator-owned `ContainerWorker`, its real adapter canary, a Codex Windows profile canary and a Claude startup-inventory parser. The earlier 140/140 result below predates these source changes; this slice's full test result is recorded here. Local commit remains pending as this entry is written.

- Strengthened `python -m harness.sandbox_probe --endpoint npipe:////./pipe/dockerDesktopLinuxEngine --image python@sha256:2f17fc044b579bab302c2e8054d3a686e2cb9a83de48e70534b94cd8ebbe06a9` passed **14/14** on the real Docker Linux engine, zero failed checks, in **4.984 s** after the final fixture-cleanup safety edit. [Durable JSON](../experiments/sandbox-containment-2026-09-25.json) pins probe SHA-256 `940d89f42c2daecb08de4776fa2c1a087b0b0d6a9927e96ce0275999b2086d37`. The two added checks and revised child logic attempt denied reads/writes of synthetic outside paths and inspect network interfaces/routes, then verify host sentinels and actual Git scope. A failed TEST-NET connection or mere path existence no longer suffices; an uncertain `docker create` preserves identity evidence for triage. Fixture cleanup verifies the resolved temp target before recursive removal.
- `python -m harness.container_worker_probe --docker <absolute Docker CLI> --endpoint npipe:////./pipe/dockerDesktopLinuxEngine --image python@sha256:2f17fc044b579bab302c2e8054d3a686e2cb9a83de48e70534b94cd8ebbe06a9` passed **18/18** on the real daemon, zero issues, **5.313 s** after the final edit. The [durable worker report](../experiments/container-worker-2026-09-25.json) pins worker SHA-256 `bbcb324a3a8543032146d1fa9bc21dd04a5adc14889d4f9635d0fc814d60b5c8`, canary SHA-256 `4c68363042bf94779d61460e5d79696746f6c7a7f7e33009aacdfeceefdde71f` and the shared-probe hash above; it records exact owned-container removal, no preserved fixture, a verified workspace artifact and scope audit, and `task_pass=null` despite command exit 0. The observed profile requires private PID/IPC namespaces. This is a model-free container command through the adapter, not a Claude/Codex tool call or an E1 control run.
- Focused fake-Docker `tests.test_container_worker` passed **15/15**; model-free `tests.test_codex_windows_probe` passed **15/15**; `tests.test_claude_tool_inventory` passed **5/5**; shared `tests.test_sandbox_probe` passed **1/1** (**36/36** targeted). These are offline contract tests, not native tool interception. The first `scripts/check.ps1` attempt, before review fixes, ran **173 Python methods, zero skips**, but failed five visual assertions because Chromium could not fetch the portal's public Google Fonts inside the command sandbox (`ERR_NETWORK_ACCESS_DENIED`). This first attempt is not counted as green.
- Final direct `scripts/check.ps1` with the already-authorized public font access, after the fixture-cleanup guard: **176/176 Python methods passed in 95.680 s, zero skips**, including both visual tests. Node Gauntlet/Scout/Swarm, portal checks and eval selftest passed. Install verification reports **25/27** conforming rows, with only the two owner-preserved user Claude `ask`/`deny` settings differences; consequently the aggregate command exits **1**, not 0. Log: `C:/Users/Yeonatan/AppData/Local/Temp/omniharness-check-20260925-isolation-final.log`. The focused cleanup regression also passed **1/1** in **0.522 s**, followed by both renewed real canaries above. The graph check ran separately because the aggregate runner stops at the installation drift: **545 nodes, 100 edges, 5 waiting proposals, 0 errors**, exit 0. `git diff --check` exited 0. The durable probe hashes match the current source files. No skipped test or permission rewrite was used to make the result green.
- The [Codex Windows canary](../experiments/codex-windows-probe-2026-09-25.json) returned **inconclusive**, exit 1. The installed CLI required the elevated backend for restricted read access, so no child report, worker read/write verdict or host-isolation claim was obtained. Its synthetic Codex home gained a `.sandbox` log and `tmp/arg0`; attribution is unresolved, and `host_artifacts_unchanged=false`, rather than a false denial/pass. No administrator setup was attempted. A separate synthetic Claude subscription invocation with `--restricted --tools "" --strict-mcp-config` timed out after 90 seconds before a final result. Its effective tool inventory, usage and billing remain unknown; it is not retried or settled as zero.
- Failed attempts remain evidence: the first adapter canary timed out on cold `docker create` with an empty CID file; exact name/nonce-label inspection found no remaining container. Its timeout was raised from 30 to 60 seconds without an implicit retry. A later canary passed container checks but failed fixture cleanup on a read-only Git object; verified cleanup was corrected and the fixture removed. The successful durable canary followed those fixes. An initial report-save assertion accidentally enumerated PowerShell properties instead of counting them; the count was corrected before the final process report was written. An automatic approval review later interrupted a durable worker-report command due to a usage limit; that rejected command did not execute, and the subsequent authorized run produced the report linked above. An attempted nested `powershell.exe` full-run invocation stopped before tests on a Python `-c` quoting `SyntaxError`; invoking the runner directly produced the final result above.

Remaining gate: connect a bounded native Claude/Codex tool bridge and verify its effective tool inventory, negative host-file canaries, cancellation and usage across all roles/retries. The 14/14 and 18/18 disposable results do not certify an authenticated agent tree, complete descendant accounting, provider billing, E1 controls or B8/B9.

## 2026-09-25 — native Claude/Codex attempt checkpoint and reference intake

Starting from clean `master` / `61e00eae33f9b0390987ecf9206a67154432a659`; `docs/t13/PLAN.md` SHA-256 `aeeac46e5be43f476f63bc79ea7db1727d234e77fdaf8f3c356ccecd826366c6`. Owner said to continue the approved harness-first work. Windows; selected bundled Python **3.12.14**. The eight new runtime/test files implement one fresh native attempt per host, coordinator-owned process evidence, timeout/cancel/recovery, Claude whole-tree result binding and Codex observed-turn/thread counters. The six newly supplied public references were researched and cataloged separately; none was executed or installed.

- Focused final check: `python -m unittest tests.test_native_process tests.test_claude_native tests.test_codex_exec tests.test_codex_accounting -q` using the bundled Python outside the command sandbox: **40/40 passed**, zero skips, 6.276 s. It includes a disposable Windows parent/child process cancellation test, malformed/tampered manifests and streams, false-success attempts, auth-routing settings, provider identity and optional cache-write counters. The same process-tree test records `unknown` inside a sandbox where `taskkill /T` cannot confirm stopping the child; no isolation claim is made from it.
- Final `scripts/check.ps1` using authorized public font access: **140/140 Python tests passed, zero skips, 80.830 s**, including both visual methods. Node **5 Gauntlet, 6 Scout, 7 Swarm** scenarios, portal checks and eval selftest passed. Log: `%TEMP%/omniharness-check-20260925-native.log`.
- Install check: **25/27 rows conform**, with only user Claude `ask`/`deny` differing from the conservative repository fragment. The owner explicitly selected preserving those settings, so the aggregate command exits **1**; no test was skipped or settings rewritten to fabricate a green aggregate. Graph check run separately: **545 nodes, 100 edges, 5 pending proposals, 0 errors**, exit 0. `git diff --check` passed before staging.
- Real **read-only** CLI status preflights using the adapters' filtered environments succeeded on this host: Claude reported `loggedIn=true`, `authMethod=claude.ai`, first-party subscription; Codex reported exactly `Logged in using ChatGPT` on stderr. No prompt/model invocation, provider billing record, credential contents or live agent benchmark was obtained. Offline fake-CLI fixtures are not authenticated model evidence. Attributable current native-agent review/research tokens and money are unavailable, not zero.
- Two independent read-only reviews (`native_review_a`, `native_review_b`) found and verified repairs for auth-provider routing, executable prefix, post-launch orphan handling, provider/role binding, interrupted receipt status, malformed manifest and cache-write consistency. Both gave final **no-blocker** verdicts on this bounded offline slice. One initial launch-failure fixture used an invalid `.exe` and hung under Windows process launch; it was replaced with a deterministic `Popen` failure mock before the final battery, rather than counted as a pass.

Initial Docker containment check **failed**: the Linux engine socket was absent. A nondestructive retry preserved socket-only runtime directories at `C:/Users/Yeonatan/AppData/Local/Docker/run.pre-omniharness-20260925` and `C:/Users/Yeonatan/AppData/Local/docker-secrets-engine.pre-omniharness-20260925`; both still exist. Docker Desktop 4.85.0 recreated a failing `dockerInference` listener and could not start the daemon. An automatic approval review rejected a broad name-based process stop because it could affect preexisting processes; only verified exact newly started PIDs were stopped. No factory reset, recursive delete or credential read occurred. This failed attempt preceded the separate recovery below; the September 21 12/12 result remains historical.

### Later September 25 Docker recovery and current disposable-profile acceptance

Read-only backend logs pointed to the recreated `dockerInference` socket. With **zero Docker processes** active, the resolved `C:/Users/Yeonatan/AppData/Local/Docker/run` directory contained exactly one zero-byte `Archive, ReparsePoint` socket. The source and unique destination were checked under that exact parent, then `run` was moved to `C:/Users/Yeonatan/AppData/Local/Docker/run.pre-omniharness-20260925-2` without deleting it. `docker-secrets-engine` was absent. Docker Desktop was then started hidden; its Linux daemon reported **29.6.2**. The three September 25 backup directories remain preserved.

`harness.sandbox_probe` ran twice successfully with the same pinned image and unchanged source: first 12/12 in 1.766 s; second 12/12 in **1.485 s** to save [durable JSON evidence](../experiments/sandbox-containment-2026-09-25.json) with date and probe SHA-256 `5f7db294bfde75b50a2109bb82d791907ef7d1752bb6a5ee054ba53e7ad567a3`. The checks include source/sibling unchanged, inaccessible outside paths, denied network/root writes, missing Docker socket, non-root, no capabilities, no-new-privileges, seccomp and actual scope audit. The test container was disposable. This is **current offline process-profile acceptance**, not live agent containment, provider accounting, E1 controls or a swarm benchmark. The full 140-test battery predates this host-only recovery; no repository runtime code changed after it.

Remaining gate: connect all native agent/role tool execution to this OS profile, run live provider/tool acceptance, connect the coordinator ledger and E1 controls, and prove or explicitly leave unknown Codex descendant usage before B8/B9 certification. This checkpoint does not complete T13, reconcile billed money or start the OmniForge OS prototype.

## 2026-09-25 — canonical receipts, with explicit host-policy drift

Baseline `ef69c846c209fb6ce8ca3223753ecb0cbbca7335`, Windows, bundled Python **3.12.14**. Runtime changes are limited to eval accounting/provenance and the shared Claude parser; no model or container benchmark ran.

- `scripts/check.ps1` with the normal sandbox: 99 Python methods executed, zero skips; five visual subtest assertions failed on `ERR_NETWORK_ACCESS_DENIED` while loading public fonts. Log: `%TEMP%/omniharness-check-20260925-receipts.log`. This attempt preceded the final review corrections.
- Final `scripts/check.ps1`, with the already-authorized public font access: **100/100 Python methods passed, zero skips, 62.474 s**, including both visual methods. **5 Gauntlet, 6 Scout, 7 Swarm** Node scenarios, portal checks and eval selftest passed. Log: `%TEMP%/omniharness-check-20260925-receipts-complete.log`.
- Installation verification: **23/25 rows conform**; `ask` and `deny` differ in the user Claude settings. The owner explicitly selected “Preservar permissões atuais; registrar divergência de instalação.” Settings, guards and the installer were not changed to force green. Therefore the aggregate command exits **1**, not 0. This is an accepted host-policy difference, not a skipped test or a newly certified conservative installation.
- The graph check was then run separately because the aggregate runner stops at installation failure: **541 nodes, 97 edges, 5 pending proposals, 0 errors**, exit 0. Inventory reflects currently reachable host skills; no skill was installed by this slice.
- Independent final read-only review found no remaining blockers in this accounting slice and reran the 8 new methods: **8/8**, zero skips, 0.900 s. Diff/link checks were completed before commit.

TDD evidence: the initial shared fixture exposed output=1 from main-loop `usage` versus output=10 and total=76 in whole-tree `modelUsage`. Separate red cases reproduced stale-result approval, incompatible resource comparison, huge monetary-counter overflow, missing-hash acceptance and initialization after result. Boundary tests now cover those cases plus failed-call usage, incomplete categories/money, crash zeros, malformed metadata, session/protocol overrides and consistent unresolved receipts. Legacy fixtures/selftest now produce bound receipt evidence rather than overriding scalar costs.

Two command/test-harness mistakes were corrected during work: the process mock initially captured Windows platform discovery, and the first log redirect named an absent local directory. Neither was counted as a successful check. No missing dependency was skipped, installed or substituted. There was no credential access, settings write, public push or external model service call; native delegated review used the owner's allowance, with attributable tokens/money unavailable.

Next acceptance: native Claude/Codex dispatch, usage, cancellation and recovery, then all-role containment and E1 live controls/B8/B9. These tests do not prove native isolation, hook delivery, complete billing or improved task quality. Historical September 21 results below remain unchanged.

Date: 2026-09-21. Host: Windows, bundled Python 3.12.14, Node 24.19.0. Parent checkpoint: T1 commit `1a2289c`. The owner now authorises a local E1/T13 checkpoint; resolve the delivery SHA from Git. Earlier sections record their then-uncommitted state. No push or live model benchmark was run.

## Current checkpoint: task authority and recovered containment

Full `scripts/check.ps1`: **92/92 Python tests, 0 skips, 67.654 s**, both visual methods included; exit 0. Node: **5 Gauntlet, 6 Scout, 7 Swarm** scenarios and portal validation passed. Eval selftest, **25 installation rows** and graph **511 nodes / 92 edges / 5 proposals / 0 errors** passed. Log: `%TEMP%/omniharness-task-authority.log`. The additional installer regression test verifies dry-run, removal of only the blanket Git-commit ask, preservation of specific amend/push/other rules and backup integrity; it was red before implementation and green afterward.

Live Docker probe: **12/12 checks, 1.297 s, exit 0**, engine 29.6.2, official Python image pinned by digest. Evidence with source hash: `../experiments/sandbox-containment-2026-09.json`; runnable `python -m harness.sandbox_probe` command and limits in [ACCOUNTING.md](ACCOUNTING.md). This separately verifies a real offline process profile; it does not claim a native agent/model session already uses that profile.

Docker recovery preserved socket-only directories instead of deleting data; no credentials were read or factory reset performed. First public image pull failed EOF; second succeeded. Settings migration backed up to `.claude/settings.json.pre-omniharness-6`. Native Codex/Astra allowance agents and local task commits have standing authority; external additional-charge services retain their own approval scope. The hard-block Bash guard was not removed because neither retired restriction is implemented there.

## S3 continuation after Claude: current evidence

Final `scripts/check.ps1`: **91/91 Python tests, zero skips, 63.981 seconds**, including both visual methods; exit 0. Node: **5 Gauntlet, 6 Scout, 7 Swarm scenarios** and portal validation passed. Eval selftest and all **25 installation rows** passed. Graph: **511 nodes, 92 edges, 5 pending proposals, 0 errors**. Log: `%TEMP%/omniharness-t13-s3-accounting.log`. `git diff --check` passed; the guard has no diff against restored HEAD. No source change followed this full battery; only evidence/handoff documentation was completed.

The additional 16 Python methods comprise Claude's four guard tests, six durable-accounting tests and six real-worktree tests. They cover concurrent reservation admission, reopen/restart persistence, whole-tree token categories, failed/unknown/over-cap usage, immutable/idempotent settlement, unique sessions, unstarted cancellation, distinct checkouts, dirty-source refusal, forged metadata, committed/staged/untracked/ignored/renamed paths and protected policy files. The staged-path test first failed when a working file restored to base hid the index diff; the audit now checks both independently. Temporary Git fixture commits occur only in disposable repositories, never this workspace.

Envelope tests were corrected after a red case showed assistant snapshot placeholders being treated as final tokens. The manual policy now shares the whole-tree parser. Node tests reject missing/failed isolation preflight before agent calls, reject unverified scope/OS evidence before integration, and release partial-wave reservations only while unstarted. All driver agents are fixtures; these scenarios are not live model/sandbox proof.

The initial reconciliation reproduced six failures and one error in 21 targeted tests after Claude restored the older settings. Preserved the Bash-only never-ask guard, restored auxiliary S4 hooks/read-only permissions and changed installer probes to its block-only contract. The final install uses backup `C:/Users/Yeonatan/.claude/settings.json.pre-omniharness-5` and the updated Swarm driver.

Real historical telemetry reread: seven local streams, six usable whole-tree aggregates, one unresolved. Known subtotal **1,311,288 tokens / US$2.857086 client-estimated**; billed dollars and complete history total remain unknown. Raw evidence was not edited and benchmark validity was not upgraded. Report: `../experiments/swarm-usage-2026-09.json`.

**Unpassed external acceptance:** Docker Desktop 4.85.0 could not initialize its inference runtime socket, and Windows refused a narrow backup rename. No image/container ran, no data was deleted/reset, and OS containment is not certified. Native Windows Claude sandboxing is unsupported; see [ACCOUNTING.md](ACCOUNTING.md). The standard battery has no skipped tests, but it does not include a successful OS/provider acceptance run. S3 live dispatch, E1 control isolation and B8/B9 remain blocked on those independent prerequisites.

## S4 continuation: final evidence

Final `scripts/check.ps1`: **75/75 Python tests, zero skips, 57.292 seconds**, including both visual methods; exit 0. Node: **5 Gauntlet, 6 Scout, 5 Swarm scenarios** and portal validation passed. Eval selftest, **25 installation rows** and the graph check (**511 nodes, 92 edges, 5 pending proposals, 0 errors**) passed. Log: `%TEMP%/omniharness-t13-s4.log`. `git diff --check` passed. The earlier 68-test record below belongs to the preceding slice.

Seven new Python methods cover history idempotence/context/expiry, changed file fingerprints, invalid/private URLs, malformed journal preservation, fixed hook commands, path/activation filters, timeout reporting and malformed input. Driver fixtures cover cold/warm reuse, distinct query IDs, same-line different findings, context mismatches, failed synthesis and malformed/incomplete lens verdicts. The Windows runner fixture now resolves platform discovery before mocking the model process, so it also works alone rather than depending on test order.

Measured deterministic fixture output:

```text
MEASURE scout offline: cold=4 warm=3 calls; reused=2
MEASURE gauntlet offline: cold=3 warm=1 calls; reused=1
```

These are simulated calls, not real tokens, dollars, elapsed model time or quality scores. No live benchmark or session approval was fabricated. The single-coordinator store/skill contract and hook coverage are in [SEEN.md](SEEN.md).

Installed updated Scout/Gauntlet driver copies and all three hook events. The installer preserved unrelated hooks and backed up settings to `C:/Users/Yeonatan/.claude/settings.json.pre-omniharness-4`. Direct JSON subprocess probes of the actual auxiliary scripts ran install/regress and layout/graph checks successfully. Regression output retains six **INCOMPARABLE** groups with no certified baseline; the hook labels command completion separately from baseline comparability. SessionStart/PostToolUse delivery by a live Claude host is still unverified, as is native Codex/Hermes hook parity.

Next dependency: implement and verify the provider-backed reservation/total-usage and worktree-isolation adapter for S3. Corrected B8/B9 measurements depend on it and on E1 live-control isolation; S4 does not close those gates. Portal S6 and T13 closure S7 remain downstream.

## Completed checks

- Red/green CLI tests for the session menu, explicit approval, identity/scope/expiry, three modes, hard stops, budget warning/exhaustion, unknown/stale usage, malformed input, Windows/native-file hook payloads, mode replacement and transcript snapshot deduplication. Directory-wide content searches and escaping globs require a decision.
- Red/green routing tests for provenance-qualified costs, missing prerequisites, exclusion of proposed alternatives, the failed second step in all modes, milestones, unknown cost and bounded attempts; the plan CLI runs against a fixture.
- `python -m unittest tests.test_envelope tests.test_layout tests.test_skills_graph`: **28 passed**, 0 skipped, 9.844 seconds after the final local policy changes.
- Node: **3 Gauntlet, 5 Scout and 5 Swarm scenarios**, plus portal validation, passed. The Swarm scenarios cover dependencies/worktree requests, overlap/cycle rejection, integration/review, conflicts, missing/over-cap accounting, losing candidates, empty tests and milestones. These are fake-agent checks, not live host evidence.
- Eval runner selftest passed. User-scope install: **23 rows passed** (15 links, 3 drivers, 3 permission lists, canonical hook, local decision probes). Graph: **511 nodes, 92 edges, 5 pending proposals, 0 errors**. Existing scanner/test unclosed-file warnings were fixed in the touched code.
- User settings preserved at `C:/Users/Yeonatan/.claude/settings.json.pre-omniharness-3`. Swarm junctions and driver installed; unrelated hooks kept their original matchers. No real workspace envelope created.

## Full battery: completed after owner autonomy amendment

The initial full `scripts/check.ps1` attempt discovered **66 tests**, skipped **0**, and failed after 56.422 seconds: only the two visual methods failed, with five failure records (four viewport/theme subtests and the interaction test). Chromium reported `net::ERR_NETWORK_ACCESS_DENIED` when loading the portal's Google Fonts. This was not marked as a pass, and assertions/fonts were not removed or substituted. Two further local regression tests were added and passed.

The bundled skill-creator's additional `quick_validate.py` initially could not import PyYAML. Automatic approval review rejected installation and font-network access for lack of specific approvals. The owner then explicitly authorised reducing repeated confirmations and configuring the policy. AGENTS.md now records task-scoped authority for necessary dependencies/public test assets; the normal escalation mechanism accepted both actions under that updated authority. PyYAML **6.0.3** was installed in the selected Python. No sandbox or native permission control was disabled.

Final `scripts/check.ps1`: **68/68 Python tests passed, 0 skipped, 62.544 seconds**, including both visual methods. All Node suites, eval selftest, 23 installation rows and the graph check also passed; the command exited 0. The final log is `%TEMP%/omniharness-t13-verified.log`.

The additional bundled skill validator passed for Swarm after its compatibility information was moved into `metadata`: that validator accepts fewer top-level fields than the portable Agent Skills specification. The information was preserved; neither validator nor test assertions were weakened. Both repository skill-layout checks passed after this metadata change, and `git diff --check` is clean. Validation is complete for this local slice; the live-host limitations below remain open.

## Scope limits

Configuration and local JSON probes do not establish actual interception in a live host. Codex/Hermes remain manual policy adapters. S3 refuses paid work until a trusted host enforces reservations and reports total actual usage; its installed driver is not a certification of that missing adapter. No B8/B9 result, live isolation proof or evolutionary improvement is claimed.
