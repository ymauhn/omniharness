# T13 validation record

## 2026-09-25 — MVP-C1: current Codex accounting wire compatibility

Baseline `fa620d61f72c4566c00b802e792b4a04556e809c`; scope `harness/codex_accounting.py` and `tests/test_codex_accounting.py`, plus owner-authority/plan documentation. The owner explicitly approved the full agreed MVP, native browser controls and mascot design as the first real task. Approved plan: `docs/omniforge/PLAN.md`, SHA-256 `f4ea4e61655fcb28a314dcad113df9dec9559e305b6f103430af420feb162b89`. Mascot acceptance brief SHA-256 `d4653d517cc955d5f22b357f67c51cda44f761dd1591b79f060a47468699be7e`.

- Acceptance fixed before correction: absent `jsonrpc` and explicit string `2.0` parse consistently; explicit invalid values and duplicate keys fail; raw hash and unknown turn/descendant spending remain intact. Worker `/root/native_mascot_path` reproduced failures on all-omitted and mixed-envelope fixtures in `codex/mvp-accounting`, then changed the shared notification parser. It reported **30/30** targeted accounting/tool-bridge/exec tests in **1.152 s**, zero skips. No managed model turn was run.
- Final root `scripts/check.ps1`: **217/217 Python tests in 108.563 s**, zero skipped including both existing visual tests; **26/26 Lab Node tests in 16.556 s**, zero skipped; other Node/portal/eval checks passed. Aggregate exit **1 only for the two owner-preserved Claude `ask`/`deny` installation differences**, 25/27 rows conform. Separate graph check: 545 nodes, 100 edges, 5 proposals, 0 errors. Log: `C:/Users/Yeonatan/AppData/Local/Temp/omniharness-mvp-accounting-20260925.log`.
- Independent fresh read-only reviews of the actual two-file code diff: `/root/mvp_accounting_review_a` and `/root/codex_tool_bridge`, each **no blocking finding**. Each ran the focused 10-test accounting suite; the first also repeated it with omitted-envelope fixtures. Neither implemented the change or received the other's verdict. No post-review code repair was required; repair limit 2, repair cycles used 0. Parser visual applicability was declared **not applicable** before implementation because no UI behavior changed. The mascot's visual gate remains separately required and pending.
- Browser authority was explicitly broadened, including an exact owner answer authorising `mcp__cua_repl` for the local Lab. Nevertheless, normal automatic review again denied the visit, claiming the underlying `browser-use` connector differed from that authorised tool. No further browser retry, raw browser command or alternate surface followed. This platform decision is not a new AGENTS restriction and does not block unrelated implementation. No fresh Lab visual verdict is claimed.
- Technical parser acceptance: passed. Human MVP/mascot acceptance: pending. Native delegation token totals, billed/estimated cost and complete child coverage: unknown; no fabricated zero receipt. Worktree isolation is observed Git separation, not OS containment. The full MVP goal remains active; dynamic tools still do not establish exclusive native tool inventory.

## 2026-09-25 — native tool-bridge and OmniForge Lab checkpoint, post-review

Starting from clean `master` / `eedc43588b9187c36c4a0a9a0641ae9c7cbaa9c0`, this pending checkpoint adds model-free Claude MCP-to-ContainerWorker and Codex App Server dynamic-tool bridges, a source-run Windows OmniForge Lab candidate, a documentary Goose comparison, an offline E2 task-graph scorer and the owner's later Copilot/UI design intake. No native agent/model tool turn, subscription-billing reconciliation or complete OS release is claimed.

- Final post-review `scripts/check.ps1` ran **215/215 Python tests in 118.894 s**, zero skipped, including both visual tests; all existing Node Gauntlet/Scout/Swarm scenarios, portal validation, eval selftest and **26/26 Lab Node tests** (zero skipped, 15.343 s) passed. The aggregate command exited **1 solely on the two owner-preserved Claude user-setting `ask`/`deny` installation differences**; 25/27 installation rows conformed. Log: `C:/Users/Yeonatan/AppData/Local/Temp/omniharness-check-20260925-v1-postreview.log`. The separate skills-graph check passed: **545 nodes, 100 edges, 5 waiting proposals, 0 errors**. The earlier interim 215-Python/22- and 24-Lab runs and all prior dated measurements are superseded for this source checkpoint, not erased.
- The first focused Lab run after the inventory/context bounds had **23/24 pass, zero skipped** under the restricted command sandbox: the Unicode PowerShell PTY test timed out waiting for confirmed shutdown, and its exact PID was absent on inspection. Repeating the full 24-test suite under normal host permissions passed **24/24**, zero skipped. After the pane-scope fix, both a focused and the final repository run passed **26/26**, zero skipped. `node-pty@1.1.0` continues to emit `AttachConsole failed` helper diagnostics during disposal even in passing runs; no test is suppressed. The independent real orphan-grandchild counterexample remains documented in [the PTY report](../experiments/omniforge-pty-descendant-2026-09-25.md); the UI's `shell encerrado` label does not certify descendants.
- The [Claude broker probe](../experiments/claude-broker-2026-09-25.json) passed **26/26** real model-free MCP/worker checks in 7.485 s with `task_pass=null`, `tokens=null`, `model_invoked=false`, pinned source hashes and exact container/fixture removal. The [Codex App Server handshake](../experiments/codex-app-server-handshake-2026-09-25.md) succeeded against installed CLI `0.155.0-alpha.16.4` without a thread/turn; dynamic tool dispatch and built-in-tool exclusion remain unproven. Codex bridge contract tests and Claude bridge/probe tests are included in the 215-test battery. A live attempt must additionally pin the executable broker/worker code, not only its JSON binding, before its receipt is called reproducible.
- [E2's offline reference scorer](../evals/E2-TASK-GRAPH.md) passed **8/8** adversarial methods. Its reference hash now binds topology, critical flags, explicit criterion text and acceptance version; an external checker/fixture digest still needs binding before live comparisons. Weights and loop coefficients remain uncalibrated, and no genetic-search gain is claimed. An independent review found and prompted bounded asynchronous asset scanning, a serialized context/note budget, the E2 identity correction and a narrower V1 Claude policy claim. The latest owner request for selection-scoped Prompt Copilot help and the separate two-skin mascot handoff are design documents, not tested product features.
- Earlier source-run Lab behavior was checked manually in the local browser: simultaneous OmniHarness `Build` and `Pesquisa` PTYs had distinct output, a project-scoped note refreshed via SSE in an already-open session, and switching to another project hid that note until returning. The final cross-project pane filter is tested by `pane-scope.mjs` and the authenticated module route, with no new visual-browser verdict: automatic review rejected opening the current localhost Lab via the computer-use browser because `AGENTS.md` routes this project to Claude Browser or Playwright. No alternate browser surface was used after that rejection. There is no packaged installer, live all-role host isolation, complete child/token attribution, B8/B9 or V1 certification. The owner chose native Claude Code terminal plus optional API-key route for the distributed product; [Anthropic's third-party guidance](https://support.claude.com/en/articles/13189465-log-in-to-your-claude-account) means a subscription-backed managed Claude route is not promised.


## 2026-09-25 — model-free worker-adapter isolation checkpoint

Starting from `master` / `b8cebf37772ef92318d8f06aed79c6c82dea195e`, after the native-attempt and Docker-recovery commits. This slice strengthens the disposable Docker process probe and adds the coordinator-owned `ContainerWorker`, its real adapter canary, a Codex Windows profile canary and a Claude startup-inventory parser. The earlier 140/140 result below predates these source changes; this slice's full test result is recorded here. Local commit remains pending as this entry is written.

- Strengthened `python -m harness.sandbox_probe --endpoint npipe:////./pipe/dockerDesktopLinuxEngine --image python@sha256:2f17fc044b579bab302c2e8054d3a686e2cb9a83de48e70534b94cd8ebbe06a9` passed **14/14** on the real Docker Linux engine, zero failed checks, in **4.984 s** after the final fixture-cleanup safety edit. [Durable JSON](../experiments/sandbox-containment-2026-09-25.json) pins probe SHA-256 `940d89f42c2daecb08de4776fa2c1a087b0b0d6a9927e96ce0275999b2086d37`. The two added checks and revised child logic attempt denied reads/writes of synthetic outside paths and inspect network interfaces/routes, then verify host sentinels and actual Git scope. A failed TEST-NET connection or mere path existence no longer suffices; an uncertain `docker create` preserves identity evidence for triage. Fixture cleanup verifies the resolved temp target before recursive removal.
- `python -m harness.container_worker_probe --docker <absolute Docker CLI> --endpoint npipe:////./pipe/dockerDesktopLinuxEngine --image python@sha256:2f17fc044b579bab302c2e8054d3a686e2cb9a83de48e70534b94cd8ebbe06a9` passed **18/18** on the real daemon, zero issues, **5.313 s** after the final edit. The [durable worker report](../experiments/container-worker-2026-09-25.json) pins worker SHA-256 `bbcb324a3a8543032146d1fa9bc21dd04a5adc14889d4f9635d0fc814d60b5c8`, canary SHA-256 `4c68363042bf94779d61460e5d79696746f6c7a7f7e33009aacdfeceefdde71f` and the shared-probe hash above; it records exact owned-container removal, no preserved fixture, a verified workspace artifact and scope audit, and `task_pass=null` despite command exit 0. The observed profile requires private PID/IPC namespaces. This is a model-free container command through the adapter, not a Claude/Codex tool call or an E1 control run.
- Focused fake-Docker `tests.test_container_worker` passed **15/15**; model-free `tests.test_codex_windows_probe` passed **15/15**; `tests.test_claude_tool_inventory` passed **5/5**; shared `tests.test_sandbox_probe` passed **1/1** (**36/36** targeted). These are offline contract tests, not native tool interception. The first `scripts/check.ps1` attempt, before review fixes, ran **173 Python methods, zero skips**, but failed five visual assertions because Chromium could not fetch the portal's public Google Fonts inside the command sandbox (`ERR_NETWORK_ACCESS_DENIED`). This first attempt is not counted as green.
- Final direct `scripts/check.ps1` with the already-authorized public font access, after the fixture-cleanup guard: **176/176 Python methods passed in 95.680 s, zero skips**, including both visual tests. Node Gauntlet/Scout/Swarm, portal checks and eval selftest passed. Install verification reports **25/27** conforming rows, with only the two owner-preserved user Claude `ask`/`deny` settings differences; consequently the aggregate command exits **1**, not 0. Log: `C:/Users/Yeonatan/AppData/Local/Temp/omniharness-check-20260925-isolation-final.log`. The focused cleanup regression also passed **1/1** in **0.522 s**, followed by both renewed real canaries above. The graph check ran separately because the aggregate runner stops at the installation drift: **545 nodes, 100 edges, 5 waiting proposals, 0 errors**, exit 0. `git diff --check` exited 0. The durable probe hashes match the current source files. No skipped test or permission rewrite was used to make the result green.
- The [Codex Windows canary](../experiments/codex-windows-probe-2026-09-25.json) returned **inconclusive**, exit 1. The installed CLI required the elevated backend for restricted read access, so no child report, worker read/write verdict or host-isolation claim was obtained. Its synthetic Codex home gained a `.sandbox` log and `tmp/arg0`; attribution is unresolved, and `host_artifacts_unchanged=false`, rather than a false denial/pass. No administrator setup was attempted. A separate synthetic Claude subscription invocation with `--restricted --tools "" --strict-mcp-config` timed out after 90 seconds before a final result. Its effective tool inventory, usage and billing remain unknown; it is not retried or settled as zero.
- Failed attempts remain evidence: the first adapter canary timed out on cold `docker create` with an empty CID file; exact name/nonce-label inspection found no remaining container. Its timeout was raised from 30 to 60 seconds without an implicit retry. A later canary passed container checks but failed fixture cleanup on a read-only Git object; verified cleanup was corrected and the fixture removed. The successful durable canary followed those fixes. An initial report-save assertion accidentally enumerated PowerShell properties instead of counting them; the count was corrected before the final process report was written. An automatic approval review later interrupted a durable worker-report command due to a usage limit; that rejected command did not execute, and the subsequent authorized run produced the report linked above. An attempted nested `powershell.exe` full-run invocation stopped before tests on a Python `-c` quoting `SyntaxError`; invoking the runner directly produced the final result above.

Remaining gate: connect a bounded native Claude/Codex tool bridge and verify its effective tool inventory, negative host-file canaries, cancellation and usage across all roles/retries. The 14/14 and 18/18 disposable results do not certify an authenticated agent tree, complete descendant accounting, provider billing, E1 controls or B8/B9.

## 2026-09-25 — native Claude/Codex attempt checkpoint and reference intake

Starting from clean `master` / `61e00eae33f9b0390987ecf9206a67154432a659`; `docs/t13/PLAN.md` SHA-256 `aeeac46e5be43f476f63bc79ea7db1727d234e77fdaf8f3c356ccecd826366c6`. Owner said to continue the approved harness-first work. Windows; selected bundled Python **3.12.14**. The eight new runtime/test files implement one fresh native attempt per host, coordinator-owned process evidence, timeout/cancel/recovery, Claude whole-tree result binding and Codex observed-turn/thread counters. The six newly supplied public references were researched and cataloged separately; none was executed or installed.

- Focused final check: `python -m unittest tests.test_native_process tests.test_claude_native tests.test_codex_exec tests.test_codex_accounting -q` using the bundled Python outside the command sandbox: **40/40 passed**, zero skips, 6.276 s. It includes a disposable Windows parent/child process cancellation test, malformed/tampered manifests and streams, false-success attempts, auth-routing settings, provider identity and optional cache-write counters. The same process-tree test records `unknown` inside a sandbox where `taskkill /T` cannot confirm stopping the child; no isolation claim is made from it.
- Final `scripts/check.ps1` using authorized public font access: **140/140 Python tests passed, zero skips, 80.830 s**, including both visual methods. Node **5 Gauntlet, 6 Scout, 7 Swarm** scenarios, portal checks and eval selftest passed. Log: `%TEMP%/omniharness-check-20260925-native.log`.
- Install check: **25/27 rows conform**, with only user Claude `ask`/`deny` differing from the conservative repository fragment. The owner explicitly selected preserving those settings, so the aggregate command exits **1**; no test was skipped or settings rewritten to fabricate a green aggregate. Graph check run separately: **545 nodes, 100 edges, 5 pending proposals, 0 errors**, exit 0. `git diff --check` passed before staging.
- Real **read-only** CLI status preflights using the adapters' filtered environments succeeded on this host: Claude reported `loggedIn=true`, `authMethod=claude.ai`, first-party subscription; Codex reported exactly `Logged in using ChatGPT` on stderr. No prompt/model invocation, provider billing record, credential contents or live agent benchmark was obtained. Offline fake-CLI fixtures are not authenticated model evidence. Attributable current native-agent review/research tokens and money are unavailable, not zero.
- Two independent read-only reviews (`native_review_a`, `native_review_b`) found and verified repairs for auth-provider routing, executable prefix, post-launch orphan handling, provider/role binding, interrupted receipt status, malformed manifest and cache-write consistency. Both gave final **no-blocker** verdicts on this bounded offline slice. One initial launch-failure fixture used an invalid `.exe` and hung under Windows process launch; it was replaced with a deterministic `Popen` failure mock before the final battery, rather than counted as a pass.

Initial Docker containment check **failed**: the Linux engine socket was absent. A nondestructive retry preserved socket-only runtime directories at `C:/Users/Yeonatan/AppData/Local/Docker/run.pre-omniharness-20260925` and `C:/Users/Yeonatan/AppData/Local/docker-secrets-engine.pre-omniharness-20260925`; both still exist. Docker Desktop 4.85.0 recreated a failing `dockerInference` listener and could not start the daemon. An automatic approval review rejected a broad name-based process stop because it could affect preexisting processes; only verified exact newly started PIDs were stopped. No factory reset, recursive delete or credential read occurred. This failed attempt preceded the separate recovery below; the September 21 12/12 result remains historical.

### Later September 25 Docker recovery and current disposable-profile acceptance

Read-only backend logs pointed to the recreated `dockerInference` socket. With **zero Docker processes** active, the resolved `C:/Users/Yeonatan/AppData/Local/Docker/run` directory contained exactly one zero-byte `Archive, ReparsePoint` socket. The source and unique destination were checked under that exact parent, then `run` was moved to `C:/Users/Yeonatan/AppData/Local/Docker/run.pre-omniharness-20260925-2` without deleting it. `docker-secrets-engine` was absent. Docker Desktop was then started hidden; its Linux daemon reported **29.6.2**. The three September 25 backup directories remain preserved.

`harness.sandbox_probe` ran twice successfully with the same pinned image and unchanged source: first 12/12 in 1.766 s; second 12/12 in **1.485 s** to save [durable JSON evidence](../experiments/sandbox-containment-2026-09-25.json) with date and probe SHA-256 `5f7db294bfde75b50a2109bb82d791907ef7d1752bb6a5ee054ba53e7ad567a3`. The checks include source/sibling unchanged, inaccessible outside paths, denied network/root writes, missing Docker socket, non-root, no capabilities, no-new-privileges, seccomp and actual scope audit. The test container was disposable. This is **current offline process-profile acceptance**, not live agent containment, provider accounting, E1 controls or a swarm benchmark. The full 140-test battery predates this host-only recovery; no repository runtime code changed after it.

Remaining gate: connect all native agent/role tool execution to this OS profile, run live provider/tool acceptance, connect the coordinator ledger and E1 controls, and prove or explicitly leave unknown Codex descendant usage before B8/B9 certification. This checkpoint does not complete T13, reconcile billed money or start the OmniForge OS prototype.

## 2026-09-25 — canonical receipts, with explicit host-policy drift

Baseline `ef69c846c209fb6ce8ca3223753ecb0cbbca7335`, Windows, bundled Python **3.12.14**. Runtime changes are limited to eval accounting/provenance and the shared Claude parser; no model or container benchmark ran.

- `scripts/check.ps1` with the normal sandbox: 99 Python methods executed, zero skips; five visual subtest assertions failed on `ERR_NETWORK_ACCESS_DENIED` while loading public fonts. Log: `%TEMP%/omniharness-check-20260925-receipts.log`. This attempt preceded the final review corrections.
- Final `scripts/check.ps1`, with the already-authorized public font access: **100/100 Python methods passed, zero skips, 62.474 s**, including both visual methods. **5 Gauntlet, 6 Scout, 7 Swarm** Node scenarios, portal checks and eval selftest passed. Log: `%TEMP%/omniharness-check-20260925-receipts-complete.log`.
- Installation verification: **23/25 rows conform**; `ask` and `deny` differ in the user Claude settings. The owner explicitly selected “Preservar permissões atuais; registrar divergência de instalação.” Settings, guards and the installer were not changed to force green. Therefore the aggregate command exits **1**, not 0. This is an accepted host-policy difference, not a skipped test or a newly certified conservative installation.
- The graph check was then run separately because the aggregate runner stops at installation failure: **541 nodes, 97 edges, 5 pending proposals, 0 errors**, exit 0. Inventory reflects currently reachable host skills; no skill was installed by this slice.
- Independent final read-only review found no remaining blockers in this accounting slice and reran the 8 new methods: **8/8**, zero skips, 0.900 s. Diff/link checks were completed before commit.

TDD evidence: the initial shared fixture exposed output=1 from main-loop `usage` versus output=10 and total=76 in whole-tree `modelUsage`. Separate red cases reproduced stale-result approval, incompatible resource comparison, huge monetary-counter overflow, missing-hash acceptance and initialization after result. Boundary tests now cover those cases plus failed-call usage, incomplete categories/money, crash zeros, malformed metadata, session/protocol overrides and consistent unresolved receipts. Legacy fixtures/selftest now produce bound receipt evidence rather than overriding scalar costs.

Two command/test-harness mistakes were corrected during work: the process mock initially captured Windows platform discovery, and the first log redirect named an absent local directory. Neither was counted as a successful check. No missing dependency was skipped, installed or substituted. There was no credential access, settings write, public push or external model service call; native delegated review used the owner's allowance, with attributable tokens/money unavailable.

Next acceptance: native Claude/Codex dispatch, usage, cancellation and recovery, then all-role containment and E1 live controls/B8/B9. These tests do not prove native isolation, hook delivery, complete billing or improved task quality. Historical September 21 results below remain unchanged.

Date: 2026-09-21. Host: Windows, bundled Python 3.12.14, Node 24.19.0. Parent checkpoint: T1 commit `1a2289c`. The owner now authorises a local E1/T13 checkpoint; resolve the delivery SHA from Git. Earlier sections record their then-uncommitted state. No push or live model benchmark was run.

## Current checkpoint: task authority and recovered containment

Full `scripts/check.ps1`: **92/92 Python tests, 0 skips, 67.654 s**, both visual methods included; exit 0. Node: **5 Gauntlet, 6 Scout, 7 Swarm** scenarios and portal validation passed. Eval selftest, **25 installation rows** and graph **511 nodes / 92 edges / 5 proposals / 0 errors** passed. Log: `%TEMP%/omniharness-task-authority.log`. The additional installer regression test verifies dry-run, removal of only the blanket Git-commit ask, preservation of specific amend/push/other rules and backup integrity; it was red before implementation and green afterward.

Live Docker probe: **12/12 checks, 1.297 s, exit 0**, engine 29.6.2, official Python image pinned by digest. Evidence with source hash: `../experiments/sandbox-containment-2026-09.json`; runnable `python -m harness.sandbox_probe` command and limits in [ACCOUNTING.md](ACCOUNTING.md). This separately verifies a real offline process profile; it does not claim a native agent/model session already uses that profile.

Docker recovery preserved socket-only directories instead of deleting data; no credentials were read or factory reset performed. First public image pull failed EOF; second succeeded. Settings migration backed up to `.claude/settings.json.pre-omniharness-6`. Native Codex/Astra allowance agents and local task commits have standing authority; external additional-charge services retain their own approval scope. The hard-block Bash guard was not removed because neither retired restriction is implemented there.

## S3 continuation after Claude: current evidence

Final `scripts/check.ps1`: **91/91 Python tests, zero skips, 63.981 seconds**, including both visual methods; exit 0. Node: **5 Gauntlet, 6 Scout, 7 Swarm scenarios** and portal validation passed. Eval selftest and all **25 installation rows** passed. Graph: **511 nodes, 92 edges, 5 pending proposals, 0 errors**. Log: `%TEMP%/omniharness-t13-s3-accounting.log`. `git diff --check` passed; the guard has no diff against restored HEAD. No source change followed this full battery; only evidence/handoff documentation was completed.

The additional 16 Python methods comprise Claude's four guard tests, six durable-accounting tests and six real-worktree tests. They cover concurrent reservation admission, reopen/restart persistence, whole-tree token categories, failed/unknown/over-cap usage, immutable/idempotent settlement, unique sessions, unstarted cancellation, distinct checkouts, dirty-source refusal, forged metadata, committed/staged/untracked/ignored/renamed paths and protected policy files. The staged-path test first failed when a working file restored to base hid the index diff; the audit now checks both independently. Temporary Git fixture commits occur only in disposable repositories, never this workspace.

Envelope tests were corrected after a red case showed assistant snapshot placeholders being treated as final tokens. The manual policy now shares the whole-tree parser. Node tests reject missing/failed isolation preflight before agent calls, reject unverified scope/OS evidence before integration, and release partial-wave reservations only while unstarted. All driver agents are fixtures; these scenarios are not live model/sandbox proof.

The initial reconciliation reproduced six failures and one error in 21 targeted tests after Claude restored the older settings. Preserved the Bash-only never-ask guard, restored auxiliary S4 hooks/read-only permissions and changed installer probes to its block-only contract. The final install uses backup `C:/Users/Yeonatan/.claude/settings.json.pre-omniharness-5` and the updated Swarm driver.

Real historical telemetry reread: seven local streams, six usable whole-tree aggregates, one unresolved. Known subtotal **1,311,288 tokens / US$2.857086 client-estimated**; billed dollars and complete history total remain unknown. Raw evidence was not edited and benchmark validity was not upgraded. Report: `../experiments/swarm-usage-2026-09.json`.

**Unpassed external acceptance:** Docker Desktop 4.85.0 could not initialize its inference runtime socket, and Windows refused a narrow backup rename. No image/container ran, no data was deleted/reset, and OS containment is not certified. Native Windows Claude sandboxing is unsupported; see [ACCOUNTING.md](ACCOUNTING.md). The standard battery has no skipped tests, but it does not include a successful OS/provider acceptance run. S3 live dispatch, E1 control isolation and B8/B9 remain blocked on those independent prerequisites.

## S4 continuation: final evidence

Final `scripts/check.ps1`: **75/75 Python tests, zero skips, 57.292 seconds**, including both visual methods; exit 0. Node: **5 Gauntlet, 6 Scout, 5 Swarm scenarios** and portal validation passed. Eval selftest, **25 installation rows** and the graph check (**511 nodes, 92 edges, 5 pending proposals, 0 errors**) passed. Log: `%TEMP%/omniharness-t13-s4.log`. `git diff --check` passed. The earlier 68-test record below belongs to the preceding slice.

Seven new Python methods cover history idempotence/context/expiry, changed file fingerprints, invalid/private URLs, malformed journal preservation, fixed hook commands, path/activation filters, timeout reporting and malformed input. Driver fixtures cover cold/warm reuse, distinct query IDs, same-line different findings, context mismatches, failed synthesis and malformed/incomplete lens verdicts. The Windows runner fixture now resolves platform discovery before mocking the model process, so it also works alone rather than depending on test order.

Measured deterministic fixture output:

```text
MEASURE scout offline: cold=4 warm=3 calls; reused=2
MEASURE gauntlet offline: cold=3 warm=1 calls; reused=1
```

These are simulated calls, not real tokens, dollars, elapsed model time or quality scores. No live benchmark or session approval was fabricated. The single-coordinator store/skill contract and hook coverage are in [SEEN.md](SEEN.md).

Installed updated Scout/Gauntlet driver copies and all three hook events. The installer preserved unrelated hooks and backed up settings to `C:/Users/Yeonatan/.claude/settings.json.pre-omniharness-4`. Direct JSON subprocess probes of the actual auxiliary scripts ran install/regress and layout/graph checks successfully. Regression output retains six **INCOMPARABLE** groups with no certified baseline; the hook labels command completion separately from baseline comparability. SessionStart/PostToolUse delivery by a live Claude host is still unverified, as is native Codex/Hermes hook parity.

Next dependency: implement and verify the provider-backed reservation/total-usage and worktree-isolation adapter for S3. Corrected B8/B9 measurements depend on it and on E1 live-control isolation; S4 does not close those gates. Portal S6 and T13 closure S7 remain downstream.

## Completed checks

- Red/green CLI tests for the session menu, explicit approval, identity/scope/expiry, three modes, hard stops, budget warning/exhaustion, unknown/stale usage, malformed input, Windows/native-file hook payloads, mode replacement and transcript snapshot deduplication. Directory-wide content searches and escaping globs require a decision.
- Red/green routing tests for provenance-qualified costs, missing prerequisites, exclusion of proposed alternatives, the failed second step in all modes, milestones, unknown cost and bounded attempts; the plan CLI runs against a fixture.
- `python -m unittest tests.test_envelope tests.test_layout tests.test_skills_graph`: **28 passed**, 0 skipped, 9.844 seconds after the final local policy changes.
- Node: **3 Gauntlet, 5 Scout and 5 Swarm scenarios**, plus portal validation, passed. The Swarm scenarios cover dependencies/worktree requests, overlap/cycle rejection, integration/review, conflicts, missing/over-cap accounting, losing candidates, empty tests and milestones. These are fake-agent checks, not live host evidence.
- Eval runner selftest passed. User-scope install: **23 rows passed** (15 links, 3 drivers, 3 permission lists, canonical hook, local decision probes). Graph: **511 nodes, 92 edges, 5 pending proposals, 0 errors**. Existing scanner/test unclosed-file warnings were fixed in the touched code.
- User settings preserved at `C:/Users/Yeonatan/.claude/settings.json.pre-omniharness-3`. Swarm junctions and driver installed; unrelated hooks kept their original matchers. No real workspace envelope created.

## Full battery: completed after owner autonomy amendment

The initial full `scripts/check.ps1` attempt discovered **66 tests**, skipped **0**, and failed after 56.422 seconds: only the two visual methods failed, with five failure records (four viewport/theme subtests and the interaction test). Chromium reported `net::ERR_NETWORK_ACCESS_DENIED` when loading the portal's Google Fonts. This was not marked as a pass, and assertions/fonts were not removed or substituted. Two further local regression tests were added and passed.

The bundled skill-creator's additional `quick_validate.py` initially could not import PyYAML. Automatic approval review rejected installation and font-network access for lack of specific approvals. The owner then explicitly authorised reducing repeated confirmations and configuring the policy. AGENTS.md now records task-scoped authority for necessary dependencies/public test assets; the normal escalation mechanism accepted both actions under that updated authority. PyYAML **6.0.3** was installed in the selected Python. No sandbox or native permission control was disabled.

Final `scripts/check.ps1`: **68/68 Python tests passed, 0 skipped, 62.544 seconds**, including both visual methods. All Node suites, eval selftest, 23 installation rows and the graph check also passed; the command exited 0. The final log is `%TEMP%/omniharness-t13-verified.log`.

The additional bundled skill validator passed for Swarm after its compatibility information was moved into `metadata`: that validator accepts fewer top-level fields than the portable Agent Skills specification. The information was preserved; neither validator nor test assertions were weakened. Both repository skill-layout checks passed after this metadata change, and `git diff --check` is clean. Validation is complete for this local slice; the live-host limitations below remain open.

## Scope limits

Configuration and local JSON probes do not establish actual interception in a live host. Codex/Hermes remain manual policy adapters. S3 refuses paid work until a trusted host enforces reservations and reports total actual usage; its installed driver is not a certification of that missing adapter. No B8/B9 result, live isolation proof or evolutionary improvement is claimed.
