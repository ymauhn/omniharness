# CONTEXT

Domain terms used in prompts, code and docs. Decisions live in `docs/adr/`; the audit that produced them in `docs/PHASE0_AUDIT.md`.

Latest owner instruction, September 25: implement the entire agreed OmniForge MVP under the [accepted execution checkpoints](docs/omniforge/PLAN.md#authorised-mvp-execution-checkpoints), with mascot design/animation as the first real dogfood task; the goal remains active until all accepted requirements are verified. Browser tool-name restrictions were explicitly removed from AGENTS, but the platform still denied the local native-browser visit after exact connector approval; no alternate surface was attempted. Parallel native work uses separate `codex/mvp-accounting` and `codex/mvp-mascot` worktrees. The accounting-envelope compatibility fix passed 217 Python and 26 Lab tests with zero skips, while the aggregate check retains the two owner-preserved Claude permission differences. Mascot work is an artifact task, not proof of managed executor confinement or complete measured usage. See the latest [validation](docs/t13/VALIDATION.md) and [handoff](docs/t13/TICKET-HANDOFF-CLAUDE.md).

Active September 25 continuation: the owner subsequently asked to parallelize OmniForge development and selected this OmniHarness checkout as its first real project. The [finite V1 contract](docs/omniforge/V1-RELEASE-CONTRACT.md) covers all five pillars; the [local Lab candidate](docs/omniforge/LAB.md) now implements projects/tasks, scoped notes, skills/assets views, three graph views and side-by-side Windows PTY sessions. It remains a source-run comparative candidate, not an installer or native agent executor. Offline Claude MCP-worker and Codex App Server dynamic-tool bridges are validated only at their model-free boundaries; neither certifies exclusive native tool routing, all-role containment, complete token accounting or V1. [E2's task-graph scorer](docs/evals/E2-TASK-GRAPH.md) is also an offline fixture, not calibrated fitness. The owner's [Prompt Copilot/UI requirements](docs/omniforge/PROMPT-COPILOT-AND-OS-EXTENSIONS-2026-09-25.md) and [parallel mascot design handoff](docs/omniforge/MASCOT-DESIGN-HANDOFF-2026-09-25.md) are planned rather than implemented. The owner-preserved Claude permission differences remain separate from zero-skip test results. Current continuation evidence and commit state are in [T13-H1](docs/t13/TICKET-HANDOFF-CLAUDE.md) and [validation](docs/t13/VALIDATION.md).

Earlier checkpoint, 2026-09-25: native Claude/Codex one-attempt process adapters and offline usage/cancel/recovery tests were committed as `89d24baa`; the **140/140 Python, zero-skip** full battery belongs to that earlier slice. After narrow socket-only Docker recovery committed as `b8cebf37`, the strengthened disposable process canary passed **14/14** and the coordinator-owned model-free worker adapter **18/18** after review fixes, both with current source hashes in durable reports. The new focused suites passed **36/36**. Its final full runner passed **176/176 Python methods, zero skips**, both visual tests, Node, portal and eval selftest. Its aggregate exit was **1 solely from two owner-preserved Claude `ask`/`deny` installation differences** (25/27 rows); the separate graph check passed (545 nodes, 100 edges, 5 proposals, 0 errors). The Codex Windows restricted-profile canary was **inconclusive** because it required the elevated backend and changed synthetic Codex-home artifacts whose origin is unresolved; a synthetic Claude no-tools turn timed out without a final receipt, so its usage is unknown. At that checkpoint no native tool bridge, complete Codex descendant accounting, all-role **agent** containment or certified benchmark existed. See [accounting](docs/t13/ACCOUNTING.md), [validation](docs/t13/VALIDATION.md), [process evidence](docs/experiments/sandbox-containment-2026-09-25.json), [worker evidence](docs/experiments/container-worker-2026-09-25.json) and [handoff](docs/t13/TICKET-HANDOFF-CLAUDE.md). Six additional owner-supplied repositories are [assessed for selective intake](docs/omniforge/RESEARCH-2026-09-25.md); none was installed.

Skill addition, 2026-09-25: [checkpoint-build](.agents/skills/checkpoint-build/SKILL.md) is installed for Claude/Codex as an original portable adaptation of the Helix workflow and the AI LABS video. It combines reference-based slices, current behavioral/visual evidence, independent review, bounded repairs and standing task authority; that skill-only checkpoint installed no Stop hook or executor. See [sources and validation](docs/research/helix-2026-09-25.md). Both new links conform; installation reports 25/27 rows, retaining the owner's two permission-policy differences. The newer native adapter status is above.

Earlier implementation, 2026-09-25: E1 v3 uses the coordinator's versioned whole-tree Claude receipt, preserves unknown/billed-versus-estimated values, rejects stale or misbound streams and compares total tokens/estimated USD only with compatible receipt provenance. At that checkpoint **100/100 Python tests, zero skips**, Node suites, eval selftest and graph check passed. The aggregate `scripts/check.ps1` exited 1 on two installation-policy rows (`ask`/`deny`): the owner explicitly chose to preserve the current more permissive Claude settings and document the drift. Do not silently reapply the conservative fragment or describe the aggregate check as green. See [validation](docs/t13/VALIDATION.md) and [handoff](docs/t13/TICKET-HANDOFF-CLAUDE.md). The latest native adapter status is above; all-role containment and live acceptance remain open before OS work.

Earlier owner direction, 2026-09-25: finish E1/T13 accounting, native execution, recovery and isolation before OmniForge desktop prototypes. The later same-day parallel-build request amends that sequence for candidate implementation, not the release evidence gates. OmniForge is an owner-first local workspace for multiple software/game/agency/AEO/SEO projects, Windows first, Claude and Codex with existing allowances, OmniRoute optional. The owner requires projects/tasks, chat, skills/assets graphs, shared scoped memory, concurrent sessions, side-by-side terminals and one generated mini-tool with new logic in the first prototype. See the [current plan](docs/omniforge/PLAN.md), [release contract](docs/omniforge/V1-RELEASE-CONTRACT.md) and [research register](docs/omniforge/RESEARCH-2026-09-25.md). The Lab implements only a subset; tourism is excluded and payment may be deferred.

Current priority (owner, 2026-09-20): evaluation integrity before commercial expansion or evolutionary pilots. Local T1 is recorded in `docs/experiments/codex-parity-2026-09.md`; next slice in `docs/evals/PLAN.md`. ADR 0006 records the five ecosystem pillars and the restricted genetic search space. The broader scientific-factory direction is approved strategy, not an implemented extension of `thesis-review`.

T1 committed as `1a2289c` with complete visual coverage. E1 implements separate execution/task/control outcomes, provenance, offline adversarial fixtures and qualified regression checks (`docs/evals/RECORDS.md`). The derived eight-record audit finds four invalid and four unverified attempts; none is a certified baseline. Live controls await enforceable home isolation. Open T13 with S1/S2 consuming this contract; E2 and genetic search remain future work.

T13 local S1/S2, S4 and S3 offline core: `docs/t13/README.md`, ADR 0005. The menu records explicit session authority; the graph recommends measured eligible alternatives; the experimental Claude driver validates scope/dependencies and stops without its trusted accounting/isolation adapter. S4 adds scoped review history and local hooks (`docs/t13/SEEN.md`). This E1/T13 checkpoint follows T1; resolve its SHA from Git. Native executor integration, S5–S7 and certified benchmarks remain open.

Implementation checkpoint, 2026-09-21: `8e6c20e`. The owner authorised scoped local commits and native Codex/Astra agents under the existing allowance; no repeated paid-call gate applies to those agents. Generic commit asking is removed with backed-up migration. Claude's Bash hard-block guard and native permissions remain. Docker was recovered nondestructively; engine 29.6.2 passed 12 real offline containment checks. Full battery: 92/92 Python, zero skips, all Node/eval/install/graph checks. See `docs/t13/ACCOUNTING.md` for coordinator reservations, whole-tree receipts, worktree audits and the remaining native execution/usage bridge. Historic known usage remains 1,311,288 tokens / US$2.857086 client-estimated, not billing or a valid benchmark. Remote Phase C/D4 plans remain unmerged and downstream; preserve stash `8105be6` and read T13-H1 before continuation.

Research and direction, 2026-09-22: [integration proposal](docs/research/harness-evolution-2026-09/PLAN.md), [sources/audit](docs/research/harness-evolution-2026-09/dossier.md) and [owner decisions](docs/research/harness-evolution-2026-09/decisions.md). The owner selected reliable harness + public demonstration for the next 4–6 weeks, developers as the audience, community purchase/entry as the primary CTA, a skills/templates/tutorial library as the offer, and Claude **and** Codex together for first certification. The proposal links truthful public evidence, two host adapters, isolation/recovery, E1 controls, E2 calibration and library conversion; genetic search remains downstream. Research found stale benchmark claims and a clipboard false-success on the landing, plus E1/swarm telemetry inconsistency. These are planned corrections, not implemented by the research. X coverage was index-only; local protocol schema exists but live complete telemetry is unverified.

Owner autonomy update (2026-09-21): approval is per task/scope, not per supporting command. Necessary dependencies, public documentation/test assets and validation proceed with a progress update. Existing approval must be checked before repeating a question. New paid/consequential actions still need authority; native sandbox controls remain intact. AGENTS.md is the current policy over older blanket pip/network confirmation wording.

- **Layer.** One of the six seams: steering and memory, execution, academic, ingestion, multimodal, security and growth. Layers are routing rows in AGENTS.md, not directories.
- **Skill.** A directory with a SKILL.md under `.agents/skills/`, six spec fields only, progressive disclosure: description at startup, body when invoked, references by phase.
- **Adapter.** A host-specific piece (a Workflow script, a hook, a settings fragment) under `gauntlet/` or `harness/`, installed by `scripts/install.py`.
- **Gate.** An authority/evidence check for a consequential action or new planning direction. Check existing task approval before asking again; routine public reads, dependencies, scoped local commits and native allowance agents are already covered. Host permissions and the Bash hard-block guard remain authoritative. Evidence gates for benchmark certification do not revoke ordinary task authority.
- **Gauntlet.** Hunt, refute, report. A *hunter* is one agent per area that must execute an experiment. A *lens* is one refuter perspective. A finding survives when refuters are a strict minority; a tie is refuted.
- **jaVistos.** The seed of already-known findings (`file:line — title`) passed back so a rerun does not hunt them again. **regras** are owner rules: violating one raises severity; items the owner already decided never resurface.
- **Seen store.** Project-local `.omniharness/seen.jsonl`, written only by the coordinator. Reuse requires the same consumer, scope, complete input fingerprint and a fresh verdict. Incomplete judgments are excluded; previous confirmations remain visible as prior issues, never new successes or presumed fixes.
- **Parecer.** A numbered review (`C6-1`, `C6-2` …) the author answers item by item. A **verdict** is the author's number plus decision.
- **Checkpoint.** `git tag ckpt/<case>/<ts>` on `git stash create || HEAD`, created before any paid run.
- **Ring.** One of the three sources of the skills graph: *installed* (SKILL.md files the hosts read), *catalog* (the marketplace manifest and `docs/catalog/` tables, on disk), *remote* (a dated snapshot of the awesome lists, fetched behind the gate). A *missing* node is an edge endpoint this machine lacks.
- **Edge.** A typed link between two skills: `calls` (parsed from SKILL.md), `precedes`, `feeds`, `alternative-to`, `guided-by` (curated in `skills-graph.toml`), `candidate-for` (a proposal). A **proposal** is a learned edge in `proposals.jsonl` waiting for the owner's `approve`.
- **Plan gate.** The grilling round after `scout`: a hard human-in-the-loop gate. The agent asks the critical questions and stops; the spec, the design direction and the code wait for the owner's explicit yes. Distinct from the credit-and-network gate of invariant 2, which guards spending; this one guards direction.
- **Dossier.** What `scout` writes after the fan-out: references with URL, why they matter, the pattern each shows; then patterns, gaps and recommendations. A **routine** is PLAN.md's numbered list of steps, each naming the installed skill that runs it. A **detour** is one bounded round of three alternatives from three worlds, with a verdict, never a change to the routine.
- **Arm.** One configuration of a benchmark run: the harness arm carries AGENTS.md and the gate; the control arm is a bare project. A **baseline record** is one JSON file per run in `evals/results/`.
- **Capability graph / task graph.** The existing skills graph maps available capabilities. The planned task graph maps task dependencies and execution attempts; centrality weights belong to the fixed reference tasks, not to agent-created retries.

## Where do I look

| Question | Place |
|---|---|
| What rules does the agent read? | `AGENTS.md` (`CLAUDE.md` imports it) |
| The Gauntlet driver and its skill | `gauntlet/` (Claude-only; installed to `~/.claude/workflows/gauntlet-driver.js` and `~/.claude/skills/gauntlet-loop`) |
| Demand to plan | `.agents/skills/scout/` (skill, sources), `scout/scout.workflow.js` (driver, installed as `scout-driver`), `.agents/skills/detour/`; plans in `<repo>/docs/scout/<slug>/` |
| The gate | `harness/settings.json`, `harness/guard_bash.py` |
| The academic layer | `.agents/skills/thesis-review/` (SKILL.md, `scripts/thesis_checks.py`, `references/`) |
| Which skill does what, and what is missing | `docs/skills-graph/` (snapshot, README), `.agents/skills/skills-graph/` (script, curated TOML, proposals) |
| Benchmarks | `evals/run.py`, `evals/cases/*`, `evals/results/`; zero-token tests in `tests/` |
| Optional tools | `docs/integrations/` (one page each, with the gate), `recipes/` (tutorials), `docs/catalog/` (free tiers, community skills, MCP servers) |
| Installing on a new machine or host | `docs/install.md`, `scripts/install.py` |
| Why things are this way | `docs/PHASE0_AUDIT.md`, `docs/adr/` |
