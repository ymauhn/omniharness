# Declared dependency of experiments/nist-longley and of tests/test_nist_longley.py (repository test run).
# The reference run used numpy 2.3.5 (results/run.json). Any numpy >= 1.26 is accepted; a different version is
# an explained environment difference under docs/omniforge/PILLAR-3-SCIENCE.md.
numpy>=1.26
