interface:
  display_name: "Scout"
  short_description: "Demand to dossier to PLAN.md with a routine of skills; researches references first, asks the owner before spending."
  default_prompt: "Use $scout <demand>. Runs the sources one by one on Codex (no Workflow tool), asks before any fan-out, writes docs/scout/<slug>/dossier.md and PLAN.md."
policy:
  allow_implicit_invocation: false
